---
phase: 02-every-planet-every-statement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/text/fold.js
  - src/text/normalize.js
  - src/path/buildPath.js
  - src/render/svg.js
  - src/render/json.js
  - test/e2e/phase2-tracer.test.js
  - test/render/svg.test.js
  - test/path/buildPath.test.js
autonomous: true
requirements: [KAMEA-02, PATH-02, CONS-03, CONS-04]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A statement containing an accented Latin letter produces a sigil whose kept letters are the base letters (É contributes E, which is then struck as a vowel), on any of the seven planets (D-22)."
    - "A number sequence with two consecutive equal digits renders exactly one element carrying class sigil-loop at that cell, in addition to the existing sigil-node circles at that cell (D-17, D-20)."
    - "A run of k consecutive equal digits renders exactly k-1 loop elements at that cell (D-18)."
    - "A repeat landing on the first or last point renders the loop AND the sigil-start circle or sigil-end bar — neither is suppressed (D-19)."
    - "A single-kept-letter statement renders one sigil-node, one sigil-start, and one sigil-end at the same cell, all three present in the markup (D-27)."
    - "For every one of the seven planets, each Pythagorean digit 1-9 resolves to a cell inside that planet's grid (row and col both at least 0 and less than the kamea order), and the boundary orders 3 (saturn) and 9 (moon) both trace without error."
    - "Every coordinate written into the SVG and into the JSON working is rounded exactly once at its single rounding point — cellCenter for cell centers, roundGeometry for marker geometry including the new loop constants — with no second rounding site introduced for loop geometry."
    - "Struck and kept entries appear in original-statement order, and when one original character folds to two letters (ß to SS) the two derived entries appear in fold order and share the same original character and index (D-25)."
    - statement: "Coincident markers at a single cell (start circle, end bar, and one or more repeat loops) read as separate, countable shapes rather than merging into one indistinguishable blob at the fixed 100x100 viewBox scale (D-18, D-19, D-27)."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT silently discard any input character during folding — every character of the original statement must remain accounted for in the kept letters or in the struck trail with a reason, including characters that fold to nothing."
      status: unverified
    - statement: "MUST NOT suppress, merge, or deduplicate an existing marker element in order to make room for a new one — a repeat loop at the start or end cell is additive, never a replacement for the boundary marker or the per-visit node."
      status: unverified
  artifacts:
    - path: "src/text/fold.js"
      provides: "Per-character, origin-tracked accent folding and the D-23 transliteration map"
      exports: ["foldStatement"]
    - path: "src/path/buildPath.js"
      provides: "PathModel extended with a consecutive-repeat event array derived from the number sequence"
      contains: "repeats"
    - path: "src/render/svg.js"
      provides: "loopLayer emitting one sigil-loop element per extra visit, plus coincident-marker offsetting"
      contains: "sigil-loop"
    - path: "test/e2e/phase2-tracer.test.js"
      provides: "End-to-end assertion that an accented statement with a consecutive repeat renders a loop on a non-Saturn kamea"
  key_links:
    - from: "src/text/normalize.js"
      to: "src/text/fold.js"
      via: "normalize calls foldStatement and classifies the folded stream instead of the raw uppercased string"
      pattern: "foldStatement"
    - from: "src/render/svg.js"
      to: "src/path/buildPath.js"
      via: "loopLayer maps over the PathModel repeats array produced by buildPath"
      pattern: "pathModel\\.repeats"
    - from: "src/render/json.js"
      to: "src/path/buildPath.js"
      via: "toWorking reads path.repeats straight through with no computation"
      pattern: "path\\.repeats"
---

<objective>
Widen the Phase 1 spine so a statement carrying accented letters and a consecutive-digit repeat traces correctly on any of the seven kameas, with the traditional repeat loop rendered as its own semantic element.

Purpose: The repeat loop (D-17, D-18, D-19, D-20) and accent folding (D-22, D-23, D-25) are the two pieces of net-new *behavior* in this phase. Everything else in Phase 2 is documentation, error enrichment, or test coverage over what this plan builds. Getting the folding seam and the PathModel repeat data right first means Plan 02 can harden and document rather than re-architect.

Output: A new `src/text/fold.js`, an extended `normalize`/`buildPath`/`svg`/`json` pipeline, and an end-to-end test proving one accented, repeat-carrying statement traces on a non-Saturn planet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-every-planet-every-statement/02-CONTEXT.md
@.planning/phases/02-every-planet-every-statement/02-RESEARCH.md
@.planning/phases/02-every-planet-every-statement/02-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 2 (this plan and Plan 02). Newly-created symbols are not drift against prior phases:

**Created in this plan (02-01):**
- File `src/text/fold.js`
- Export `foldStatement(statement)` from `src/text/fold.js`
- Module constants `TRANSLITERATION_MAP`, `COMBINING_MARKS` in `src/text/fold.js`
- JSDoc typedef `FoldRecord` (`{ original, originalIndex, folded }`) in `src/text/fold.js`
- Internal function `detectRepeats(numbers)` in `src/path/buildPath.js`
- JSDoc typedef `RepeatEvent` (`{ atPoint, count }`) in `src/path/buildPath.js`
- PathModel field `repeats`
- Internal function `loopLayer(pathModel)` in `src/render/svg.js`
- Module constants `LOOP_RADIUS_FRACTION`, `LOOP_OFFSET_FRACTION`, `LOOP_NEST_STEP_FRACTION` in `src/render/svg.js`
- CSS class `sigil-loop` (public CSS contract, joins the D-08 taxonomy)
- `NormalizeResult` field `keptEntries`; `StruckEntry` fields `original` and `folded`; `KeptEntry` typedef in `src/text/normalize.js`
- `SigilWorking` fields `keptTrail` and `repeats` in `src/render/json.js`
- File `test/e2e/phase2-tracer.test.js`

**Created in Plan 02-02:**
- File `test/text/fold.test.js`
- `SigilError` constructor parameter `details` and instance property `.details` in `src/errors.js`
- File snapshots under `test/__file_snapshots__/` named `matrix-<planet>.svg`
- README sections for the folding rule table and the loop marker

## Flagged assumptions (unresolved edge probes)

The deterministic edge probe returned three rows it could not classify. They are recorded here as explicit assumptions rather than dropped:

| Requirement | Probe | Assumption carried |
|---|---|---|
| PATH-02 | unclassified — review manually | Assumed the only repeat trigger is `numbers[i] === numbers[i-1]` on the final digit sequence; no other notion of "repeat" (letter recurrence, cell revisit from a non-adjacent index) triggers a loop. Locked by the Task 1 acceptance criteria and the `normalize('BK')` fixture. |
| CONS-04 | unclassified — review manually | Assumed folding is total — `foldStatement` never throws for any string input, including lone surrogates and unbounded stacked combining marks. Verified defensively in Plan 02-02. |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end accented-statement-with-repeat sigil on a non-Saturn kamea — one path only</name>
  <reversibility rating="costly">Adds `sigil-loop` to the public CSS contract (D-08) and `repeats`/`keptTrail` to the consumed working schema (D-14); removing either later breaks embedding sites and programmatic consumers.</reversibility>
  <read_first>
    - src/text/normalize.js (current classification loop and StruckEntry typedef — the shape being extended)
    - src/path/buildPath.js (current PathModel construction — the object the repeats field attaches to)
    - src/render/svg.js (nodeLayer/startMarker/endMarker/perpendicularUnit/roundGeometry and the fixed layer array in renderSvg)
    - src/render/json.js (toWorking's fixed key order and its stated no-computation contract)
    - src/render/coords.js (cellSize, cellCenter, formatCoord — the single rounding point)
    - src/generate.js (the single normalize() call site and the pipeline result handed to toWorking)
    - .planning/phases/02-every-planet-every-statement/02-PATTERNS.md (per-file analog assignments and verbatim-ready scaffolds)
    - .planning/phases/02-every-planet-every-statement/02-RESEARCH.md (Pattern 1, Pattern 2, Pitfalls 1-6)
    - test/text/normalize.test.js (existing assertions that must keep passing, notably normalize('BK') keeping both letters)
  </read_first>
  <files>src/text/fold.js, src/text/normalize.js, src/path/buildPath.js, src/render/svg.js, src/render/json.js, test/e2e/phase2-tracer.test.js</files>
  <action>
Wire ONE statement through every layer this phase touches. The tracer statement is `CLARITÉ` on planet `jupiter` (4x4 kamea): it folds É to E, strikes A/I/E as vowels, keeps C, L, R, T, encodes to digits 3, 3, 9, 2, and therefore carries exactly one consecutive repeat at the second point. Build each layer for keeps — this is production code, not a spike.

Create `src/text/fold.js` exporting `foldStatement(statement)`. Open it with the codebase's standard module-header doc comment naming CONS-04, stating the module has zero knowledge of numbers or kamea geometry, and stating that folding never throws. Declare `TRANSLITERATION_MAP` as a module-level literal object with twelve case-sensitive entries per D-23: ß and ẞ to SS, æ and Æ to AE, œ and Œ to OE, ø and Ø to O, þ and Þ to TH, ð and Ð to D. Declare `COMBINING_MARKS` as a global-flagged character-class regex over the combining-diacritical-marks block. `foldStatement` iterates `[...statement]` (code-point aware, per Pitfall 6) and returns one `FoldRecord` per ORIGINAL character: `{ original, originalIndex, folded }`. For each character, consult the map first; otherwise NFD-normalize that single character and strip its combining marks (D-22). Do not normalize the whole statement before iterating — Pitfall 1: whole-string folding destroys the original-index provenance D-25 requires. Do not rely on native `toUpperCase` to fold ß, per Pitfall 3.

Extend `src/text/normalize.js` to drive its loop from `foldStatement(statement)` instead of from the raw uppercased string. Preserve the existing branch order exactly — non-letter, then vowel, then already-seen — and preserve the existing `kept: string[]` and `struck[]` shapes and their `char`/`index`/`reason` fields so current tests keep passing. For each fold record, uppercase its `folded` string and classify each resulting character independently: `char` is the classified uppercase character, `index` is the fold record's `originalIndex`, and two new fields are added to both struck and kept trails — `original` (the raw pre-fold character) and `folded` (that character's full fold output). Per RESEARCH Open Question 2, when one original expands to two letters both derived entries carry the same `original` and the same `index`. Add a new `keptEntries` array to `NormalizeResult` carrying the same `{ char, index, original, folded }` shape for kept letters, leaving `kept` as a plain string array. A character whose fold output is empty contributes no classified entry, so push a struck entry with reason `non-letter` and an empty `char` for it — nothing is silently discarded. Add JSDoc typedefs for `FoldRecord` and `KeptEntry` in the established style; add no runtime validation library.

Extend `src/path/buildPath.js` with an internal `detectRepeats(numbers)` pass run over the NUMBER array, never over letters (Pitfall 2 / PITFALLS.md Pitfall 7 — `normalize('BK')` keeps both letters yet B and K both encode to 2). Walk runs of consecutive equal values and, for each run of length k greater than 1, push one `{ atPoint, count }` event where `atPoint` is the index of the LAST point in the run and `count` is k-1, so there is one loop per extra visit (D-18). Attach the result as a new `repeats` field on the returned PathModel using the same idiom `points` and `segments` already use. Add a `RepeatEvent` typedef and extend the `PathModel` typedef. State in the doc comment that repeat detection runs on numbers, not letters, and why.

Extend `src/render/svg.js` with a `loopLayer(pathModel)` function shaped like the existing `nodeLayer`. Add three geometry constants alongside `NODE_RADIUS_FRACTION`/`START_RADIUS_FRACTION`/`END_BAR_LENGTH_FRACTION`, expressed as fractions of `cellSize` and never as absolute units: `LOOP_RADIUS_FRACTION = 0.09`, `LOOP_OFFSET_FRACTION = 0.14`, `LOOP_NEST_STEP_FRACTION = 0.05`. For each repeat event emit `count` elements, each a `<path>` carrying class `sigil-loop` drawn as an open elliptical arc (a single `A` arc command with the large-arc and sweep flags set so it reads as a curl rather than a closed ring, per D-17), centered on the point offset outward along `perpendicularUnit` of the incoming segment. Round every derived value through the existing `roundGeometry` and emit coordinates through `formatCoord`. Paint with `stroke="var(--sigil-marker-stroke, currentColor)"` and `stroke-width="var(--sigil-stroke-width, 2)"` and `fill="none"` — the same custom properties the existing markers use, no literal color, no inline presentation attribute (Pitfall 8). Append `loopLayer(pathModel)` to the fixed layer array in `renderSvg` AFTER `endMarker(pathModel)`, without reordering the existing entries. Do not modify `nodeLayer` — the per-visit nodes stay (Pitfall 5, D-06). Multi-loop nesting and coincident-boundary offsetting are Task 2; for this task a single loop at a non-boundary point is sufficient and the offset machinery must be in place so Task 2 fills the gap without restructuring.

Extend `src/render/json.js` by appending two straight read-throughs after the existing `end` key, preserving the existing key order for byte stability: `keptTrail` from the normalize result's `keptEntries`, and `repeats` from `path.repeats`. Compute nothing in this module — thread `keptEntries` through the `GeneratePipelineResult` typedef and through the object `generate.js` already builds. Extend the `SigilWorking` typedef accordingly.

Create `test/e2e/phase2-tracer.test.js` following the structure of `test/e2e/saturn-tracer.test.js`.
  </action>
  <verify>
    <automated>npx vitest run test/e2e/phase2-tracer.test.js test/text/normalize.test.js test/path/buildPath.test.js test/render/svg.test.js test/render/json.test.js && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/text/fold.js` exists and exports `foldStatement`; `grep -c "'ẞ'" src/text/fold.js` returns at least 1, proving the capital sharp-s has its own map entry per D-23 and Pitfall 3.
    - `node -e "import('./src/text/fold.js').then(m=>console.log(JSON.stringify(m.foldStatement('ßÉ'))))"` prints two records whose `folded` values are `SS` and `E` respectively and whose `originalIndex` values are 0 and 1.
    - `src/text/normalize.js` contains `foldStatement` as an imported call, and the existing test `normalize('BK')` still yields `['B','K']` (Pitfall 2 boundary preserved).
    - `normalize('CLARITÉ')` yields `kept` equal to `['C','L','R','T']` and a struck entry whose `original` is `É`, whose `char` is `E`, and whose `reason` is `vowel` (D-22, D-25).
    - `buildPath` output for digits `[3,3,9,2]` has `repeats` equal to `[{ atPoint: 1, count: 1 }]` (D-18, PATH-02).
    - `buildPath` output for digits `[5,5,5]` has a single repeat event with `count` equal to 2 (D-18).
    - `generateSigil('CLARITÉ','jupiter').svg` contains exactly one occurrence of `class="sigil-loop"` and contains `sigil--jupiter` (D-17, D-20, KAMEA-02).
    - `generateSigil('CLARITÉ','jupiter').svg` still contains four `sigil-node` circles, one `sigil-start`, and one `sigil-end` — the loop is additive (Pitfall 5, D-06).
    - `generateSigil('CLARITÉ','jupiter').working.repeats` equals `[{ atPoint: 1, count: 1 }]` and `.working.keptTrail` has four entries (D-20, D-25).
    - The CLI subprocess `node bin/sigil-spinner.js 'CLARITÉ' --planet jupiter` writes stdout byte-identical to the library `svg` value (INT-02 parity preserved).
    - `npm run typecheck` and `npm run lint` both exit 0.
    - Existing test files `test/render/svg.test.js`, `test/path/buildPath.test.js`, `test/render/json.test.js`, `test/determinism.test.js` all still pass unchanged.
  </acceptance_criteria>
  <done>A single accented statement carrying a consecutive digit repeat generates a correct Jupiter sigil with one loop element, end to end through library and CLI, and the change is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Countable multi-loops, coincident boundary markers, and the single-node sigil</name>
  <reversibility rating="costly">The rendered form of the loop becomes part of the visual contract for sigils already embedded on live sites; determinism means changing the offset geometry changes output for identical inputs (D-17).</reversibility>
  <read_first>
    - src/render/svg.js (the state left by Task 1 — loopLayer, the three LOOP_* constants, perpendicularUnit and its zero-length fallback, startMarker, endMarker)
    - src/path/buildPath.js (the repeats array shape Task 1 produced)
    - test/render/svg.test.js (existing anatomy assertions, notably the five-sigil-node count and the fixed layer-order test, which must keep passing)
    - test/path/buildPath.test.js (existing one-point PathModel assertions)
    - .planning/phases/02-every-planet-every-statement/02-CONTEXT.md (D-18, D-19, D-27 and the "draw every fact, offset for legibility, suppress nothing" tie-breaker)
  </read_first>
  <files>src/render/svg.js, test/render/svg.test.js, test/path/buildPath.test.js</files>
  <behavior>
    - A digit sequence with a run of three equal values renders two `sigil-loop` elements at that cell, at different radii or offsets so they are individually countable (D-18).
    - A repeat whose `atPoint` equals `pathModel.start` renders both a `sigil-start` circle and the loop element(s); neither is omitted (D-19).
    - A repeat whose `atPoint` equals `pathModel.end` renders both a `sigil-end` line and the loop element(s) (D-19).
    - A one-point PathModel renders exactly one `sigil-node`, one `sigil-start`, and one `sigil-end`, with the start circle and end bar geometrically offset from each other rather than drawn on the identical center (D-27).
    - Two identical calls produce byte-identical SVG, including all loop geometry (INT-03).
    - No `style=` attribute and no literal color appear anywhere in the emitted markup; the existing paint-attribute test continues to pass (Pitfall 8).
  </behavior>
  <action>
Complete the coincident-marker geometry that Task 1 left as a single-loop happy path. Work only inside `src/render/svg.js` plus the two test files.

For multi-loop nesting (D-18): in `loopLayer`, when a repeat event's `count` exceeds 1, emit each loop at an increasing distance from the point center, stepping the offset by `LOOP_NEST_STEP_FRACTION * cellSize` per additional loop and growing the arc radius by the same step, so `count` loops are visually countable rather than stacked on identical geometry. Derive the offset direction from `perpendicularUnit` of the segment entering `atPoint`, reusing the existing zero-length fallback of a fixed `{ x: 1, y: 0 }` so a one-point model stays deterministic.

For boundary coincidence (D-19): when a repeat event's `atPoint` equals `pathModel.start` or `pathModel.end`, add one extra `LOOP_OFFSET_FRACTION * cellSize` of displacement before the nesting steps begin, so the loop clears the start circle or end bar. Do not add a branch that skips emitting the loop, and do not add a branch that skips emitting the boundary marker — the CONTEXT tie-breaker is that every fact is drawn and geometry moves to make room.

For the single-node sigil (D-27): `startMarker` and `endMarker` already resolve to the same point when `points.length` is 1. Offset the end bar's center by `LOOP_OFFSET_FRACTION * cellSize` along the fixed fallback direction in that one-point case only, so the crossbar and the start circle are both readable at the shared cell. Leave the multi-point geometry of both markers exactly as it is — the existing worked-example snapshot must not change.

Write the tests first, as the behavior block above enumerates them, then implement until they pass. Add the new assertions to `test/render/svg.test.js` in the existing `describe` style, and add a repeat-run assertion for a three-equal-digit sequence to `test/path/buildPath.test.js`. Do not modify the existing worked-example snapshot file; if it changes, the multi-point geometry was altered and the change is wrong.
  </action>
  <verify>
    <automated>npx vitest run && npm run typecheck && npm run lint</automated>
    <human-check>Open the SVG produced by `node bin/sigil-spinner.js 'CLARITÉ' --planet jupiter` in a browser and confirm the repeat marker reads as a small loop or curl at the cell, per D-17, and not as a notch, chevron, or closed ring. For a triple-repeat statement, confirm the two loops are individually countable rather than overlapping into one shape.</human-check>
  </verify>
  <acceptance_criteria>
    - A PathModel built from digits `[5,5,5]` renders exactly two `sigil-loop` elements, and their emitted arc radius values are not all equal (D-18 countability).
    - A statement whose first two digits are equal renders both one `sigil-start` element and at least one `sigil-loop` element in the same SVG (D-19).
    - A statement whose last two digits are equal renders both one `sigil-end` element and at least one `sigil-loop` element in the same SVG (D-19).
    - A one-kept-letter statement renders exactly one `sigil-node`, one `sigil-start`, and one `sigil-end`, and the `sigil-end` line's midpoint coordinates differ from the `sigil-start` circle's `cx`/`cy` (D-27, CONS-03).
    - `test/__file_snapshots__/worked-example.svg` is unchanged by this task — `git diff --exit-code test/__file_snapshots__/worked-example.svg` exits 0.
    - The existing test named `emits exactly five sigil-node elements, including both at the twice-visited cell` still passes (Pitfall 5).
    - The existing test named `assembles layers in the fixed order: path, then nodes, then start, then end` still passes or is updated only to append the loop layer at the end of the expected order.
    - The existing test named `never emits an inline style attribute` still passes for a statement containing loops.
    - `npx vitest run` exits 0 with no skipped tests; `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Multi-loop nesting, boundary coincidence, and the single-node dual marker all render every fact with legible offsets, proven by tests, with the Phase 1 worked-example snapshot byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `generateSigil(statement, planet, options)` | Arbitrary untrusted string reaches the text layer; the statement is the only attacker-controlled input in the system |
| CLI argv / stdin → library | Same untrusted string arriving through a second entry point; must hit identical validation (INT-04) |
| library → emitted SVG string | Untrusted text can reach markup only through the optional `<title>` element |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | `src/text/fold.js` regex `COMBINING_MARKS` and the existing `VOWELS`/`NON_LETTER` regexes | medium | mitigate | Keep every regex in this phase a bare character class or Unicode property class with no nested quantifier and no alternation, so no backtracking blowup is reachable. Task 1 introduces exactly one new regex and it is a single global character class. |
| T-02-02 | Denial of Service | `foldStatement` on a statement with unbounded stacked combining marks ("Zalgo" input) | low | mitigate | The per-character fold strips all marks for one base character in one bounded pass, linear in the number of marks. No combinatorial path exists. A stress vector is added in Plan 02-02. |
| T-02-03 | Tampering | `renderSvg` `<title>` element, `src/render/escapeXml.js` | high | mitigate | Already mitigated in Phase 1 by `escapeXml`. This plan adds no new raw-text-into-markup path — `loopLayer` emits pure numeric geometry with no text content. The existing `embeds the XML-escaped statement when the title option is enabled` test is the regression guard and must keep passing. |
| T-02-04 | Information Disclosure | JSON working `keptTrail` and extended struck entries | low | accept | The working intentionally echoes the caller's own statement and its derivation. It is returned to the caller who supplied it, never persisted or transmitted. `src/render/json.js` already documents that consumers must escape `working.statement` before rendering it into HTML. |
| T-02-05 | Repudiation | Unicode confusables folding visually distinct inputs to the same letters | low | accept | Not a security boundary for this tool. Deterministic, documented folding is the intended contract (D-22); identical folded input producing an identical sigil is the feature, not a vulnerability. |
| T-02-SC | Tampering | npm installs | high | mitigate | This phase installs zero new packages (RESEARCH: Package Legitimacy Audit not applicable). No package-manager install task exists in this plan, therefore no legitimacy checkpoint is required. Regression guard: the runtime dependency set stays empty. |

Canon-referral breadcrumb: injection and prototype-pollution style concerns are canon security items covered by `/gsd-secure-phase` and eslint security rules; they are recorded here rather than minted as bespoke prohibitions.
</threat_model>

<verification>
- `npx vitest run` passes with every pre-existing Phase 1 test still green.
- `npm run typecheck` and `npm run lint` exit 0.
- `node bin/sigil-spinner.js 'CLARITÉ' --planet jupiter` writes an SVG containing `sigil--jupiter` and one `sigil-loop` element to stdout and nothing to stderr.
- `test/__file_snapshots__/worked-example.svg` and `test/__file_snapshots__/worked-example.working.json` are byte-unchanged except for the two appended working keys, confirming no geometry drift for existing multi-point sigils.
</verification>

<success_criteria>
- Accent folding is implemented per-character with original-index provenance intact (D-22, D-23, D-25).
- Consecutive-repeat detection runs over the number sequence and produces one loop event per extra visit (PATH-02, D-18).
- The loop renders as its own `sigil-loop` element, additively alongside nodes and boundary markers (D-17, D-19, D-20).
- A one-kept-letter statement renders a complete sigil anatomy at a single cell (D-27, CONS-03).
- A non-Saturn kamea is exercised end to end through library and CLI (KAMEA-02).
</success_criteria>

<output>
Create `.planning/phases/02-every-planet-every-statement/02-01-SUMMARY.md` when done
</output>
