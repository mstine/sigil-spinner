---
phase: 01-first-sigil-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/errors.js
  - src/text/normalize.js
  - src/path/buildPath.js
  - src/render/coords.js
  - src/render/svg.js
  - src/render/escapeXml.js
  - src/generate.js
  - src/index.js
  - src/data/kamea.js
  - bin/sigil-spinner.js
  - test/text/normalize.test.js
  - test/path/buildPath.test.js
  - test/render/coords.test.js
  - test/render/svg.test.js
  - test/e2e/saturn-tracer.test.js
autonomous: true
requirements: [CONS-01, PATH-01, PATH-03, REND-01, INT-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running `node bin/sigil-spinner.js \"I WILL SUCCEED\" --planet saturn` writes a self-contained, viewBox-based inline SVG to stdout and writes nothing else to stdout (REND-01, INT-02)"
    - "normalize() keeps the FIRST occurrence of a repeated letter and strikes every later one — the doubled L and doubled C in \"I WILL SUCCEED\" each survive once, yielding kept letters W, L, S, C, D (CONS-01 adjacency edge)"
    - "Kept letters appear in the order they occur in the original statement; two letters never swap position (CONS-01 ordering edge)"
    - "A statement reducing to one kept letter produces a valid single-point PathModel with start === end; a statement reducing to zero kept letters throws SigilError with code E_EMPTY_SEQUENCE rather than emitting empty markup (CONS-01 empty edge)"
    - "The path traces number sequence 5,3,1,3,4 across Saturn's kamea with a start marker element carrying class sigil-start at the first cell and an end marker element carrying class sigil-end at the last cell (PATH-01, D-05)"
    - "A one-point path places the start marker and the end marker on the same cell — first and last index coincide without error (PATH-01 boundary edge)"
    - "Every coordinate the renderer emits comes from the single shared cellCenter() in src/render/coords.js, rounded once to a fixed decimal precision, so identical input yields byte-identical coordinate strings on any platform (PATH-01 precision edge, Pitfall 10)"
    - "A cell visited twice non-consecutively — Saturn cell (1,0) in the worked example — produces two distinct path vertices with a segment drawn back to it; the vertices are not merged or deduplicated (REND-01 adjacency edge)"
    - "The single-point sigil renders a real node element rather than a path element with an empty geometry attribute (REND-01 empty edge)"
    - "SVG layers are emitted in one fixed documented order — path, then nodes, then start marker, then end marker — so repeated runs produce byte-identical markup (REND-01 ordering edge)"
    - "The generated SVG carries no inline styling attribute and every stroke/fill value is a var(--sigil-*) reference with a fallback, not a bare literal (REND-01, Pitfall 8)"
    - "The root element carries class \"sigil sigil--saturn\" and children carry sigil-path, sigil-node, sigil-start, sigil-end (D-08)"
    - "viewBox is \"0 0 100 100\" for every planet and cell size is the viewBox extent divided by the grid order (D-07)"
    - "generateSigil(statement, planet, options) is importable from src/index.js and returns a plain object with svg and working keys (INT-01, D-13)"
    - "The intention statement appears nowhere in the generated SVG unless the caller passes the title option, and when it does it is XML-escaped (D-16)"
  prohibitions:
    - statement: "MUST NOT embed the practitioner's intention statement in the SVG artifact by default — the statement appears in generated output only on explicit caller opt-in, honoring the release-the-intention posture of sigil practice"
      status: unresolved
    - statement: "MUST NOT silently discard characters from the statement — every character removed must be retained with the reason it was struck, so the practitioner can audit the derivation rather than trust it"
      status: unresolved
  artifacts:
    - path: "src/text/normalize.js"
      provides: "Vowel and repeat striking, order-preserving, struck letters tagged with reason"
      exports: ["normalize"]
    - path: "src/path/buildPath.js"
      provides: "Numbers + cells + planet to renderer-agnostic PathModel"
      exports: ["buildPath"]
    - path: "src/render/coords.js"
      provides: "The single shared (row,col) to viewBox (x,y) transform and coordinate formatter"
      exports: ["cellCenter", "cellSize", "formatCoord"]
    - path: "src/render/svg.js"
      provides: "PathModel to self-contained inline SVG string"
      exports: ["renderSvg"]
    - path: "src/generate.js"
      provides: "Orchestrator wiring normalize, encode, cell lookup, path, render"
      exports: ["generateSigil"]
    - path: "bin/sigil-spinner.js"
      provides: "CLI entry writing the SVG artifact to stdout"
    - path: "test/e2e/saturn-tracer.test.js"
      provides: "End-to-end assertion over the worked example statement"
  key_links:
    - from: "src/generate.js"
      to: "src/data/kamea.js"
      via: "cellForNumber(planet, n) per encoded digit — the only route to kamea geometry"
      pattern: "cellForNumber"
    - from: "src/render/svg.js"
      to: "src/render/coords.js"
      via: "cellCenter() for every emitted coordinate; no inline coordinate arithmetic"
      pattern: "cellCenter"
    - from: "bin/sigil-spinner.js"
      to: "src/index.js"
      via: "imports generateSigil from the public entry only, never an internal module"
      pattern: "src/index.js"
---

<objective>
The walking-skeleton tracer: drive one intention statement through every layer of the pipeline —
normalize, encode, kamea lookup, PathModel, coordinate transform, SVG string, CLI stdout — and then
give the rendered sigil its traditional anatomy (start circle, end bar, node at every visited cell)
under the locked class taxonomy.

Purpose: prove the architecture end-to-end on one real path before widening it. Every module boundary
in ARCHITECTURE.md gets exercised by a real call, so a seam that does not work is discovered now rather
than after three more plans are committed on top of it.

Output: a working `sigil-spinner "I WILL SUCCEED" --planet saturn` producing self-contained,
viewBox-based, fully class-hooked inline SVG on stdout, and an importable `generateSigil()` returning
`{ svg, working }` (D-13).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-first-sigil-end-to-end/01-CONTEXT.md
@.planning/phases/01-first-sigil-end-to-end/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-first-sigil-end-to-end/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and paths created by this plan (none exist before it runs):

| Kind | Symbol / path |
|------|---------------|
| File | `src/errors.js`, `src/text/normalize.js`, `src/path/buildPath.js` |
| File | `src/render/coords.js`, `src/render/svg.js`, `src/render/escapeXml.js` |
| File | `src/generate.js`, `src/index.js`, `bin/sigil-spinner.js` |
| File | `test/text/normalize.test.js`, `test/path/buildPath.test.js`, `test/render/coords.test.js`, `test/render/svg.test.js`, `test/e2e/saturn-tracer.test.js` |
| Class | `SigilError` (with stable `.code`) |
| Error code | `E_EMPTY_SEQUENCE`, `E_UNKNOWN_PLANET` |
| Function | `normalize(statement)` returning kept + struck |
| Function | `buildPath(numbers, cells, planet, order)` returning a PathModel |
| Function | `cellSize(order)`, `cellCenter(row, col, order)`, `formatCoord(n)` |
| Function | `renderSvg(pathModel, options)`, `escapeXml(str)` |
| Function | `generateSigil(statement, planet, options)` returning svg + working |
| CSS class | `sigil`, `sigil--{planet}`, `sigil-path`, `sigil-node`, `sigil-start`, `sigil-end` |
| CSS custom property | `--sigil-stroke`, `--sigil-stroke-width`, `--sigil-node-fill`, `--sigil-node-opacity`, `--sigil-marker-stroke` (exact names are executor discretion per CONTEXT "Claude's Discretion"; the full surface is Phase 3 REND-05) |
| Option key | `title` (boolean, D-16) |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "statement to Saturn sigil SVG" — one path, every layer</name>
  <precondition>src/data/kamea.js exports cellForNumber and gridSize with the signed-off Saturn grid (plan 01-01 committed).</precondition>
  <read_first>
    - `.planning/research/ARCHITECTURE.md` — the pipeline diagram, Component Responsibilities table, Pattern 1 (pipe-and-filter), Pattern 2 (PathModel shape), and Anti-Patterns 1, 3, and 4
    - `.planning/phases/01-first-sigil-end-to-end/01-RESEARCH.md` — the worked example for "I WILL SUCCEED", the `cellCenter` code example, the `SigilError` taxonomy example, and the `parseArgs` plus stdin example
    - `.planning/research/PITFALLS.md` — Pitfall 5 (degenerate inputs), Pitfall 8 (styleability), Pitfall 10 (coordinate consistency), and the Integration Gotchas table (stdout discipline)
    - `src/data/kamea.js` — the resolver signatures this task calls: `cellForNumber`, `gridSize`, `planetNames`
    - `src/data/pythagorean.js` — the `toPythagoreanDigit` signature
  </read_first>
  <files>src/errors.js, src/text/normalize.js, src/path/buildPath.js, src/render/coords.js, src/render/svg.js, src/generate.js, src/index.js, src/data/kamea.js, bin/sigil-spinner.js, test/text/normalize.test.js, test/path/buildPath.test.js, test/render/coords.test.js, test/e2e/saturn-tracer.test.js</files>
  <action>
    Wire ONE statement through every layer, end to end. No second call site, no batching, no
    speculative options. Real error handling on this single path.

    `src/errors.js`: export a `SigilError` class extending Error with a stable `.code` property and a
    human-readable message, per D-15. Consumers branch on `.code`, never on message text. Define the
    code constants `E_EMPTY_SEQUENCE` and `E_UNKNOWN_PLANET`. Migrate the two guards that plan 01-01
    left as `RangeError` in `src/data/kamea.js` onto `SigilError` with `E_UNKNOWN_PLANET` for an
    unknown planet or unknown set; the out-of-range digit guard keeps throwing as well.

    `src/text/normalize.js`: export `normalize(statement)` returning an object with a `kept` array and
    a `struck` array (CONS-01). Uppercase, then walk the statement once in order. Strike A, E, I, O,
    and U as vowels. Strike any letter already kept, preserving the first occurrence. Strike any
    character that is not a letter A-Z. Push every removed character onto `struck` as a record of the
    character, its zero-based index in the original statement, and a reason tag — one of vowel,
    repeat, or non-letter — so nothing is dropped without an accounting. That accounting is this
    plan's transparency prohibition, and it is what lets the working narrate the full derivation in
    plan 01-03 per D-14. Y is treated as a consonant in this phase; CONS-04's documented Y rule is
    Phase 2 scope, so do not add contextual or phonetic detection. Keep every regex to a simple
    non-nested character class — no nested quantifiers, which is the backtracking guard in the threat
    model. This module must not import from `src/data/kamea.js` (ARCHITECTURE.md internal boundary).

    `src/render/coords.js`: the single shared coordinate transform (Pitfall 10 — duplicated
    coordinate math is how renderers silently drift apart). Export `cellSize(order)` returning the
    viewBox extent divided by the grid order, and `cellCenter(row, col, order)` returning an object
    with `x` and `y` at the cell's center, each rounded ONCE to a fixed decimal precision defined as a
    module constant. Rounding at this single point is the determinism contract: both the SVG renderer
    and the JSON working (plan 01-03) consume these same rounded numbers, so the two artifacts cannot
    disagree. Also export `formatCoord(n)` producing the shortest exact decimal string for an
    already-rounded number, used everywhere a coordinate is written into markup. Per D-07 the viewBox
    is fixed at 0 0 100 100 for every planet, so no other module computes a scale factor.

    `src/path/buildPath.js`: export `buildPath(numbers, cells, planet, order)` returning the plain
    PathModel object described in ARCHITECTURE.md Pattern 2 — planet, gridSize, a `points` array with
    one entry per number carrying its digit, row, col, and the `cellCenter` x and y, a `segments`
    array of line segments between consecutive point indices, and `start` and `end` indices. It
    returns data only and emits no markup of any kind (ARCHITECTURE.md Anti-Pattern 1) — the choice of
    output format belongs to the renderer. A one-element `numbers` array yields one point, an empty
    `segments` array, and `start` equal to `end` — it must not throw (Pitfall 5, the Phase 1 degenerate
    safety net). A zero-length array is the caller's error, not this function's.

    `src/render/svg.js`: export `renderSvg(pathModel, options)` returning a self-contained SVG string
    built by template literal (no DOM, no dependency). Root element attributes: `xmlns` on the SVG
    namespace, `viewBox` of 0 0 100 100 (D-07), and `class` of "sigil sigil--" plus the planet name
    (D-08). Emit the traced polyline as one element carrying class `sigil-path`, its geometry
    attribute built from the PathModel points via `formatCoord` — a moveto at the start point and a
    lineto per subsequent point. Set paint attributes to `var(--sigil-stroke, currentColor)` and
    `var(--sigil-stroke-width, 2)` and set `fill` to none. Never emit a presentation attribute
    carrying a bare literal color, and never emit an inline styling attribute at all (Pitfall 8) —
    every themeable value goes through a var() reference with a fallback so the sigil renders with
    zero external CSS present and themes when a site opts in. Emit no `id` attributes anywhere: D-05
    deliberately keeps this phase id-free so REND-06's collision work stays in Phase 3.

    `src/generate.js`: export `generateSigil(statement, planet, options = {})` — the only module
    allowed to import across `text/`, `data/`, and `path/` (ARCHITECTURE.md internal boundaries). Call
    `normalize`, map the kept letters through `toPythagoreanDigit`, throw `SigilError` with
    `E_EMPTY_SEQUENCE` when the resulting number sequence is empty, map each digit through
    `cellForNumber`, call `buildPath`, then `renderSvg`. Return the plain eager object with `svg` and
    `working` keys per D-13 — the working object reaches the full D-14 field set in plan 01-03; in
    this task it carries the values the tracer already computes, and it is a plain JSON-able object
    with no methods. Hold no module-level mutable state and perform no I/O, so concurrent calls are
    independent (INT-02 concurrency edge). Take exactly the inputs each stage needs and return new
    values — no mutable context object threaded through the pipeline (ARCHITECTURE.md Anti-Pattern 4).

    `src/index.js`: re-export `generateSigil` and `SigilError`. This is the entire public surface.

    `bin/sigil-spinner.js`: the thin CLI, with a `#!/usr/bin/env node` shebang and the executable bit
    set. Parse with `node:util.parseArgs` using `allowPositionals` true and a string `planet` option.
    Take the statement from the first positional. Import ONLY from `../src/index.js` — never an
    internal module. Call `generateSigil`, write the returned `svg` to `process.stdout`, and write
    nothing else there: stdout carries the artifact and only the artifact (D-10, D-12, and the
    Integration Gotchas table). On a thrown error, write the error's code and message to
    `process.stderr` and exit nonzero. Do NOT validate the planet name here — that validation lives in
    the library so programmatic callers get identical guarantees (ARCHITECTURE.md Anti-Pattern 3); the
    full CLI flag surface and the planet error path land in plan 01-03.

    Tests, all under Vitest. `test/text/normalize.test.js`: "I WILL SUCCEED" yields kept W, L, S, C, D;
    every struck character carries a reason tag of vowel, repeat, or non-letter; kept order matches
    statement order; a single-letter result and a zero-letter result both return without throwing.
    `test/render/coords.test.js`: for order 3, cell (1,1) centers at 50,50 and cell (0,0) centers at
    approximately 16.667,16.667; the same call twice returns identical values; `formatCoord` output is
    stable across repeated calls. `test/path/buildPath.test.js`: the worked example produces five
    points in order at cells (1,1), (1,0), (2,1), (1,0), (0,0) with `start` 0 and `end` 4; the
    twice-visited cell (1,0) appears as two separate points; a one-number sequence yields one point
    with `start` equal to `end` and no segments; the returned object survives a JSON round trip
    unchanged. `test/e2e/saturn-tracer.test.js`: `generateSigil` on the worked example returns an svg
    string whose root carries the fixed viewBox and the sigil and sigil--saturn classes and which
    contains the sigil-path class; calling it twice returns byte-identical strings; a statement of
    only vowels throws with code E_EMPTY_SEQUENCE; the svg string contains no inline styling attribute
    and no id attribute.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; node bin/sigil-spinner.js "I WILL SUCCEED" --planet saturn | tee /tmp/sigil-tracer.svg | grep -q 'class="sigil sigil--saturn"'</automated>
  </verify>
  <acceptance_criteria>
    - `node bin/sigil-spinner.js "I WILL SUCCEED" --planet saturn` exits 0 and writes an SVG document to stdout
    - `grep -c 'viewBox="0 0 100 100"' /tmp/sigil-tracer.svg` is at least 1
    - `grep -c 'class="sigil sigil--saturn"' /tmp/sigil-tracer.svg` is at least 1
    - `grep -c 'class="sigil-path"' /tmp/sigil-tracer.svg` is at least 1
    - `grep -c ' style=' /tmp/sigil-tracer.svg` reports 0 — the artifact carries no inline styling attribute
    - `grep -c ' id=' /tmp/sigil-tracer.svg` reports 0 — the phase is id-free per D-05
    - `grep -cE '<(svg|path|circle|line|g)[ >]' src/path/buildPath.js` reports 0 — no markup in the path builder (ARCHITECTURE.md Anti-Pattern 1)
    - `grep -c 'src/index.js' bin/sigil-spinner.js` is at least 1, and `grep -cE "src/(data|path|render|text|generate)" bin/sigil-spinner.js` reports 0 — the CLI imports only the public entry
    - `test -x bin/sigil-spinner.js` succeeds and `head -1 bin/sigil-spinner.js` is the env-node shebang
    - `test/e2e/saturn-tracer.test.js` has a passing case asserting two identical `generateSigil` calls return byte-identical svg strings
    - `test/e2e/saturn-tracer.test.js` has a passing case asserting an all-vowel statement throws with `.code` equal to `E_EMPTY_SEQUENCE`
    - `test/e2e/saturn-tracer.test.js` has a passing case asserting the returned object has a string `svg` and a JSON-serializable `working`
    - `test/text/normalize.test.js` has a passing case asserting kept letters for "I WILL SUCCEED" join to `WLSCD`
    - `test/text/normalize.test.js` has a passing case asserting every entry in `struck` carries a non-empty reason
    - `test/path/buildPath.test.js` has a passing case asserting five points at cells (1,1), (1,0), (2,1), (1,0), (0,0) with the twice-visited cell present twice
    - `test/render/coords.test.js` has a passing case asserting `cellCenter(1,1,3)` is 50,50
    - `npm test` and `npm run typecheck` each exit 0
  </acceptance_criteria>
  <done>A single CLI invocation turns "I WILL SUCCEED" plus saturn into a self-contained, class-hooked, viewBox-based SVG on stdout, and the same call through the library returns svg plus working. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sigil anatomy — start circle, end bar, visited-cell nodes, opt-in title</name>
  <precondition>bin/sigil-spinner.js prints an SVG for the worked example with --planet saturn (the tracer slice is committed).</precondition>
  <read_first>
    - `src/render/svg.js` — the renderer as the tracer left it; this task extends its layer composition
    - `src/render/coords.js` — `cellSize`, `cellCenter`, `formatCoord`; marker geometry derives from these, never from new inline arithmetic
    - `src/path/buildPath.js` — the PathModel `points`, `start`, and `end` fields the markers key off
    - `.planning/phases/01-first-sigil-end-to-end/01-CONTEXT.md` — decisions D-05, D-06, D-07, D-08, D-16
    - `.planning/research/PITFALLS.md` — Pitfall 8 (styleability) and Pitfall 10 (shared transform)
  </read_first>
  <reversibility rating="costly">Per D-08 the class names are the public CSS contract embedding sites write selectors against; renaming them later breaks every site that has themed a sigil.</reversibility>
  <files>src/render/svg.js, src/render/escapeXml.js, src/generate.js, test/render/svg.test.js</files>
  <behavior>
    - Rendering the worked example emits exactly one element with class `sigil-start` at the first point's coordinates
    - Rendering the worked example emits exactly one element with class `sigil-end` at the last point's coordinates
    - Rendering the worked example emits exactly five elements with class `sigil-node`, one per visited cell — including two at the twice-visited Saturn cell (1,0), which are not deduplicated
    - A one-point sigil emits one `sigil-node`, one `sigil-start`, and one `sigil-end`, all at the same coordinates, and no path element with an empty geometry attribute
    - Layer output order is fixed: path, then nodes, then start marker, then end marker — two runs produce byte-identical strings
    - Rendering with the title option absent leaves the statement text entirely absent from the SVG
    - Rendering with the title option enabled embeds the statement XML-escaped, so a statement containing angle brackets and ampersands yields well-formed markup
    - Every paint attribute in the output is a var() reference with a fallback or the keyword none; no bare color literal and no inline styling attribute appears
  </behavior>
  <action>
    Extend `src/render/svg.js` into per-layer composition — each layer is a function returning a markup
    fragment and `renderSvg` assembles them in one fixed order (ARCHITECTURE.md structure rationale;
    Phase 3 adds grid and glyph layers to the same seam without touching existing layers).

    Node layer (D-06): emit a circle carrying class `sigil-node` at every visited cell — one per
    PathModel point, so a cell visited twice gets two nodes and revisits stay visible in the markup.
    Radius and paint come from var() references with fallbacks; per D-06 the default renders nodes
    invisible, revealable by a single CSS rule from an embedding site, so express that default as a
    custom-property fallback on a paint property (opacity or fill) rather than an attribute a
    stylesheet cannot reach.

    Start marker (D-05): a small circle carrying class `sigil-start` at the first point. End marker
    (D-05): a short bar carrying class `sigil-end` at the last point, drawn perpendicular to the
    incoming segment — for a one-point sigil with no incoming segment, use a fixed orientation so
    output stays deterministic. Both are plain shape elements with semantic classes, NOT SVG marker
    definitions — that is what keeps this phase free of ids and defers REND-06 to Phase 3.

    Derive all marker geometry — node radius, start radius, end-bar length — from `cellSize(order)`
    via fixed fractions, never from a hardcoded unit constant and never by recomputing cell size
    locally. That honors D-07's requirement that all seven planets render at consistent scale in the
    fixed viewBox, and it keeps an edge-cell marker from crossing the 0 or 100 boundary at any grid
    order — D-07's internal-inset intent expressed as derived geometry rather than a config surface.

    Write `src/render/escapeXml.js` exporting `escapeXml(str)` mapping the five XML entities for
    ampersand, less-than, greater-than, double quote, and apostrophe. Use it in exactly one place: the
    optional title element.

    Title handling (D-16): the intention statement is absent from the SVG unless the caller passes
    `title: true` in options. When absent, emit no title element, no description element, and no data
    attribute carrying statement text. When present, emit a title element containing the XML-escaped
    statement. This is a practitioner-posture decision, not a technical one — classic practice
    releases the intention once it is sigilized — so keep that framing in the JSDoc. Thread the option
    from `generateSigil` through to `renderSvg`.

    Add JSDoc typedefs for the PathModel and the render options so `npm run typecheck` covers the
    renderer's contract.

    Write `test/render/svg.test.js` covering every bullet in this task's behavior block, plus a
    snapshot of the full worked-example SVG via Vitest's `toMatchSnapshot`, plus a case asserting that
    a statement containing angle brackets and ampersands round-trips as well-formed escaped markup
    under the title option, plus a case asserting the default render contains no occurrence of the
    statement text.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; node bin/sigil-spinner.js "I WILL SUCCEED" --planet saturn > /tmp/sigil-anatomy.svg &amp;&amp; test "$(grep -o 'class="sigil-node"' /tmp/sigil-anatomy.svg | wc -l | tr -d ' ')" = "5"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o 'class="sigil-start"' /tmp/sigil-anatomy.svg | wc -l` reports exactly 1
    - `grep -o 'class="sigil-end"' /tmp/sigil-anatomy.svg | wc -l` reports exactly 1
    - `grep -o 'class="sigil-node"' /tmp/sigil-anatomy.svg | wc -l` reports exactly 5 — one per visited cell, revisits included
    - `grep -c ' style=' /tmp/sigil-anatomy.svg` reports 0
    - `grep -cE '(stroke|fill)="(?!var\(--sigil-|none)' /tmp/sigil-anatomy.svg` reports 0, or an equivalent assertion that every paint attribute value begins with `var(--sigil-` or is exactly `none`
    - `grep -ci 'succeed' /tmp/sigil-anatomy.svg` reports 0 — the statement does not leak into the default artifact (D-16)
    - `test/render/svg.test.js` has a passing case asserting the title option produces a title element whose statement text is XML-escaped for a statement containing `<`, `>`, and `&`
    - `test/render/svg.test.js` has a passing case asserting the default render contains no occurrence of the statement text
    - `test/render/svg.test.js` has a passing case asserting a single-kept-letter statement renders one node, one start, one end, and no path element with an empty geometry attribute
    - `test/render/svg.test.js` has a passing Vitest snapshot for the worked-example SVG that matches on a second run
    - `grep -c 'cellSize' src/render/svg.js` is at least 1 — the renderer consumes the shared transform rather than recomputing cell size (Pitfall 10)
    - `npm test`, `npm run typecheck`, and `npm run lint` each exit 0
  </acceptance_criteria>
  <done>The Saturn sigil renders with its traditional anatomy — start circle, end bar, a node at every visited cell — under the D-08 class taxonomy, fully themeable, with the statement withheld from the artifact unless opted in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv / stdin → library | Untrusted, arbitrary-length user text crosses into the pipeline here |
| library → generated SVG artifact | The artifact is embedded verbatim into third-party HTML pages |
| generated SVG → embedding site CSS | The site's stylesheet is the theming surface; markup must not out-specify it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `src/render/svg.js` optional title element | medium | mitigate | `escapeXml()` five-entity transform applied to statement text; title emitted only under the explicit `title` option (D-16); test asserts well-formedness for a statement containing angle brackets and ampersands |
| T-01-05 | Information Disclosure | Generated SVG embedded on a public page | medium | mitigate | Statement omitted from the artifact by default (D-16); no title, description, or data attribute carries it unless the caller opts in; acceptance criterion asserts absence |
| T-01-03 | Denial of Service | `src/text/normalize.js` regexes | low | mitigate | Simple non-nested character classes only; no nested quantifiers, so no catastrophic backtracking on a crafted statement |
| T-01-08 | Tampering | stdout artifact stream consumed by a build pipeline | high | mitigate | `bin/sigil-spinner.js` writes only the artifact to stdout; every diagnostic goes to stderr with a nonzero exit (Integration Gotchas) |
</threat_model>

## Flagged Assumptions (probe items carried unresolved)

- **PATH-03 / unclassified** — "PathModel is renderer-agnostic, consumed identically by SVG and JSON."
  The probe returned this row unclassified. It is approximated here by structural checks (no markup
  tokens in `src/path/buildPath.js`, PathModel survives a JSON round trip), but "consumed identically"
  cannot be fully proven until the JSON renderer exists in plan 01-03 — the real proof is the
  cross-artifact agreement assertion in that plan.

Prohibitions carried unresolved (descriptor-less, disposes flagged-unverified):

- "MUST NOT embed the practitioner's intention statement in the SVG artifact by default." Partially
  mechanically checkable (the absence assertion above), but holding across every future option
  combination is judgment-tier.
- "MUST NOT silently discard characters from the statement." The struck-with-reason accounting is
  asserted for the worked example; that every conceivable input is fully accounted for is
  judgment-tier.

Canon-referral breadcrumb (surfaced, deliberately not minted): XML/markup injection via embedded
statement text and downstream XSS on a consumer rendering the statement are canon — covered by
`/gsd-secure-phase` and the `T-01-01` and `T-01-05` rows above; not minted as prohibitions.

<verification>
- `node bin/sigil-spinner.js "I WILL SUCCEED" --planet saturn` writes a valid, self-contained SVG to stdout and nothing else
- `npm test` exits 0 across normalize, coords, buildPath, svg, and e2e suites
- `npm run typecheck` and `npm run lint` exit 0
- Two identical invocations produce byte-identical SVG
- The generated SVG contains no inline styling attribute, no `id` attribute, and no bare color literal
- No markup tokens appear in `src/path/buildPath.js`; the renderer consumes `cellSize` rather than recomputing it
</verification>

<success_criteria>
- One CLI invocation turns an intention statement plus saturn into a correct, traditionally-traced sigil as self-contained inline SVG (REND-01, PATH-01, INT-02)
- `generateSigil()` is importable and returns svg plus working from one call (INT-01, D-13)
- The PathModel is a plain JSON-serializable object with zero rendering concerns (PATH-03)
- Vowel and repeat striking keeps the first occurrence in statement order, with every struck character accounted for (CONS-01)
- The sigil carries start marker, end marker, and a node at every visited cell under the D-08 class taxonomy (D-05, D-06, D-08)
</success_criteria>

<output>
Create `.planning/phases/01-first-sigil-end-to-end/01-02-SUMMARY.md` when done
</output>
