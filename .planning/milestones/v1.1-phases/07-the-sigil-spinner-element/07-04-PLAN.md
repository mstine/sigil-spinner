---
phase: 07-the-sigil-spinner-element
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified: []
autonomous: false
requirements: [WRAP-01, WRAP-02, WRAP-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "Under the documented sizing recipe (`display: inline-block; width: 240px; aspect-ratio: 1 / 1`), a populated `<sigil-spinner>` occupies a visible square area and its sigil is neither clipped nor stretched — the fixed `viewBox` scaling to the host rather than overflowing it."
      verification: backstop
    - statement: "The three theming demonstration sigils — default/unstyled, custom-property teal, class-selector orange-dashed — are simultaneously legible and visibly distinguishable from one another on the rendered page. This is the literal claim of D-95 and ROADMAP success criterion 2, and the specific thing only a human look confirms."
      verification: backstop
    - statement: "The gallery's card grid (`240px` cards, `32px` gap) wraps and reflows at a narrow viewport without any card's sigil being clipped or its caption overlapping a neighbour — a plain CSS layout claim on a repo-only demo page, verified by looking."
      verification: backstop
    - "The full suite (`npm test`), the pack-and-install smoke test (`npm run test:pack`), `npm run typecheck`, and `npm run lint` are all green simultaneously after both wave-2 plans have landed."
    - "`package.json` `version` is `1.0.0`, `dependencies` is absent or empty, no `dist/` or build output exists in the tree, and no publish or release-workflow dispatch happened in this phase (D-98, ROADMAP success criterion 5)."
    - "`bin/` is untouched by this phase, so the deferred `D-12` citation-collision reopen condition (which names `bin/sigil-spinner.js:20` among its triggers) stays unmet — confirmed by inspection rather than assumed."
  artifacts:
    - .planning/phases/07-the-sigil-spinner-element/07-04-SUMMARY.md
  key_links:
    - "`examples/element.html` served over HTTP → the human look that ROADMAP success criterion 1 requires and no green test can substitute for"
    - "both wave-2 plans → one simultaneous green gate, which neither plan can prove alone"
---

<objective>
Close the phase on the two things neither wave-2 plan can establish by itself: a human actually looking at rendered output, and every gate green at the same time across both parallel plans.

The verification lesson this phase carries is the reason this plan exists. Both real v1.0 defects — G-02-1's detached loop arcs and G-03-1's inert `font-size` `var()` fallback — passed a fully green 1,453-test suite and were caught by a human looking at rendered output. ROADMAP success criterion 1 is written as "confirmed by a human looking at the rendered page in a browser, not only by a green test", and `examples/element.html` (D-95) is the instrument that check is performed with — planned as a deliverable with that job, not as documentation that happens to be viewable. The three `backstop` truths above are exactly the claims that abstain to `human_needed` rather than passing silently, and this is where they get answered.

The second half is the D-98 boundary, confirmed rather than assumed: nothing published, `version` still `1.0.0`, zero dependencies, no build output. `npm install @falkensmage/sigil-spinner` still resolves to `1.0.0` and gets no element at phase close — that is the deliberate scope line, and publishing `1.1.0` through the proven release workflow is a milestone-close action.

Purpose: make the phase's success criteria answered, not inferred.
Output: `07-04-SUMMARY.md` recording the human verdict and the simultaneous-green evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-the-sigil-spinner-element/07-UI-SPEC.md
@.planning/phases/07-the-sigil-spinner-element/07-02-SUMMARY.md
@.planning/phases/07-the-sigil-spinner-element/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simultaneous green gate and the D-98 boundary, confirmed by inspection</name>
  <files>.planning/phases/07-the-sigil-spinner-element/07-04-SUMMARY.md (no source files modified — this task runs gates and records evidence)</files>
  <precondition>Both wave-2 plans have landed — `examples/element.html`, the expanded `test/browser/element.test.js`, the `package.json` `exports` edit, the extended `test/pack-install.test.js`, the README element section, and `test/element-docs.test.js` all exist on the working tree.</precondition>
  <read_first>
    - `.planning/phases/07-the-sigil-spinner-element/07-02-SUMMARY.md` and `07-03-SUMMARY.md` — what each wave-2 plan actually landed, including any deviation recorded there
    - `package.json` — current `version`, `dependencies`, `files`, `exports`, `scripts`
    - `.planning/ROADMAP.md` § Phase 7 — the five success criteria and the Milestone-Wide Constraints block
    - `.planning/STATE.md` § Deferred Items — the `D-12` reopen condition and its named trigger files
  </read_first>
  <action>
Run every gate in one pass on the merged tree and record the exact outputs in the summary: `npm test`, `npm run test:pack`, `npm run typecheck`, `npm run lint`. Both wave-2 plans were green independently; this is the first point at which they are green together, and a cross-plan interaction (a `package.json` `exports` edit that a test picks up differently, a README change that trips a guard) can only surface here.

Then confirm the D-98 boundary and ROADMAP success criterion 5 by inspection rather than assumption:

- `package.json` `version` is still exactly `1.0.0`.
- `package.json` has no `dependencies` key, or an empty one.
- `package.json` `files` is still exactly `src`, `bin`, `README.md`.
- No build output exists in the tree — no `dist/`, no bundled artifact, and no `scripts` entry that produces one. "The source is what runs" still holds literally.
- No publish happened: no `npm publish` invocation in this phase's commits and no release-workflow dispatch. `.github/workflows/release.yml` is unmodified.
- `npm pack --dry-run --json` lists `src/element/sigil-spinner-element.js` and lists no path beginning with `examples/`.

Finally, confirm the deferred `D-12` citation-collision reopen condition stayed unmet. Its trigger list names `src/errors.js:20`, `src/generate.js:163`, `src/generate.js:238`, and `bin/sigil-spinner.js:20` — the condition fires only if one of those is edited for another reason. This phase was not expected to touch `bin/` at all, so confirm by inspecting the phase's diff rather than assuming, and record the finding either way. If any of those files WAS edited, say so plainly in the summary and flag the deferred item as reopened rather than letting it pass unremarked.

Record every result in `07-04-SUMMARY.md` with the actual command output, not a claim that it passed.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run test:pack &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0; `npm run test:pack` exits 0; `npm run typecheck` exits 0; `npm run lint` exits 0 — all four on the same tree, with output recorded in the summary.
    - `node -e "const p=require('./package.json'); if(p.version!=='1.0.0')process.exit(1); if(p.dependencies&&Object.keys(p.dependencies).length)process.exit(1); if(JSON.stringify(p.files)!==JSON.stringify(['src','bin','README.md']))process.exit(1); console.log('ok')"` prints `ok`.
    - `ls dist 2>/dev/null | wc -l` returns `0` and `node -e "const s=require('./package.json').scripts; if(s.build||s.prepare||s.prepublishOnly)process.exit(1); console.log('ok')"` prints `ok`.
    - `git status --porcelain .github/workflows/release.yml` returns empty, and `git log --oneline` for this phase's commits contains no publish or dispatch commit.
    - `npm pack --dry-run --json` output contains `src/element/sigil-spinner-element.js` and contains no path beginning with `examples/`.
    - `git diff --name-only <phase-base-commit>..HEAD` is inspected and the result recorded; whether or not it includes `bin/sigil-spinner.js`, `src/errors.js`, `src/generate.js`, the finding and its consequence for the deferred `D-12` item are stated explicitly in the summary.
    - The summary records the actual `npm test` pass count, so the phase's test-count delta against v1.0's 1,453 is visible rather than implied.
  </acceptance_criteria>
  <done>All four gates are green on one tree, the D-98 boundary is confirmed by inspection with evidence recorded, and the deferred `D-12` reopen condition's status is stated rather than assumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: the human look at examples/element.html (ROADMAP success criterion 1, backstops B1/B2/B3)</name>
  <what-built>A `<sigil-spinner>` custom element that renders a themeable sigil in a plain HTML page loaded as ESM — no build step, no runtime dependencies, no shadow root — plus `examples/element.html`, a repo-only demo page carrying several sigils across different planets, three theming demonstrations side by side, a revealed grid layer, a glyph example, a live planet control that mutates an attribute after load, and one deliberately-erroring instance showing the documented `data-sigil-error` outline hook.

The automated half of ROADMAP success criterion 1 is already green: the browser test asserts a `.sigil-path` is visible with non-zero `getBBox()` geometry, that both theming mechanisms reach the element's computed styles, and that the element's `innerHTML` is byte-identical to `generateSigil`'s output. What no test can answer is whether the result actually looks right on screen — which is exactly how both real v1.0 defects reached a shipped release through a fully green suite.</what-built>
  <how-to-verify>
1. From the repository root, start a static server: `python3 -m http.server 8000`
2. Open `http://localhost:8000/examples/element.html` in a browser.
3. Confirm each of the following by looking — say which ones fail rather than approving the set:

   **Success criterion 1 — a visibly correct sigil.** Every gallery card shows a sigil: a continuous traced path across the kamea, with its start and end markers, drawn inside a visible square area. No blank cards, no invisible-but-present SVG, no path collapsed to a dot or a single line.

   **Backstop B1 — the sizing recipe holds.** Each populated `<sigil-spinner>` occupies a visible square area of roughly 240 by 240 pixels, and its sigil is neither clipped at the card edges nor stretched out of square.

   **Backstop B2 / success criterion 2 — both theming mechanisms, visibly distinguishable.** The three demonstration sigils are simultaneously legible and obviously different from each other: the unstyled one in the page's default text color, the custom-property one in teal, and the class-selector one in orange with a dashed stroke. You should be able to tell the two mechanisms apart without reading the page source.

   **Backstop B3 — the grid reflows.** Narrow the browser window until the card grid wraps. No card's sigil is clipped and no caption overlaps a neighbour.

   **Success criterion 3 — attribute mutation re-renders.** Change the "Change planet" select. The target sigil should redraw immediately for the new planet, with a visibly different grid order and path.

   **Success criterion 4 — several elements, independent.** Mutating the live control changes only its own sigil; every other sigil on the page is untouched.

   **The error contract.** The deliberately-erroring instance shows the red dashed outline and renders no sigil inside it. Open the browser console and confirm a `<sigil-spinner> failed to render:` message with a real error object carrying a `code`.

   **The grid and glyph instances.** One card shows the kamea grid lines and numbers revealed underneath the path; one card shows the planetary glyph.
  </how-to-verify>
  <resume-signal>Type "approved" if every item above is visibly correct, or describe exactly which items fail and what you see instead</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev static server → localhost browser | the documented `python3 -m http.server` command serves the repository tree to the local browser for the human check; a dev instruction, never a dependency and never a shipped artifact |
| repository tree → published npm registry | deliberately NOT crossed in this phase (D-98) — the boundary is confirmed closed rather than left unexamined |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Tampering | the npm registry (a publish this phase must not perform) | high | mitigate | D-98 — no publish task exists in any plan of this phase, `version` stays `1.0.0`, and Task 1 confirms by inspection that `.github/workflows/release.yml` is unmodified and no dispatch occurred. A publish is irreversible (72-hour conditional unpublish window, versions never reusable), so the mitigation is that the act is absent and its absence is checked, not merely intended. |
| T-07-11 | Information Disclosure | `python3 -m http.server` run from the repository root | low | accept | The documented human-check command serves the whole working tree, including `.planning/`, on localhost for the duration of the look. Accepted: it binds to the local machine, is started and stopped by hand for a one-off visual check, and is a documented dev instruction that ships nowhere. The alternative — a bespoke hardened server for a manual look — is machinery serving nothing. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed this phase — `.planning/phases/07-the-sigil-spinner-element/07-RESEARCH.md` § Package Legitimacy Audit records N/A. Task 1 re-confirms `dependencies` is absent or empty on the merged tree, so a dependency introduced by either wave-2 plan would fail here. |
</threat_model>

<verification>
- `npm test`, `npm run test:pack`, `npm run typecheck`, `npm run lint` — all four green simultaneously on the merged tree, with output recorded.
- `package.json` inspection — `version` `1.0.0`, no `dependencies`, `files` unchanged, no build script, no `dist/`.
- `npm pack --dry-run --json` — element module present, no `examples/` path.
- Human look at `http://localhost:8000/examples/element.html` — ROADMAP success criteria 1, 2, 3, 4 and backstops B1, B2, B3 confirmed by eye.
- Deferred `D-12` reopen condition status stated explicitly in the summary.
</verification>

<success_criteria>
- ROADMAP success criterion 1 answered by a human looking at rendered output, not by a green test.
- ROADMAP success criteria 2, 3, 4 confirmed visually on the demo page, complementing the browser assertions that prove them mechanically.
- ROADMAP success criterion 5 confirmed by inspection: zero runtime dependencies, no build output, the source is still what runs.
- The three `backstop` truths (B1, B2, B3) have a recorded human verdict rather than a silent pass.
- D-98 held: nothing published, `version` still `1.0.0`.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol below is NEW in Phase 7 and did not exist before it. Do not flag these as drift against prior phases.

| Artifact | Kind | Created in |
|---|---|---|
| `src/element/sigil-spinner-element.js` | new file (first `src/element/` module) | 07-01 |
| `SigilSpinnerElement` | exported class (first `HTMLElement` subclass in the tree) | 07-01 |
| `SigilSpinnerElement.observedAttributes` | static getter | 07-01 |
| `TAG_NAME` | module-level constant, value `sigil-spinner` | 07-01 |
| `<sigil-spinner>` | custom element tag name | 07-01 |
| `show-title` (or the checkpoint-selected equivalent) | new HTML attribute | 07-01 |
| `data-sigil-error` | new host attribute reflecting a `SigilError` code | 07-01 |
| `test/browser/element.test.js` | new test file | 07-01 (expanded 07-02) |
| `startStaticServer` | new test helper (`node:http` static file server) | 07-01 |
| `examples/` directory and `examples/element.html` | new repo-only page (not in `files`) | 07-02 |
| `live-planet` | new element id on the example page's `<select>` control | 07-02 |
| `exports["./element"]`, `exports["./package.json"]` | new `package.json` subpaths | 07-03 |
| `resolveOnly` field on `ENTRY_POINTS` rows | new test-data field in `test/pack-install.test.js` | 07-03 |
| `test/element-docs.test.js` | new README/`observedAttributes` drift guard | 07-03 |
| README § element section | new README section | 07-03 |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-the-sigil-spinner-element/07-04-SUMMARY.md` when done
</output>
