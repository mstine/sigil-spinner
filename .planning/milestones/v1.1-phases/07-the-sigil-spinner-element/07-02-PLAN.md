---
phase: 07-the-sigil-spinner-element
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - examples/element.html
  - test/browser/element.test.js
autonomous: true
requirements: [WRAP-01, WRAP-02, WRAP-03]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An attribute that is PRESENT BUT EMPTY is distinguishable in the DOM from an attribute that is ABSENT. `<sigil-spinner statement=\"\" planet=\"saturn\">` treats the empty statement as present, passes it to generateSigil, and surfaces the library's own thrown code via data-sigil-error; an element missing the statement attribute entirely stays inert with zero element children and NO data-sigil-error (D-91). The element must not coerce an empty string to absent, and must not coerce absent to an empty string."
    - "The element passes getAttribute('statement') to generateSigil VERBATIM - no trim, no case folding, no Unicode normalization of its own. All letter-handling (vowel/repeat striking, the 84-entry stroke/bar fold table, non-ASCII rules) stays owned by the library per INT-04 and D-23. Checkable: a statement containing non-ASCII characters rendered through the element produces innerHTML byte-identical to calling generateSigil directly with the same string."
    - "Two forms, both required. (a) Setting an observed attribute to the value it already holds re-renders and yields byte-identical innerHTML. (b) Loading the element module twice on one page does not throw and leaves exactly one registered definition - the guarded customElements.define of D-90, which exists because the platform ships no unregister or redefine API."
    - "A page that loads the element module but contains ZERO <sigil-spinner> tags does not throw, and still registers the definition - so an element created and appended later still upgrades and renders correctly. The zero-instance case is a real state for a dynamically-built page, not only the many-instance case."
    - "All three upgrade orderings produce identical final innerHTML for identical attributes: (a) element in parsed HTML with the module already defined; (b) element in parsed HTML upgraded later by a deferred module; (c) document.createElement + setAttribute before appendChild. The assertion is on FINAL CONTENT, never on render-call count - RESEARCH.md establishes that in path (b) isConnected is already true for every attributeChangedCallback fired during upgrade, so the D-88 isConnected gate does not suppress those calls and a call-count assertion would be both wrong and brittle."
    - "A `<sigil-spinner>` carrying only one of `statement`/`planet` (or neither) has zero element children, emits no thrown error, and carries no `data-sigil-error` attribute — the inert state of D-91. Setting the missing attribute renders it, from the same instance, with no page reload."
    - "Setting `planet` to an unknown value clears the element to zero element children and reflects `data-sigil-error=\"E_UNKNOWN_PLANET\"` on the host; setting `planet` back to a valid one removes the attribute and restores an `<svg>` child (D-92's full round trip, not just the error leg)."
    - "A connected `<sigil-spinner>` with valid `statement` and `planet` has `innerHTML` byte-identical to `generateSigil(statement, planet, options).svg` for the same inputs — the pure-pass-through claim of D-85/D-86 stated as an equality rather than as \"an SVG is present\"."
    - "With a `statement` far longer than any test fixture (≥500 characters), the rendered `<svg>` still carries `viewBox=\"0 0 100 100\"` and its `.sigil-path` still has a non-degenerate bounding box — sigil geometry is bounded by kamea order, not statement length (locked, Phase 1–3), so more kept letters densify the path inside the same fixed viewBox rather than overflowing it."
    - "Navigating to the served `examples/element.html` yields at least one rendered `.sigil-path` — which fails loudly if the module never loaded. This is the assertion that catches the module-load failure class RESEARCH.md identified: the static server MUST send `Content-Type: text/javascript` or Chromium's strict MIME enforcement silently rejects the module and every `<sigil-spinner>` on the page stays inert with no console error of its own."
    - "Several co-rendered `<sigil-spinner>` elements on one page each contain exactly one `<svg>`, and the count of `id` attributes anywhere within their rendered subtrees is zero (D-93 — id-free by construction, not by generated uniqueness). Re-rendering one instance leaves the others' content unchanged."
  artifacts:
    - examples/element.html
    - test/browser/element.test.js
  key_links:
    - "`examples/element.html` → served over the test's own `node:http` server → the same `Content-Type: text/javascript` path the browser test depends on"
    - "`test/browser/element.test.js` → `generateSigil` imported directly, used as the byte-identity oracle against the element's rendered `innerHTML`"
    - "page-level `.sigil-path { … }` rule and a `--sigil-*` override on an ancestor → the element's light-DOM children (the empirical test of D-82)"
---

<objective>
Turn the tracer's one proven assertion into the phase's real verification surface, and build the instrument the human check is performed with.

Two deliverables. `examples/element.html` (D-95) is a repo-only page carrying several sigils, both theming mechanisms side by side and visibly distinguishable, the grid layer revealed, a glyph example, and a live control that mutates an attribute after load — it is the artifact ROADMAP success criterion 1's human look is performed against, not optional documentation. `test/browser/element.test.js` grows from the tracer's single assertion to D-94's full nine-assertion list plus the edge and state-coverage predicates in `must_haves.truths` above.

The verification lesson this phase carries is load-bearing: both real v1.0 defects passed a fully green 1,453-test suite and were caught by a human looking at rendered output. A test that asserts only "registers and reflects attributes" would pass while rendering nothing visible (D-88, D-89, D-90, D-91, D-92, D-93 each get an assertion that proves behavior, not wiring; D-81's presence-semantics footgun gets pinned rather than merely stated).

Purpose: make the element's contract enforced rather than described.
Output: `examples/element.html` and a fully expanded `test/browser/element.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-the-sigil-spinner-element/07-UI-SPEC.md
@.planning/phases/07-the-sigil-spinner-element/07-RESEARCH.md
@.planning/phases/07-the-sigil-spinner-element/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build examples/element.html — the instrument the human check uses</name>
  <files>examples/element.html</files>
  <read_first>
    - `.planning/phases/07-the-sigil-spinner-element/07-UI-SPEC.md` in full — the Host Element Sizing Contract, the Spacing Scale, the Typography table (page chrome only), both Color surfaces including the exact teal/orange-dashed demonstration palette and the `data-sigil-error` CSS hook recipe, and the Copywriting Contract's live-control label and `<select>` option set
    - `.planning/phases/07-the-sigil-spinner-element/07-CONTEXT.md` § D-95 — what the page owes
    - `src/element/sigil-spinner-element.js` — the attribute names as actually implemented, so the page's markup matches
    - `README.md` § CSS Custom Properties — the 15 `--sigil-*` names and the seven semantic class names, so the demo overrides use real ones
  </read_first>
  <action>
Create `examples/element.html` — a hand-authored, repo-only HTML page with no build step, no web font, no `@font-face`, no network request of any kind, and no runtime dependency. It is NOT added to `package.json`'s `files` array and must never ship in the tarball; plan 07-03 asserts that mechanically.

Load the element with a single `<script type="module" src="../src/element/sigil-spinner-element.js"></script>`. The page is opened over a local HTTP server (`python3 -m http.server` from the repo root, or the browser test's own server) — document that as a one-line dev instruction in a comment at the top of the file, never as a dependency.

**Sizing recipe, verbatim in spirit and load-bearing.** Include a `sigil-spinner { display: inline-block; }` rule and, for the gallery cards, `width: 240px; aspect-ratio: 1 / 1;`. An unstyled custom element computes to `display: inline` and the inner `<svg>` carries `viewBox="0 0 100 100"` with no `width`/`height` attributes — that pair produces a correct DOM and an empty screen, which is precisely the "technically correct, visibly nothing" defect class this phase exists to avoid. `aspect-ratio: 1 / 1` is the half that keeps the square viewBox from stretching.

**Page chrome.** Font `system-ui, sans-serif`. Four sizes and two weights only: Body 16px/400/1.5, Label 14px/600/1.2, Heading 20px/600/1.2, Display 28px/600/1.2 — hierarchy carried by size, not by adding weights. Spacing tokens, all multiples of 4: 4px caption-to-mechanism-label, 8px sigil-to-caption, 16px card padding, 24px section padding, 32px grid gap between gallery cards, 48px between major sections, 64px page top/bottom margin. Colors: `#ffffff` page background, `#f4f4f5` card and section backgrounds, `#2563eb` used for exactly three things and nothing else — the live control's `<label>` text, its focus outline, and a small "Live" badge beside it.

**The three theming demonstration sigils, side by side and visibly distinguishable.** One default and completely unstyled, so it renders at `currentColor` and honestly shows the library's zero-opinion default — never repaint it with the page's accent. One themed through a custom property, `--sigil-stroke: #0d9488` set on the host or an ancestor, rendering teal. One themed through a class selector, `.sigil-path { stroke: #ea580c; stroke-dasharray: 4 2; }`, rendering orange and dashed. Different hue AND a stroke-pattern difference is deliberate: a human glancing at the page must tell the two mechanisms apart without reading the source. Label each with its mechanism.

**Coverage the page also owes.** Several sigils across different planets; one instance with the `grid` layer visibly revealed via the `--sigil-grid-opacity` custom property; one instance using the `glyph` attribute. The page chrome's `system-ui` font must never be applied to `.sigil-grid-number` or `.sigil-glyph` — those default to `sans-serif` through `--sigil-grid-number-font` / `--sigil-glyph-font` and are the page author's override surface, not the demo's global stack. Conflating them would misrepresent the library's real default to anyone reading the example as documentation.

**The live control.** A `<label for="live-planet">Change planet</label>` beside a `<select id="live-planet">` whose seven options are the classical planet names — Saturn, Jupiter, Mars, Sun, Venus, Mercury, Moon. Its `change` handler sets the target `<sigil-spinner>`'s `planet` attribute via `setAttribute`, demonstrating WRAP-03's re-render contract live. Put a small "Live" badge next to it in the accent color.

**The error-state instance.** Include one intentionally-erroring `<sigil-spinner>` — an invalid `planet` value — styled with the documented hook recipe `sigil-spinner[data-sigil-error] { outline: 2px dashed #dc2626; outline-offset: 4px; }`, so the human check confirms the D-92 error contract visually and not only the happy path. Label it as deliberate.

**The gallery layout.** A wrapping card grid, 240px cards with a 32px gap, that reflows at a narrow viewport without any card's sigil being clipped or any caption overlapping a neighbour.

The page authors no markup into any element's `innerHTML` by string concatenation — every `<sigil-spinner>` is written literally in the HTML source, and the live control only calls `setAttribute`.
  </action>
  <verify>
    <automated>node -e "const s=require('node:fs').readFileSync('examples/element.html','utf-8'); const need=['sigil-spinner','display: inline-block','aspect-ratio: 1 / 1','--sigil-stroke','.sigil-path','stroke-dasharray','live-planet','data-sigil-error','type=\"module\"']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('missing:',miss.join(', ')); process.exit(1);} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/element.html` exists and contains a single `<script type="module" ...>` tag whose `src` resolves to `src/element/sigil-spinner-element.js` relative to the page.
    - The stylesheet contains a `sigil-spinner` rule declaring `display: inline-block`, and a gallery-card rule declaring both `width: 240px` and `aspect-ratio: 1 / 1`.
    - Exactly three theming-demonstration instances exist and are labelled: one with no override, one whose theming comes from `--sigil-stroke: #0d9488`, and one whose theming comes from a `.sigil-path` rule declaring `stroke: #ea580c` and `stroke-dasharray: 4 2`.
    - A `<label for="live-planet">` with the text `Change planet` and a `<select id="live-planet">` carrying exactly seven `<option>` elements exist, and the page's script sets a `<sigil-spinner>`'s `planet` attribute through `setAttribute` on the select's `change` event.
    - At least one instance carries the `glyph` attribute, and at least one reveals the grid layer via a `--sigil-grid-opacity` override.
    - A `sigil-spinner[data-sigil-error]` CSS rule exists declaring `outline: 2px dashed #dc2626`, and at least one instance carries a deliberately invalid `planet` value.
    - `grep -cE "@font-face|fonts\.googleapis|https?://" examples/element.html` returns `0` — no web font, no external network reference of any kind.
    - `grep -c "innerHTML" examples/element.html` returns `0` — the page never builds markup by string assignment.
    - `package.json`'s `files` array is byte-unchanged (still exactly `src`, `bin`, `README.md`) — `git diff --name-only` for this task lists only `examples/element.html`.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>`examples/element.html` renders a labelled gallery in which the unstyled, custom-property-themed, and class-selector-themed sigils are simultaneously visible and distinguishable, the live planet control mutates an attribute after load, and one deliberate error instance shows the documented outline hook.</done>
</task>

<task type="auto">
  <name>Task 2: Browser assertions — rendering, theming reach, and pass-through fidelity</name>
  <files>test/browser/element.test.js</files>
  <precondition>Playwright's Chromium is installed — `npx vitest run test/browser/element.test.js` (the tracer from 07-01) currently passes. If it fails with a launch error, halt: run `npx playwright install chromium` first.</precondition>
  <read_first>
    - `test/browser/element.test.js` — the tracer's current state: the harness, the `startStaticServer` helper, and the module-loading approach the new assertions must reuse rather than re-invent
    - `test/browser/theming-resolution.test.js` — the `computed()` helper shape (`page.evaluate` + `getComputedStyle`) the theming assertions follow
    - `.planning/phases/07-the-sigil-spinner-element/07-RESEARCH.md` § Architecture Patterns 5 (visibility mechanisms) and § Code Examples 2 (the class-selector plus custom-property assertion shape)
    - `src/element/sigil-spinner-element.js` — the implemented attribute-to-option mapping, so the byte-identity oracle calls `generateSigil` with exactly the options the element assembles
    - `examples/element.html` — the page the served-page assertion navigates to
    - `README.md` § CSS Custom Properties — real `--sigil-*` names and real class names for the overrides
  </read_first>
  <action>
Extend `test/browser/element.test.js` with the rendering, theming, and pass-through half of D-94's assertion list. Reuse the existing harness and `startStaticServer` helper; add no second server and no second `beforeAll`. Import `generateSigil` directly from `../../src/index.js` and use it as the byte-identity ORACLE — never restate an expected SVG string as a literal.

Add these assertions:

1. **Pure pass-through, stated as an equality.** A connected `<sigil-spinner>` with a valid `statement` and `planet` has `innerHTML` byte-identical to `generateSigil(statement, planet, options).svg` for the same inputs. This is the invariant that goes red the instant the element starts diverging from the raw-SVG embed path — it is the companion test named in 07-01's assumption-delta decision, and it upgrades the tracer's "an SVG is present" into "the element adds and removes nothing".

2. **Custom-property reach.** A `--sigil-*` override set on the host `<sigil-spinner>` or an ancestor changes the COMPUTED style of a descendant inside the rendered SVG — not merely the presence of a `var()` in the markup. This is the G-03-1 defect class one DOM layer deeper; read computed values with `getComputedStyle`, exactly as `theming-resolution.test.js` does.

3. **Class-selector reach.** A page-level rule such as `.sigil-path { stroke-dasharray: 4 2; }`, authored in the page's own `<style>` from OUTSIDE the element, actually matches and applies to the element's light-DOM children. This is the empirical test of D-82 rather than trust in the implementation choice: assert the computed value differs from the unstyled default.

4. **The D-81 presence-semantics footgun, pinned.** `<sigil-spinner curve="false" …>` produces the same `innerHTML` as `generateSigil(statement, planet, { curve: true, … })` — the literal string `false` enables curves, because the attribute is present. Assert against the oracle, and assert it differs from the curve-disabled output, so the test cannot pass vacuously.

5. **Verbatim encoding.** A `statement` containing non-ASCII characters, passed through the element, yields `innerHTML` byte-identical to calling `generateSigil` directly with the same string. The element trims nothing, folds no case, and normalizes no Unicode of its own — every letter-handling rule stays owned by the library.

6. **Long statement.** With a `statement` of at least 500 characters, the rendered `<svg>` still carries `viewBox="0 0 100 100"` and the `.sigil-path` still has a non-degenerate bounding box (both `getBBox()` dimensions greater than zero). Sigil geometry is bounded by kamea order, not statement length — more kept letters densify the path inside the same fixed viewBox rather than overflowing it.

7. **The served example page actually loads its module.** Navigate to `examples/element.html` through the same static server and assert at least one `.sigil-path` is rendered. This is the assertion that catches the module-load failure class: if the server ever stops sending `Content-Type: text/javascript`, Chromium's strict MIME enforcement rejects the module silently, every `<sigil-spinner>` on the page stays inert, and no console error of the page's own is produced. Make the failure message say so, so a future maintainer reads a diagnosis rather than "expected 1, got 0".

Use Playwright's own visibility check plus a `getBBox()` geometry read where the claim is "on screen with real geometry"; use `getComputedStyle` where the claim is about style resolution. Give each assertion a title naming the decision or gap it proves (D-81, D-82, D-85, D-86) so a failure is self-locating.
  </action>
  <verify>
    <automated>npx vitest run test/browser/element.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/browser/element.test.js` exits 0 and the reported test count is at least 7 higher than the tracer's count recorded in `07-01-SUMMARY.md`.
    - A test asserts strict equality between an element's `innerHTML` and `generateSigil(...).svg` for the same inputs, with the expected value produced by calling `generateSigil` in the test — `grep -c "<svg" test/browser/element.test.js` shows no hardcoded expected SVG markup literal.
    - A test reads a computed style through `getComputedStyle` after a `--sigil-*` override and asserts the overridden value differs from the un-overridden value (both values are read, not one asserted against a constant).
    - A test authors a `.sigil-path` rule in the page's own `<style>` and asserts the computed property differs from the unstyled default.
    - A test uses the literal attribute value `curve="false"` and asserts equality against the oracle called with the curve option enabled, plus inequality against the oracle called with it disabled.
    - A test uses a statement containing at least one non-ASCII character and asserts byte-identity against a direct `generateSigil` call.
    - A test uses a statement of length ≥ 500 and asserts the rendered `<svg>` carries `viewBox="0 0 100 100"` and the `.sigil-path` `getBBox()` width and height are both greater than 0.
    - A test navigates the page to the served `examples/element.html` and asserts at least one `.sigil-path` is present and visible; its failure message names the `Content-Type: text/javascript` / strict MIME cause.
    - Exactly one `beforeAll` and one `afterAll` exist in the file — the harness was extended, not duplicated.
    - `npm test`, `npm run typecheck`, and `npm run lint` all exit 0.
  </acceptance_criteria>
  <done>Every rendering and theming claim WRAP-01 and WRAP-02 make is proven by a browser assertion against a real oracle, including the equality that keeps the element's output identical to the raw-SVG path.</done>
</task>

<task type="auto">
  <name>Task 3: Browser assertions — lifecycle, error contract, and multi-instance independence</name>
  <files>test/browser/element.test.js</files>
  <precondition>Playwright's Chromium is installed — `npx vitest run test/browser/element.test.js` currently passes with Task 2's assertions in place.</precondition>
  <read_first>
    - `test/browser/element.test.js` — current state after Task 2
    - `.planning/phases/07-the-sigil-spinner-element/07-RESEARCH.md` § Architecture Patterns 3 in full — the three upgrade orderings, the finding that `isConnected` is already `true` for every `attributeChangedCallback` fired during a parser-created upgrade, and Pitfall 3 explaining why a render-call-count assertion would be wrong and brittle
    - `.planning/phases/07-the-sigil-spinner-element/07-CONTEXT.md` § D-88, D-90, D-91, D-92, D-93
    - `src/element/sigil-spinner-element.js` — the implemented inert path, error path, and guarded registration
    - `src/errors.js` — the exact `E_*` code strings the error assertions compare against
    - `README.md` § Multi-Embed Safety — the id policy these assertions encode
  </read_first>
  <action>
Extend `test/browser/element.test.js` with the lifecycle, failure, and multi-instance half of D-94's assertion list. Same harness, same server, no new `beforeAll`.

Add these assertions:

1. **Double registration does not throw (D-90).** Load the element module twice into one page and assert nothing throws and exactly one definition is registered. Capture page errors (a `pageerror` listener) so a `NotSupportedError` cannot pass unnoticed by only checking that the navigation resolved.

2. **All three upgrade orderings agree (D-88).** Assert IDENTICAL FINAL `innerHTML` across: (a) the element present in parsed HTML with the module already defined; (b) the element present in parsed HTML, upgraded later by a deferred/module script loaded after it — the most common real-world usage, and a materially different code path because it runs the upgrade algorithm rather than `createElement`; (c) `document.createElement` plus `setAttribute` calls made BEFORE `appendChild`. Assert on final content only. Never assert a render-call count and never spy on the render method expecting one call: in paths (a) and (b) `isConnected` is already `true` during the attribute reactions the upgrade enqueues, so the `isConnected` gate does not suppress them and the element legitimately renders more than once while producing the correct final content each time.

3. **Idempotent re-render.** Setting an observed attribute to the value it already holds re-renders and yields byte-identical `innerHTML`.

4. **Attribute mutation re-renders (WRAP-03).** Changing `planet` after insertion produces the new planet's sigil, verified against a `generateSigil` oracle call for the new value, not merely "the content changed".

5. **Several instances co-render independently with zero ids (D-93, WRAP-03).** Put several `<sigil-spinner>` elements on one page, including two with identical `statement` and `planet` and no `id-prefix`. Assert each contains exactly one `<svg>`, the total count of `id` attributes anywhere within their rendered subtrees is zero, the two identical instances render byte-identical SVG, and re-rendering one instance leaves the others' `innerHTML` unchanged. No collision is possible because nothing to collide is emitted — this proves the claim by construction rather than by generated uniqueness.

6. **The inert state (D-91).** An element carrying only one of `statement`/`planet`, and one carrying neither, each have zero element children, produce no thrown error and no console error, and carry no `data-sigil-error` attribute. Then set the missing attribute on that SAME instance and assert it renders, with no page reload.

7. **Present-but-empty is not absent.** An element with `statement=""` and a valid `planet` treats the empty statement as PRESENT: it passes through to `generateSigil` and the library's own thrown code is surfaced on `data-sigil-error`. Contrast it in the same test file with an element missing `statement` entirely, which stays inert with zero element children and NO `data-sigil-error`. The element must neither coerce empty to absent nor absent to empty.

8. **The error round trip (D-92).** Set `planet` to an unknown value: assert the element clears to zero element children and reflects `data-sigil-error="E_UNKNOWN_PLANET"` on the host, and that `console.error` received a message beginning `<sigil-spinner> failed to render:` with an error object whose `code` is intact (capture via a `console` listener). Then set `planet` back to a valid value and assert the attribute is REMOVED and an `<svg>` child is restored — the full round trip, not only the error leg.

9. **The zero-instance page (D-90).** A page that loads the module but contains no `<sigil-spinner>` tag at all does not throw and still registers the definition; an element created with `document.createElement` and appended afterwards upgrades and renders correctly. This is a real state for a dynamically-built page, not only the many-instance case.

Give each assertion a title naming the decision it proves so a failure is self-locating.
  </action>
  <verify>
    <automated>npx vitest run test/browser/element.test.js &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/browser/element.test.js` exits 0; `npm test` exits 0.
    - `grep -cE "toHaveBeenCalledTimes|callCount|renderCount" test/browser/element.test.js` returns `0` — no render-call-count assertion exists anywhere in the file.
    - A test loads the element module twice in one page, registers a `pageerror` listener, and asserts zero page errors were captured.
    - A test asserts strict equality of final `innerHTML` across three distinctly-constructed cases: parsed-HTML-with-module-already-defined, parsed-HTML-upgraded-by-a-later-module-script, and `createElement` + `setAttribute` before `appendChild`.
    - A test sets an observed attribute to its current value and asserts the `innerHTML` before and after are strictly equal.
    - A test changes `planet` after insertion and asserts the result equals a `generateSigil` oracle call for the NEW planet.
    - A test renders at least three `<sigil-spinner>` elements including two with identical `statement`/`planet` and no `id-prefix`, and asserts: each has exactly one `<svg>` child; a `querySelectorAll('[id]')` count across their subtrees is `0`; the two identical instances' `innerHTML` are strictly equal; and mutating one leaves another's `innerHTML` unchanged.
    - A test asserts an element with only one required attribute has `children.length === 0`, `hasAttribute('data-sigil-error') === false`, and zero captured console errors — then sets the missing attribute on the same node and asserts a rendered `<svg>` appears.
    - A test distinguishes `statement=""` (surfaces a code on `data-sigil-error`) from an absent `statement` (no `data-sigil-error`, zero children) in the same file.
    - A test asserts `data-sigil-error` equals `E_UNKNOWN_PLANET` after an invalid planet, that a captured console error message starts with `<sigil-spinner> failed to render:` and its error argument's `code` is intact, and that setting a valid planet afterwards removes the attribute and restores an `<svg>` child.
    - A test loads the module on a page with zero `<sigil-spinner>` tags, asserts no page error, then appends a `document.createElement('sigil-spinner')` with attributes set and asserts it renders.
    - `npm run typecheck` and `npm run lint` both exit 0.
  </acceptance_criteria>
  <done>Every lifecycle, failure, and multi-instance claim WRAP-03, D-88, D-90, D-91, D-92 and D-93 make is proven by a browser assertion on observable final state, with no brittle call-count assertion anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page author's HTML attributes → element → DOM | unchanged from 07-01; this plan adds the assertions that keep it enforced |
| repo-only `examples/` directory → published tarball | a demo page that leaked into `files` would expand the published surface without review |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | element `innerHTML` write path | high | mitigate | Inherited from 07-01's D-86 source rule. This plan adds the behavioral half: the byte-identity assertion (`innerHTML` strictly equals `generateSigil(...).svg`) goes red the moment any element-local string is added to or removed from the library's output, which is the observable signature of a second, unescaped injection path. |
| T-07-04 | Tampering | `examples/element.html` | low | mitigate | The page is repo-only. `package.json`'s `files` array is left byte-unchanged by this plan (acceptance criterion on Task 1), and plan 07-03 asserts no `examples/` path appears in the `npm pack --dry-run` manifest. |
| T-07-07 | Tampering | `examples/element.html` script block | medium | mitigate | The live control writes only through `setAttribute` with a value drawn from a closed seven-option `<select>`; the page builds no markup by string assignment at all, enforced by a source assertion (`innerHTML` count is zero in the example page). A demo page that hand-built markup around the library's output would reintroduce exactly the surface D-86 forbids in the element itself. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed this phase — `.planning/phases/07-the-sigil-spinner-element/07-RESEARCH.md` § Package Legitimacy Audit records N/A. The example page loads no CDN script and no web font, enforced by a source assertion that no `http` or `https` reference and no `@font-face` appears in it. |
</threat_model>

<verification>
- `npx vitest run test/browser/element.test.js` — D-94's nine assertions plus the edge and state-coverage predicates all pass in a real browser.
- `npm test` — the full suite green.
- `npm run typecheck` and `npm run lint` — both exit 0.
- The served-page assertion navigating to `examples/element.html` proves the demo page's module actually loads, closing the strict-MIME silent-failure class.
- Source assertions on `examples/element.html`: no external network reference, no web font, no markup built by string assignment, `files` untouched.
</verification>

<success_criteria>
- ROADMAP success criterion 2 (both theming mechanisms reach the element) is proven by computed-style assertions, not asserted in prose.
- ROADMAP success criteria 3 and 4 (attribute mutation re-renders; several elements independent with no id collisions) are proven by browser assertions on final observable state.
- The element's rendered output is proven byte-identical to `generateSigil`'s, so the two documented embedding paths cannot silently diverge.
- `examples/element.html` exists and renders the three theming demonstrations, the live control, the glyph and grid instances, and the deliberate error instance — ready for the human look in plan 07-04.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol below is NEW in Phase 7 and did not exist before it. Do not flag these as drift against prior phases.

| Artifact | Kind | Created in |
|---|---|---|
| `src/element/sigil-spinner-element.js` | new file (first `src/element/` module) | 07-01 |
| `SigilSpinnerElement` | exported class (first `HTMLElement` subclass in the tree) | 07-01 |
| `SigilSpinnerElement.observedAttributes` | static getter | 07-01 |
| `TAG_NAME` | module-level constant, value `sigil-spinner` | 07-01 |
| `<sigil-spinner>` | custom element tag name | 07-01 |
| `show-title` (or the checkpoint-selected equivalent) | new HTML attribute | 07-01 |
| `data-sigil-error` | new host attribute reflecting a `SigilError` code | 07-01 |
| `test/browser/element.test.js` | new test file | 07-01 (expanded 07-02) |
| `startStaticServer` | new test helper (`node:http` static file server) | 07-01 |
| `examples/` directory and `examples/element.html` | new repo-only page (not in `files`) | 07-02 |
| `live-planet` | new element id on the example page's `<select>` control | 07-02 |
| `exports["./element"]`, `exports["./package.json"]` | new `package.json` subpaths | 07-03 |
| `resolveOnly` field on `ENTRY_POINTS` rows | new test-data field | 07-03 |
| `test/element-docs.test.js` | new README/`observedAttributes` drift guard | 07-03 |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-the-sigil-spinner-element/07-02-SUMMARY.md` when done
</output>
