---
phase: 08-the-sigil-skill
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - skill/references/correspondences.md
  - skill/SKILL.md
autonomous: false
requirements: [SKILL-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No correspondence row reaches the installed skill without having been ratified by Matt; an unratified row is either visibly marked as a draft or absent."
    - "The correspondence file records that the rows came from Matt directly, on a named date, and are not general astrological training."
    - "The skill instructs that the chosen planet and the domain of intent it was matched on are stated out loud in the reply, and that the choice is never bounced back to the user as a question."
    - "If the ratification gate is not answered, the phase closes with the pending marker in place and a description that does not claim planet-selection judgment — never with a guess."
    - "The documented invocation string runs against the published registry from a directory outside this repository, and the whole suite, typecheck, lint, and pack smoke test are green on the final tree."
  artifacts:
    - path: "skill/references/correspondences.md"
      provides: "The worked examples for ambiguous cases, plus the lineage attribution line and capture date"
      contains: "correspondences"
    - path: "skill/SKILL.md"
      provides: "The seven-row correspondence table, the selection heuristic covering the two-planet case, and the state-your-reasoning instruction"
      contains: "planet"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/correspondences.md"
      via: "a direct one-hop link — the table and heuristic load with the body, the worked examples are read on demand"
      pattern: "references/correspondences\\.md"
    - from: "skill/SKILL.md"
      to: "the reply a session writes"
      via: "an explicit instruction to name the planet and the matched domain of intent, and never to ask the user to choose"
      pattern: "reasoning"
  prohibitions:
    - statement: "MUST NOT present correspondences derived from general astrological training, or from an unratified corpus draft, as Matt's ratified lineage. A confident row sourced from general training is worse than no row, because the skill would claim exactly the authority the requirement exists to guarantee."
      category: transparency
---

<objective>
Capture the one part of this phase that is genuinely Matt's and genuinely un-researchable — the planet correspondences — and then seal the phase against both the live registry and the cold-session check.

Purpose: the phase is ordered so this plan is last. Everything else already works without it, so a "no" or a "not now" at the ratification gate costs one plan rather than the whole phase. Claude arrives at that gate holding a corpus-derived draft to correct rather than a blank page, because correcting a wrong row is a reliable operation where generating seven rows from nothing is not. Nothing ships unratified: the alternative to a ratified table is the pending marker and the narrower description, not a guess.

Output: `skill/references/correspondences.md`, the correspondence section of `skill/SKILL.md`, and the phase seal.

Decisions implemented by this plan: D-110 (the seven-row table plus written heuristic inline, worked examples one hop away), D-111 (state the reasoning out loud; never bounce the choice back as a question), D-112 (attribution as Matt's lineage with a capture date), D-113 (the blocking ratification checkpoint, arrived at holding a corpus-grounded draft rather than a blank page), D-114 (the plan is last, and the declined branch keeps the pending marker and the narrowed description rather than shipping a guess), D-117 (the live registry discharge), D-119 (the cold-session gate at the seal, with no automated proxy). D-102's fifth required element lands here in the ratified branch.
</objective>

## Artifacts this phase produces

| Artifact | Created in | Note |
|---|---|---|
| `skill/SKILL.md` | 08-01 (spine), extended 08-03, completed here | New top-level repo-only directory |
| `scripts/skill-install.js` | 08-01 | New top-level repo-only directory |
| `package.json` → `scripts["skill:install"]` | 08-01 | The phase's only `package.json` change |
| `eslint.config.js` / `tsconfig.json` → `scripts/**/*.js` globs | 08-01 | Keeps the new script inside both hygiene gates |
| `test/skill-cli-parity.test.js` | 08-02 | Internal `parseCliOptionKeys(source)`, `parseSkillFlagTokens(md)`, `INTENTIONALLY_UNDOCUMENTED` |
| `test/skill-install-parity.test.js` | 08-02 | Internal `INSTALLED_ROOT`, `REPO_SKILL_ROOT` |
| `skill/VERIFY.md` | 08-03 | The instrument the two un-automatable criteria are performed with |
| `skill/references/correspondences.md` | 08-04 | Gated behind the ratification checkpoint in Task 2 |
| `~/.claude/skills/sigil/**` | installed by 08-01, refreshed 08-03 and here | Outside the repository; never the source of truth |

Deliberate non-targets: `package.json`, `test/pack-install.test.js`, `src/`, `bin/`, `README.md`. This plan changes exactly two files in the repository and reinstalls.

## Flagged Assumptions (spec-less edge probe)

| Requirement | Probe result | Surfaced as |
|---|---|---|
| SKILL-02 | `unclassified` / `unresolved` | The probe's shape taxonomy has no category for "a judgment is grounded in a specific person's lineage rather than in general training." There is no boundary, adjacency, encoding, or ordering property to derive, and no defensible acceptance criterion could be written from the requirement text alone — which is precisely why this requirement carries a human gate on the roadmap. Assumption: the requirement is discharged by two things a machine can check (the rows carry an attribution line naming Matt and a capture date; no row carries a draft marker) plus one thing only a human can (the rows are actually his). If that assumption is wrong, the failure mode is a skill whose confidence exceeds its authority — which the ratification gate in Task 2 and the transparency prohibition in `must_haves` are the only defenses against. |

Rows for SKILL-01 and SKILL-03 are surfaced in 08-01 and 08-02. No-silent-drop equality holds across the plan set: 3 surfaced, 0 dropped.

The third of three kept prohibitions is authored here; the other two are in 08-01. One candidate was dropped by canon referral rather than minted: shell-metacharacter injection through the invocation recipe is canon injection, covered by `/gsd-secure-phase` and the threat models in 08-01 and this plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-the-sigil-skill/08-CONTEXT.md
@.planning/phases/08-the-sigil-skill/08-RESEARCH.md
@.planning/phases/08-the-sigil-skill/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The corpus-grounded draft, every row marked unratified</name>
  <precondition>The `mcp__psyche-semantic__semantic_recall` tool is available in this session (a read-only availability check; if it is not, halt and surface rather than substituting general astrological training).</precondition>
  <files>skill/references/correspondences.md</files>
  <read_first>
- `skill/SKILL.md` as it stands after 08-03 — the pending-correspondences comment this task must leave in place, and the section shape Task 3 will fill.
- `~/.psyche/identity/behaviors.md` § Corpus-Grounded Readings — the enumerated astrological and cross-cutting corpora this draft pulls from, the union rule, the hold-the-divergence rule, and the honesty fallback when recall returns nothing relevant.
- `~/.psyche/identity/cognitive-profile.md` § Clinical EF Calibration — severely impaired task initiation and strong response inhibition and metacognition. This is the evidence behind arriving at the gate with a draft rather than with seven open questions.
- `.planning/phases/08-the-sigil-skill/08-CONTEXT.md` — the decisions governing the table's shape, the attribution requirement, and the draft-to-correct choreography.
- `.planning/PROJECT.md` § Current Milestone — the "must come from Matt, not from general training" framing this requirement exists to satisfy.
  </read_first>
  <action>
Per D-113, call `mcp__psyche-semantic__semantic_recall` with `content_type` set to knowledge, once per corpus cluster rather than once overall, and draw from the union of everything relevant rather than from the nearest single match. The astrological corpora enumerated in behaviors.md are the School of Astrological Arcana, Celestial Alchemy, the podia mythology-for-astrologers and techniques-for-intermediate-astrologers material, the Eighth House Astrology substack, and the Artemisian. The cross-cutting corpora that may inform any reading are CreativeMind, the Mórrígan Intensive, New Thought, and elemental witchcraft. Where two lineages diverge on a planet's domain, hold the divergence in the draft rather than averaging it — these are distinct lineages, not one consensus.

Write `skill/references/correspondences.md` containing a seven-row proposed table, one row per classical planet in the order the library validates them: saturn, jupiter, mars, sun, venus, mercury, moon. Each row carries the planet, the domains of intent it governs, and a one-line statement of when to reach for it. Every row is prefixed with an explicit proposed-and-unratified marker, per D-113.

Open the file with a header block stating in plain terms what it is and is not: these rows are a corpus-derived draft assembled from Matt's own onboarded material, not his ratified lineage; nothing here may be presented as his judgment until the ratification gate closes; and D-112's attribution line and capture date are deliberately absent and land only after ratification, because an attribution written before ratification would be the exact false claim this whole structure exists to prevent.

Apply the honesty fallback per corpus rule: if recall returns nothing relevant for a given planet, say so in that row rather than filling it from general astrological training. A row reading that no corpus support was found is the correct output. A confident row sourced from general training is the specific dishonesty this requirement exists to prevent, and it is worse than shipping without the correspondences at all.

Do not touch `skill/SKILL.md` in this task. Per D-114 the pending marker stays until the gate closes. Do not install; per D-113 nothing ships until Matt ratifies, so the draft is not meant to reach the destination in this state.
  </action>
  <verify>
    <automated>test -f skill/references/correspondences.md &amp;&amp; test "$(grep -ci 'unratified' skill/references/correspondences.md)" -ge 7 &amp;&amp; git diff --quiet -- skill/SKILL.md &amp;&amp; echo DRAFT_UNRATIFIED_OK</automated>
  </verify>
  <acceptance_criteria>
- `skill/references/correspondences.md` exists and contains a table with exactly seven data rows, one per classical planet, covering saturn, jupiter, mars, sun, venus, mercury and moon — the same seven names the library's planet resolver accepts.
- Every one of the seven rows carries an unratified marker: `grep -ci 'unratified' skill/references/correspondences.md` is at least 7.
- The file contains no attribution line and no capture date yet: `grep -ci 'captured from Matt\|capture date' skill/references/correspondences.md` is 0.
- The header block states that the rows are a corpus-derived draft and must not be presented as Matt's judgment before ratification.
- `git diff --quiet -- skill/SKILL.md` exits 0 — `skill/SKILL.md` is untouched by this task and its pending marker is intact.
- The summary records which corpora `semantic_recall` actually returned results from, and names any planet for which it returned nothing relevant.
- No install was run: `cmp -s skill/SKILL.md "$HOME/.claude/skills/sigil/SKILL.md"` still exits 0 and the destination contains no correspondences file.
  </acceptance_criteria>
  <done>A seven-row corpus-derived draft exists in the repository with every row visibly unratified, no attribution, no capture date, and no path to the installed skill — ready to be corrected rather than authored at the gate.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Ratify the seven planet correspondences, row by row</name>
  <decision>Whether the seven-row correspondence draft in `skill/references/correspondences.md` becomes Matt's ratified lineage in the skill, with or without corrections — or whether the phase closes without it.</decision>
  <context>
This is the roadmap's second human gate and the only one still open. SKILL-02 requires that planet selection be a reasoned choice grounded in Matt's own correspondences rather than a guess or a question bounced back — and correspondences are lineage knowledge, not something research can supply.

The draft in front of you was assembled from your own onboarded corpus, one call per corpus cluster, drawn from the union rather than the nearest match, with divergences between lineages held rather than averaged. Every row is marked unratified. Correcting a wrong row is cheap; generating seven rows from a blank page is the elicitation that stalls, which is why the draft exists at all.

The undo cost is one-way in substance: an unratified correspondence set that ships is a skill claiming lineage authority it does not have, and it cannot be un-relied-upon by whoever already used it. That is why nothing reaches the installed skill until this gate closes.

The cost of declining is deliberately small. This is the last plan in the phase; everything else already works. Declining costs one plan, not the phase.
  </context>
  <options>
    <option id="ratify-all">
      <name>Ratify every row as drafted</name>
      <pros>The skill ships complete: the table, the heuristic, and the description's judgment trigger all land.</pros>
      <cons>None beyond the rows being right — which is what this gate is asking.</cons>
    </option>
    <option id="ratify-with-corrections">
      <name>Ratify with corrections — state the corrected rows inline</name>
      <pros>The expected path. The rows become genuinely yours rather than the corpus's approximation of them, and the corrections are what make the attribution honest.</pros>
      <cons>Requires reading seven rows and correcting the wrong ones.</cons>
    </option>
    <option id="not-now">
      <name>Not now — close the phase without SKILL-02</name>
      <pros>Costs one plan rather than the phase. Nothing already shipped depends on this. The skill still fires, still invokes correctly, and still carries the embedding checklist and the drift guard.</pros>
      <cons>The skill will bounce planet selection back to the user rather than reasoning about it. The pending marker stays visible in the file and the description stays narrowed so it does not promise judgment the body cannot deliver.</cons>
    </option>
  </options>
  <resume-signal>Select: ratify-all, ratify-with-corrections (with the corrections stated), or not-now</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the gate's outcome, reinstall, and seal the phase</name>
  <reversibility rating="one-way">What the skill claims about the provenance of its planet judgment cannot be un-relied-upon by a session that already acted on it; the attribution line and capture date are what let a later reader tell a ratified row from a drifted one.</reversibility>
  <files>skill/SKILL.md, skill/references/correspondences.md</files>
  <read_first>
- The resolution of Task 2's checkpoint, verbatim, including any corrections stated inline.
- `skill/SKILL.md` as it stands after 08-03 — the narrowed description, the pending-correspondences comment, and the section the table and heuristic go into.
- `skill/references/correspondences.md` as drafted in Task 1.
- `skill/VERIFY.md` as authored in 08-03 — both procedures are run from this task.
- `.planning/phases/08-the-sigil-skill/08-CONTEXT.md` — the required shape of the table and heuristic, the reasoning instruction and its negative half, the attribution requirement, and the fallback if the gate declined.
- `test/skill-cli-parity.test.js` — the row-anchored flag extraction, so the correspondence table's own row shape cannot be mistaken for a flag row.
  </read_first>
  <action>
Branch on the gate's resolution. Under no circumstance fill the table from general astrological training — that is the one outcome both branches exist to make impossible.

If the gate ratified, with or without corrections: apply every stated correction first, then write the ratified seven-row table into the correspondence section of `skill/SKILL.md` per D-110, replacing the pending-correspondences HTML comment. Each row carries the planet, the domains of intent it governs, and a one-line statement of when to reach for it. Immediately after the table, write the selection heuristic in prose. D-110 requires the heuristic to cover the case a flat table structurally cannot — a statement that reads as two planets at once — by naming which axis wins and why. That case is the whole difference between a reasoned choice and a lookup, so a heuristic that only restates the table has not done its job.

Add D-111's reasoning instruction, both halves. The positive half: name the chosen planet, name the domain of intent it was matched on, and when the statement was genuinely ambiguous, name the axis that decided it and what the runner-up was. The negative half, stated because it is the failure mode the criterion calls out by name: never bounce the choice back to the user as a question. A stated reasoned choice the user can correct in one sentence is the deliverable; an interrogation is the thing this skill exists to remove.

Broaden the description frontmatter to add D-102's fifth required element — the judgment trigger 08-01 deliberately withheld under D-114, namely that the skill applies when choosing which planet suits an intention. Keep the other four required elements intact, including the skip clause naming the two adjacent installed skills. Stay inside the combined character cap.

Expand `skill/references/correspondences.md` into its final form: two to four worked examples covering the ambiguous cases the heuristic describes, each showing the two candidate planets, the axis that decided it, and the runner-up. Strip every unratified marker. Add D-112's attribution line recording that the correspondences came from Matt directly, on the date this gate closed, and that they are not general astrological training — this line is what makes the planet choice trustworthy rather than merely confident, and what lets a future reader tell whether a row was ratified or drifted in later.

Link the reference file from `skill/SKILL.md` directly, one hop, never through an intermediate index. Per D-110 the table and heuristic load with the body because they are what is needed at the moment of choosing; the worked examples are read on demand because they are the long part and are needed only for the ambiguous minority.

If the gate declined: take D-114's fallback exactly and change nothing about the correspondence content. Leave the pending-correspondences comment where it is, leave the description narrowed so it does not claim planet-selection judgment, leave every unratified marker in the reference file, and record in the summary that SKILL-02 was not delivered this phase and why. Then proceed to the seal steps below unchanged — everything else in the phase still holds.

Either way, finish by reinstalling with the force flag so the destination carries the final tree, then running the whole gate set: the suite, typecheck, lint, and the pack smoke test, plus D-117's live registry procedure from `skill/VERIFY.md`, executed from a scratch directory outside this repository. Record the resolved version, the exit status, the first bytes of stdout and the contents of stderr in the summary. Then hand `skill/VERIFY.md`'s cold-session procedure to the human check — per D-119 it is a blocking gate at the phase seal with no automated proxy, because a subagent spawned from this session inherits its context and would prove nothing about a cold session's routing.
  </action>
  <verify>
    <automated>npm run skill:install -- --force &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:pack &amp;&amp; D="$(mktemp -d)" &amp;&amp; ( cd "$D" &amp;&amp; npx -y @falkensmage/sigil-spinner@latest 'I WILL FINISH THIS' --planet saturn ) | head -c 5 | grep -q '&lt;svg' &amp;&amp; echo PHASE_SEAL_OK</automated>
    <human-check>
Run Procedure 1 in `skill/VERIFY.md` — the cold-session check for ROADMAP success criterion 1. Preconditions matter and produce a false negative if skipped: the forced install above must have completed in *this* session, and the check must be run in a brand-new Claude Code session opened afterwards, in a scratch directory that is not this repository and has no installed dependency tree.

Type the two prompts exactly as they appear in the file. Do not name the tool, the package, or any flag at any point.

Record pass or fail for all four conditions separately: the skill fired without being named; a correct sigil was produced; a planet was chosen with the reasoning stated in the reply; and no flag or package name came from the user.

If the ratification gate returned not-now, the third condition is expected to fail. Record that outcome as expected rather than as a defect — the description was written narrow precisely so the skill does not promise reasoning the body cannot deliver.
    </human-check>
  </verify>
  <acceptance_criteria>
- `npm test`, `npm run typecheck`, `npm run lint` and `npm run test:pack` all exit 0 on the final tree.
- `npm run skill:install -- --force` exits 0, and `npx vitest run test/skill-install-parity.test.js` reports no divergence and no orphan.
- `npx vitest run test/skill-cli-parity.test.js` exits 0 — the correspondence table's row shape did not register as a flag row, and the extracted flag-token set is still exactly the seven from 08-01.
- D-117's live registry procedure was run from a `mktemp -d` directory: exit status 0, stdout beginning with an `<svg` element, empty stderr, and the resolved version recorded in the summary.
- Ratified branch: `grep -c 'SKILL-02 pending' skill/SKILL.md` is 0; `skill/SKILL.md` contains a seven-row planet table plus a prose heuristic that explicitly addresses a statement reading as two planets at once; `grep -ci 'never ask the user to choose\|do not ask the user\|never bounce' skill/SKILL.md` is at least 1; `grep -c 'references/correspondences.md' skill/SKILL.md` is at least 1; `grep -ci 'unratified' skill/references/correspondences.md` is 0; the attribution line naming Matt and a capture date is present; and `skill/references/correspondences.md` contains between 2 and 4 worked examples. <!-- planner-discipline-allow: SKILL-02 pending -->
- Ratified branch: the description frontmatter now carries D-102's fifth element, the judgment trigger for choosing which planet suits an intention, alongside the four elements 08-01 committed, and the skip clause naming the two adjacent skills is still present.
- Declined branch: `grep -c 'SKILL-02 pending' skill/SKILL.md` is 1; the description frontmatter is byte-unchanged from 08-03 (`git diff -U0 -- skill/SKILL.md | grep -c '^[+-]description:'` is 0); `grep -ci 'unratified' skill/references/correspondences.md` is at least 7; and the summary records SKILL-02 as not delivered with the gate's stated reason. <!-- planner-discipline-allow: SKILL-02 pending -->
- Either branch: `grep -rEl 'RitualSync|/Users/' skill/` produces no output, and `git status --porcelain package.json test/pack-install.test.js src bin README.md` produces no output. <!-- planner-discipline-allow: RitualSync --> <!-- planner-discipline-allow: /Users/ -->
- D-119's cold-session human check has been run and all four D-118 conditions are recorded individually with the date and the Claude Code version.
  </acceptance_criteria>
  <done>The gate's outcome is authored honestly into both files — a ratified, attributed, dated table with a heuristic covering the two-planet case, or the pending marker and narrowed description left exactly in place — the installed copy is byte-identical to the repository, every automated gate is green on the final tree, the documented invocation is proven against the live registry from outside this repository, and the cold-session check has been performed and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus recall → asserted lineage | A model-assembled draft crossing into content the skill presents as a specific person's ratified judgment. This is the phase's highest-consequence boundary and the only one guarded by a human rather than a test. |
| repository → home directory | The final forced install writes the completed tree into the personal skill directory. |
| public npm registry → cold machine | The seal's live procedure fetches and executes the published package from a scratch directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-14 | Spoofing | Correspondence content presented as Matt's lineage | high | mitigate | Three layers, in order. The draft is written with every row visibly marked unratified and with no attribution line, so it cannot accidentally read as ratified. Nothing reaches the installed skill until the blocking ratification checkpoint closes. The attribution line and capture date are written only in the ratified branch, and their absence in the declined branch is itself the signal. Acceptance criteria assert the marker count in both directions, so neither branch can silently take the other's shape. |
| T-08-15 | Repudiation | A correspondence set that has drifted from current practice | medium | accept | The attribution line's capture date is what lets a later reader tell when the rows were ratified, but there is no mechanism proposed here for noticing that a shipped table has gone stale relative to Matt's current practice — the flag parity guard covers flags, not judgment. Accepted with the gap recorded; CONTEXT.md already carries the reopen condition, which is the arrival of the first correction. |
| T-08-16 | Tampering | The correspondence table's row shape read as a flag row | medium | mitigate | The flag guard's regex is anchored to a backticked double-hyphen token in a row's first cell; a planet name is neither. An acceptance criterion re-runs the parity guard after the table lands and asserts the extracted token set is still exactly the seven from 08-01. |
| T-08-17 | Tampering | The forced reinstall overwriting the destination | medium | mitigate | The install is one-directional by construction, the destination is built from the home directory plus fixed segments and never from input, and the force flag names every path it overwrites. The install-parity guard runs immediately afterwards and reports divergence and orphans together. |
| T-08-18 | Information Disclosure | Personal lineage material reaching the registry | high | mitigate | The published file allowlist was frozen in Phase 6 and confirmed unextended at 08-01's one-way gate; an acceptance criterion here re-asserts that the manifest and the pack smoke test are both unmodified on the final tree, and the pack smoke test itself runs green as part of the seal. |
| T-08-SC | Tampering | The registry fetch performed by the seal's live procedure | low | accept | No package is installed into this repository by this plan; `dependencies` and `devDependencies` are untouched, so the package-legitimacy gate does not apply. The runtime fetch is of this project's own scoped package, published in Phase 6 with a verified provenance attestation, with the fully scoped name documented exactly. Accepted with the attestation as the transfer. |
</threat_model>

<verification>
- The checkpoint resolution recorded verbatim, including any corrections stated inline.
- `npm test`, `npm run typecheck`, `npm run lint` and `npm run test:pack` all exit 0 on the final tree.
- The install-parity and CLI-parity guards both green after the final forced install.
- The live registry procedure recorded with resolved version, exit status, first bytes of stdout, and stderr contents.
- The cold-session check recorded with all four conditions individually marked, plus date and Claude Code version.
- `git status --porcelain package.json test/pack-install.test.js src bin README.md` produces no output.
</verification>

<success_criteria>
ROADMAP success criteria 2 and 3 are settled and criterion 1 is measured. Either the planet choice is a reasoned one grounded in correspondences Matt ratified, attributed and dated in the file, with the reasoning stated out loud and never bounced back as a question — or the phase closes with that requirement visibly undelivered and a description that does not promise it. In both outcomes the documented invocation is proven against the published registry from outside this repository, every automated gate is green, and the cold-session check has been performed with its four conditions recorded.
</success_criteria>

<output>
Create `.planning/phases/08-the-sigil-skill/08-04-SUMMARY.md` when done. Record the checkpoint resolution verbatim, which branch was taken, the corpora `semantic_recall` returned results from, the live registry transcript, and the four cold-session conditions with their outcomes.
</output>
