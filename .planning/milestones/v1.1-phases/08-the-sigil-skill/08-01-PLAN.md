---
phase: 08-the-sigil-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/SKILL.md
  - scripts/skill-install.js
  - package.json
  - eslint.config.js
  - tsconfig.json
autonomous: false
requirements: [SKILL-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`skill/SKILL.md` exists in the repository and is the only place the skill's content is authored."
    - "`npm run skill:install` copies `skill/` into the personal skill directory in one direction only, and refuses to replace a destination file that differs from its repo counterpart unless `--force` is passed."
    - "The invocation string the skill documents runs against the published registry from a directory outside this repository and writes an SVG to stdout."
    - "The npm tarball still contains no repo-only directory — `skill/` and `scripts/` are absent from `npm pack` output."
    - "`npm run lint` and `npm run typecheck` both cover the new install script rather than silently skipping it."
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Agent Skills frontmatter (name/scope/description/allowed-tools), the invocation recipe with its shell-safety rule, and the seven-row CLI flag table"
      contains: "name: sigil"
    - path: "scripts/skill-install.js"
      provides: "One-directional repo -> personal-skill-directory install with a fail-rather-than-merge divergence check"
      contains: "homedir"
    - path: "package.json"
      provides: "The single new `scripts` key, `skill:install`"
      contains: "skill:install"
  key_links:
    - from: "skill/SKILL.md"
      to: "the published npm package"
      via: "the documented npx invocation string — scoped package name plus @latest, no local path"
      pattern: "npx -y @falkensmage/sigil-spinner@latest"
    - from: "scripts/skill-install.js"
      to: "the personal skill directory"
      via: "os.homedir() joined with fixed path segments, never a caller-supplied destination"
      pattern: "homedir\\(\\)"
  prohibitions:
    - statement: "MUST NOT capture adjacent symbolic work — tarot spreads, natal charts, transits, dream interpretation, or general symbolic synthesis — that belongs to the already-installed `oracle` skill. An over-broad description that fires on all of it erodes trust in skill-surfacing generally."
      category: values
    - statement: "MUST NOT distribute Matt's personal lineage material to npm consumers. The published tarball's file list is a one-way contract and the skill carries judgment that is his, not the library's."
      category: privacy
---

<objective>
Prove the whole runtime path of the sigil skill end to end, in one thin slice: a repo-canonical `skill/SKILL.md` that a one-directional install script copies into the personal skill directory, whose documented invocation string then runs for real against the published registry from a directory that has never seen this repository.

Purpose: this is the phase's tracer. RESEARCH.md's architecture describes two deliberately unbraided flows — the runtime path a cold session takes, and the in-repo mechanical drift check. This plan traces the runtime path through every one of its layers on one path, so a wrong assumption about install semantics, frontmatter shape, or npx resolution surfaces after one commit rather than after four.

Output: `skill/SKILL.md` (frontmatter, invocation recipe, flag table, pending-correspondences marker), `scripts/skill-install.js`, one new `package.json` script key, and the lint/typecheck glob extensions that keep the new script inside the repo's existing hygiene gates.

Decisions implemented by this plan: D-99 (repo-canonical source), D-100 (tarball boundary), D-102 (the description's required elements), D-103 (frontmatter shape and declared tools), D-104 (the inline flag table and its amendment of the delegate-to-help guidance), D-114 (the pending-correspondences marker and the narrowed description while the gate is open), D-115 (the invocation form and the no-local-path rule).
</objective>

## Artifacts this phase produces

Every symbol Phase 8 creates, across all four plans. This plan creates the first three groups.

| Artifact | Created in | Note |
|---|---|---|
| `skill/SKILL.md` | 08-01 (spine), extended 08-03, 08-04 | New top-level repo-only directory |
| `scripts/skill-install.js` | 08-01 | New top-level repo-only directory |
| `package.json` → `scripts["skill:install"]` | 08-01 | The phase's only `package.json` change |
| `eslint.config.js` → `scripts/**/*.js` in the `files` glob | 08-01 | Keeps the new script linted |
| `tsconfig.json` → `scripts/**/*.js` in `include` | 08-01 | Keeps the new script type-checked |
| `test/skill-cli-parity.test.js` | 08-02 | Exports nothing; internal `parseCliOptionKeys(source)`, `parseSkillFlagTokens(md)`, `INTENTIONALLY_UNDOCUMENTED` |
| `test/skill-install-parity.test.js` | 08-02 | Internal `INSTALLED_ROOT`, `REPO_SKILL_ROOT` |
| `skill/VERIFY.md` | 08-03 | The instrument the two un-automatable criteria are performed with |
| `skill/references/correspondences.md` | 08-04 | Gated behind the D-113 ratification checkpoint |
| `~/.claude/skills/sigil/**` | installed by 08-01, refreshed by 08-03/08-04 | Outside the repository; never the source of truth |

Deliberate non-targets, recorded so they are not "fixed" by pattern-matching Phase 7: `package.json`'s `files`, `exports`, `main`, `bin`, and `version`; `test/pack-install.test.js`'s `EXPECTED_TARBALL_FILES` and `ALLOWED_TARBALL_ROOTS`; everything under `src/` and `bin/`.

## Flagged Assumptions (spec-less edge probe)

This phase has no SPEC.md, so the deterministic edge probe ran against the requirement text directly. It returned `unclassified` / `unresolved` for every row. Per the no-auto-dismiss rule these are surfaced rather than silently dropped, and rather than auto-resolved with a backstop.

| Requirement | Probe result | Surfaced as |
|---|---|---|
| SKILL-01 | `unclassified` / `unresolved` | The probe's shape taxonomy has no category for "a prose file causes a language model to route to it." Whether the skill fires is not a data-shape property and cannot be derived from the requirement text. Assumption: the only honest verification is the cold-session human check authored in 08-03 and run in 08-04. |
| SKILL-02 | `unclassified` / `unresolved` | Covered by 08-04's flagged-assumption block. |
| SKILL-03 | `unclassified` / `unresolved` | Covered by 08-02's flagged-assumption block. |

No-silent-drop equality holds: 3 probe-surfaced items, 3 surfaced as flagged assumptions across the plan set (SKILL-01 here, SKILL-03 in 08-02, SKILL-02 in 08-04), 0 dropped.

Prohibition recall ran the two-stage protocol in-prompt. Three bespoke items survived the precision pass and are authored descriptor-less into `must_haves.prohibitions` across this plan and 08-04. One candidate was dropped by canon referral: shell-metacharacter injection through the invocation recipe is canon injection, covered by `/gsd-secure-phase` and this plan's `<threat_model>`; not minted as a prohibition.

<assumption_delta_decision>
**The noun that is now primary:** the *correspondence mapping* — a rule from intent-domain to planet. Before this phase, `--planet` was a required, user-supplied, library-validated CLI parameter with no derivation rule modeled anywhere. This phase introduces one.

**Decision: `add-alongside`.**

Rationale: the CLI parameter does not change. `--planet` stays required and explicit, `resolvePlanetKey` still owns validation, and no inference enters `src/` or `bin/`. What is added is a *skill-layer* derivation rule that produces a value for that unchanged parameter. Promoting it — a `--planet auto`, or a mapping table in `src/data/` — would push Matt's personal lineage judgment into the published library, which D-100 exists to prevent, and would make correctness of a symbolic judgment a property of a byte-deterministic generator. Those are different kinds of claim and should not share an artifact.

**Accepted debt:** the derivation rule will live only in prose, in one skill file, with no mechanical check that it stays coherent. `test/skill-cli-parity.test.js` covers flags, not judgment — CONTEXT.md's Deferred Ideas already names this gap ("A re-ratification trigger for the correspondences").

**What would force a later promote:** a second consumer needing the derivation. An MCP server (MCP-01), a hosted UI (WRAP-04), or a `--planet auto` flag each make the mapping shared state, at which point it must move out of the skill into a versioned, testable artifact carrying the same drift-guard discipline as `KAMEA_SET_VERSIONS`.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-the-sigil-skill/08-CONTEXT.md
@.planning/phases/08-the-sigil-skill/08-RESEARCH.md
@.planning/phases/08-the-sigil-skill/08-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm where the skill's canonical source lives (D-99)</name>
  <decision>Whether the sigil skill's canonical source is a new in-repo `skill/` directory with a one-directional install into the personal skill directory, or lives somewhere else.</decision>
  <context>
CONTEXT.md was gathered in `--auto` mode; no decision in it was confirmed in conversation. D-99 carries a `costly` reversibility rating, so it is surfaced here before any file is written, following the Phase 7 precedent where the phase's one-way decisions were confirmed at a pre-execution gate.

Why it matters now: every other plan in this phase points at whichever answer this gate gives. SKILL-03's drift check reads the source location; the install script writes from it; the install-parity guard compares against it. Undoing this later means re-pointing all three, plus re-establishing which copy was authoritative during the gap.

Why the recommended option is recommended, in two independent arguments either of which is sufficient. First: a drift check that reads the personal skill directory works on exactly one machine and is a no-op on a fresh clone and on every CI runner — which is the entire population that would catch the drift, so SKILL-03 becomes unrunnable. Second: `~/.psyche/identity/design-principles.md` § 10 ("Maintenance Must Not Return to the Failing Substrate") was written from the prior failure of this exact pattern — an installed skill copy silently becoming the de facto truth until the next sync destroyed it — and its stated fix is repo-canonical source plus a byte-identity drift guard.
  </context>
  <options>
    <option id="option-a">
      <name>Repo-canonical `skill/`, installed copy, parity guard (D-99 as written)</name>
      <pros>SKILL-03 runs on a fresh clone and on CI. The content is versioned, diffable, and reviewable. Matches the design principle written from the prior failure of the alternative.</pros>
      <cons>Two copies exist, so a parity guard is required to keep them honest. A re-install step is needed after every content edit.</cons>
    </option>
    <option id="option-b">
      <name>Author only in the personal skill directory</name>
      <pros>One copy, no install step, no parity question.</pros>
      <cons>Un-versioned, un-diffable, un-reviewable. SKILL-03 becomes a check that can only ever run on one laptop, which is the same as not having it.</cons>
    </option>
    <option id="option-c">
      <name>Symlink the personal skill directory at the repo's `skill/`</name>
      <pros>Cheapest. Eliminates the parity question entirely.</pros>
      <cons>Couples a global skill's availability to this repo staying at one path on one machine. Checking out another branch silently swaps the installed skill underneath every open session.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the npm tarball boundary (D-100)</name>
  <decision>Whether `package.json`'s `files` array is extended to ship the skill directory in the published tarball.</decision>
  <context>
D-100 carries a `one-way` rating: a published tarball's file list cannot be retracted, only superseded by a new version. The gate exists because the recommended answer requires no action at all — `files` is an allowlist, currently `["src", "bin", "README.md"]`, frozen by Phase 6 — and a decision that looks like an omission is exactly the one a later agent "fixes."

What is at stake: the skill will carry Matt's own planet correspondences, which are personal lineage judgment rather than part of the library's contract. Shipping them would both change a frozen tarball surface and distribute that judgment to everyone who runs an install.

Consequence for this phase either way: under the recommended answer `test/pack-install.test.js` needs no edit at all — a notable difference from Phase 7, where the `examples/` precedent did get an edit for an unrelated reason.
  </context>
  <options>
    <option id="option-a">
      <name>Do not extend `files` — the skill stays repo-only (D-100 as written)</name>
      <pros>No action required; the allowlist already excludes it. The published contract is untouched. Personal lineage material stays out of the registry. `test/pack-install.test.js` needs no edit.</pros>
      <cons>Anyone wanting the skill must clone the repo and run the install script rather than getting it from npm.</cons>
    </option>
    <option id="option-b">
      <name>Add the skill directory to `files`</name>
      <pros>The skill travels with the package.</pros>
      <cons>Irreversible once published. Distributes personal esoteric judgment to every consumer. Requires editing the frozen `EXPECTED_TARBALL_FILES` and `ALLOWED_TARBALL_ROOTS` constants, expanding this phase's blast radius into Phase 6's verified surface.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="tracer">
  <name>Task 3: End-to-end — authored skill, installed copy, live registry invocation</name>
  <precondition>`npm view @falkensmage/sigil-spinner version` returns a published version (the Phase 6 artifact this phase's invocation recipe depends on), and `npx` is on PATH.</precondition>
  <reversibility rating="costly">The repo-canonical source location decided in Task 1 is referenced by the install script, the install-parity guard, and the CLI-parity guard; moving it later means re-pointing all three and re-establishing which copy was authoritative in between.</reversibility>
  <files>skill/SKILL.md, scripts/skill-install.js, package.json, eslint.config.js, tsconfig.json</files>
  <read_first>
- `bin/sigil-spinner.js` lines 1-135 — the header comment documenting D-09 (the `-` stdin sentinel), D-10 (artifact selection), D-11 (`--output` stdout purity) and D-12 (stderr diagnostics), and the `parseArgs` `options` object literal at lines 116-127 that is the authoritative flag set. Read it; do not import it and do not edit it.
- `package.json` — current `scripts`, `files`, `exports`, `bin`; the only edit here is one new `scripts` key.
- `eslint.config.js` — the first config block's `files` glob array, which currently omits any `scripts` path.
- `tsconfig.json` — the `include` array, which currently omits any `scripts` path.
- `README.md` § CLI (lines 69-109) — the flag semantics the skill's table restates.
- `~/.claude/skills/capture/SKILL.md` and `~/.claude/skills/draft/SKILL.md` — the house frontmatter convention and the pattern of embedding a non-negotiable constraint block directly in the skill body.
- `.planning/phases/08-the-sigil-skill/08-RESEARCH.md` — § Common Pitfalls 1 (what `allowed-tools` actually is), 2 (`scope` is not a recognized field), 3 (the shell-injection surface), and the live npx transcript in § Code Examples.
- `test/pack-install.test.js` lines 37-75 — `EXPECTED_TARBALL_FILES` and `ALLOWED_TARBALL_ROOTS`, read only to confirm they need no edit.
  </read_first>
  <action>
Create `skill/SKILL.md`, the repo-canonical source confirmed in Task 1 (D-99).

Frontmatter, per D-103: `name: sigil` — lowercase, and it must match the install directory's final segment or the skill does not load. `scope: personal` — kept for house consistency with the other installed skills; it is not a recognized Agent Skills field and does nothing at the platform level, since the directory determines scope. Do not describe it in the file as load-bearing. `allowed-tools` listing `Bash`, `Read`, `Write`, `Edit`, `Glob`, `Grep`. Declare all six for the correct reason: this field is a same-turn permission pre-approval, not a restriction — every tool stays callable regardless — so the cost of omitting one is an interactive permission prompt interrupting the cold session mid-invocation, which visibly contradicts SKILL-01's criterion. A bare `Bash` is correct; a command-scoped pattern is an acceptable refinement.

`description` must carry four of D-102's five required elements now, and the fifth in plan 08-04 per D-114. Element 1, what it does in the tool's own nouns: planetary sigils, kamea magic squares, intention statements, embeddable CSS-stylable inline SVG. Element 2, the vocabulary a real request actually uses, as example trigger phrases: make me a sigil, sigilize this intention, planetary sigil, kamea, sigil for a page. Element 3, the build-context trigger: embedding a sigil into a page or site being built. Element 5, an explicit SKIP clause naming the two adjacent installed skills by name — tarot spreads, natal charts, transits, dream work and general symbolic interpretation belong to `oracle`, and decision framing belongs to `decide`. Element 4, the judgment trigger for choosing which planet suits an intention, is deliberately withheld until the correspondences are ratified in 08-04 — this is D-114's requirement, and the reason is that a description promising reasoning the body cannot yet deliver is worse than a narrower one. Third person throughout. Splitting the trigger phrases into a `when_to_use` key instead of packing them all into `description` is acceptable; either way stay inside the combined 1,536-character cap. Do not cite any activation-rate percentage as a verified figure anywhere in the file or in the commit message — the qualitative guidance is confirmed, the specific numbers are not.

Body sections for this task: an invocation section, a flag table, and a placeholder planet section.

The invocation section, per D-115, documents `npx -y @falkensmage/sigil-spinner@latest` followed by the statement and `--planet`. State why each part is there: `-y` because a non-interactive session otherwise stops on npx's install confirmation, and `@latest` because a dist-tag forces a registry lookup where a bare name could serve a stale resolution from cache — an invisible failure in a tool whose contract is determinism. Then state the shell-safety rule explicitly rather than leaving the clean example to imply it: a real intention statement is free-form prose and can contain a quote character, a backtick, or a command-substitution sequence, so the statement must be single-quoted with any embedded single quote escaped by the standard close-quote-backslash-quote-reopen idiom, or — preferred whenever the command is assembled programmatically — piped in through the CLI's existing single-hyphen stdin sentinel, which sidesteps shell-quoting correctness entirely. Also document the library form as the alternative for build scripts that already carry the dependency. No machine-local filesystem path, and no reference to this repository's checkout location, may appear anywhere under the new directory — D-115 names either as a review failure, because the skill is global and must work on a machine that has never cloned this repo.

The flag table, per D-104, is a markdown table whose first cell in each data row is a backtick-wrapped, double-hyphen-prefixed token. Exactly seven data rows, one per key in the `parseArgs` `options` literal, carrying the flag, its type and whether it is required, and its purpose. This inline table deliberately overrides the earlier research guidance to delegate mechanical detail to a help flag: this CLI has no help flag — an unrecognized option exits with the CLI-local usage diagnostic — so following that guidance literally would point a fresh session at a command that errors. Downstream agents must not delete this table in deference to that guidance — D-104 is the amendment, and plan 08-02's drift check (D-107) is what makes the duplication safe.

The planet section carries only an HTML comment recording that SKILL-02's correspondences are not yet captured from Matt, with no table and no heuristic, per D-114. Plan 08-04 replaces it or leaves it, depending on its gate.

Create `scripts/skill-install.js` per D-99, using only Node builtins — `node:fs`, `node:path`, `node:os`, `node:url`. No new dependency; `dependencies` and `devDependencies` are both untouched. It resolves the repo source root from `import.meta.url` and the destination from `os.homedir()` joined with the fixed segments for the personal skills directory and the skill's own name. The destination is never taken from argv or from an environment variable. It walks the repo source recursively, and runs in two passes so a partial write is impossible: first classify every file as new, identical, or divergent; then act. If any file is divergent, write nothing at all, print every divergent path, and exit non-zero with a message stating that the repo is always the source of truth and that re-running with a force flag will replace the installed copy. With the force flag it writes, naming each path it overwrites — a replacement is never silent. If everything is identical it prints that the install is already up to date and exits zero. Files present at the destination with no counterpart in the repo source are reported as orphans and are never deleted; plan 08-02's install-parity guard (D-101) is where an orphan turns red. Nothing is ever read from the destination back into the repo. Annotate the module with JSDoc types sufficient for the repo's strict `checkJs` pass.

Add exactly one key to `package.json`'s `scripts`, named `skill:install`, invoking the new script with node. Change nothing else in that file — `files`, `exports`, `main`, `bin`, `version`, `dependencies` and `devDependencies` all stay exactly as they are, per Task 2's gate and D-100.

Extend `eslint.config.js`'s first config block `files` glob and `tsconfig.json`'s `include` array to cover the new scripts directory. Without this the new script is silently outside both gates, which is the kind of quiet coverage hole this repo's fail-loud convention exists to prevent. This is the reason the install script is a plain `.js` file rather than a differently-suffixed module: the package is already `type: module`, so a `.js` file there is ESM, and every existing glob and every other source file in the repo uses that suffix.

Finally run the install and the live invocation, so this task's own commit proves the whole path rather than only the files.
  </action>
  <verify>
    <automated>npm run skill:install &amp;&amp; test -f "$HOME/.claude/skills/sigil/SKILL.md" &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; D="$(mktemp -d)" &amp;&amp; ( cd "$D" &amp;&amp; npx -y @falkensmage/sigil-spinner@latest 'I WILL FINISH THIS' --planet saturn ) | head -c 5 | grep -q '&lt;svg' &amp;&amp; echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
- `npm run skill:install` exits 0 and `$HOME/.claude/skills/sigil/SKILL.md` exists and is byte-identical to `skill/SKILL.md` (`cmp -s skill/SKILL.md "$HOME/.claude/skills/sigil/SKILL.md"` exits 0).
- Running `npm run skill:install` a second time with no intervening edit exits 0 and reports the install already up to date, writing nothing.
- After appending a single character to `$HOME/.claude/skills/sigil/SKILL.md`, `npm run skill:install` exits non-zero, names that path in its output, and leaves the file unmodified; re-running as `npm run skill:install -- --force` exits 0, names the path it overwrote, and restores byte-identity. (Restore state with a final forced install.)
- `head -1 skill/SKILL.md` is `---` and `grep -c '^name: sigil$' skill/SKILL.md` is 1.
- `skill/SKILL.md` frontmatter declares an `allowed-tools` value covering all six of `Bash`, `Read`, `Write`, `Edit`, `Glob`, `Grep` (D-103).
- The set of double-hyphen tokens in the first cell of `skill/SKILL.md`'s flag-table rows, extracted with `grep -oE '^\| `--[a-z-]+`' skill/SKILL.md | grep -oE '\-\-[a-z-]+' | sort`, is exactly: `--curve`, `--glyph`, `--id-prefix`, `--json`, `--output`, `--planet`, `--title` — seven tokens, matching the seven keys of the `parseArgs` `options` literal in `bin/sigil-spinner.js`.
- `skill/SKILL.md` contains an HTML comment recording that the correspondences are not yet captured, and contains no seven-row planet table (D-114). The description carries D-102 elements 1, 2, 3 and 5 and does not claim planet-selection judgment.
- `grep -rEl 'RitualSync|/Users/' skill/ scripts/` produces no output. <!-- planner-discipline-allow: RitualSync --> <!-- planner-discipline-allow: /Users/ -->
- `grep -c 'sigil-spinner@latest' skill/SKILL.md` is at least 1 and `grep -c "npx -y @falkensmage/sigil-spinner@latest" skill/SKILL.md` is at least 1.
- `node -e "const p=require('./package.json'); if(p.scripts['skill:install']===undefined) process.exit(1); if(JSON.stringify(p.files)!==JSON.stringify(['src','bin','README.md'])) process.exit(1); console.log('package.json boundary intact')"` exits 0.
- `npm pack --dry-run --json 2>/dev/null | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const f=JSON.parse(s)[0].files.map(x=>x.path);const bad=f.filter(p=>p.split("/")[0]==="skill"||p.split("/")[0]==="scripts");if(bad.length){console.error("repo-only paths in tarball: "+bad.join(", "));process.exit(1)}console.log("tarball roots clean, "+f.length+" files")})'` exits 0.
- `git status --porcelain test/pack-install.test.js` produces no output — D-100's no-edit boundary held, and `EXPECTED_TARBALL_FILES` and `ALLOWED_TARBALL_ROOTS` are untouched.
- `npm run lint` exits 0 and `npm run typecheck` exits 0, both with `scripts/skill-install.js` inside their globs (confirm with `npx eslint --debug scripts/skill-install.js 2>&1 | tail -1` exiting 0, and by `tsconfig.json`'s `include` array containing a `scripts` entry).
- From a directory created by `mktemp -d`, the exact invocation string documented in `skill/SKILL.md` exits 0 and writes output beginning with an `<svg` element to stdout.
  </acceptance_criteria>
  <done>`skill/SKILL.md` exists with correct frontmatter, a seven-row flag table matching the CLI's real option keys, an invocation recipe carrying the shell-safety rule and no machine-local path; `npm run skill:install` installs it one-directionally and refuses a divergent destination without `--force`; the tarball still carries no repo-only directory; and the documented invocation runs for real against the registry from outside the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intention statement → shell command | Free-form prose, potentially copied from an untrusted source at the user's request, is interpolated into a command a Claude Code session executes via Bash. This is the phase's only genuine untrusted-input crossing. |
| repository → home directory | `scripts/skill-install.js` is the only thing in this project that writes outside the repository tree. |
| skill frontmatter → session tool permissions | `allowed-tools` pre-approves tool use for the turn that invokes the skill. |
| public npm registry → cold machine | The documented invocation fetches and executes a package on a machine that has never seen this repo, with confirmation auto-answered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | The invocation recipe in `skill/SKILL.md` | high | mitigate | The recipe states the safe form explicitly rather than letting a punctuation-free example imply it: single-quote the statement with the standard embedded-single-quote escape, or pipe it through the CLI's existing stdin sentinel — which removes shell quoting from the path entirely and is the documented preference when the command is assembled programmatically. Verified by an acceptance criterion asserting the stdin-sentinel alternative is present. |
| T-08-02 | Tampering | `scripts/skill-install.js` destination resolution | medium | mitigate | The destination is built from `os.homedir()` plus fixed literal path segments and is never taken from argv or the environment, so there is no caller-controlled component to traverse with. The walk is rooted at the repo's own skill directory. Writes are two-pass, so a divergence discovered mid-tree cannot leave a partially-written destination. |
| T-08-03 | Tampering | `scripts/skill-install.js` overwrite behavior | medium | mitigate | A destination file differing from its repo counterpart aborts the entire install with the path named and exit non-zero; replacement requires an explicit force flag and names every path it overwrites. Files at the destination with no repo counterpart are reported and never deleted. Data flows in one direction only — nothing at the destination is ever read back into the repo. |
| T-08-04 | Elevation of Privilege | `allowed-tools` in `skill/SKILL.md` frontmatter | low | mitigate | Declare exactly the six tools the documented workflows need and no more. The field is a same-turn pre-approval that clears on the next message, not a sandbox, so the risk direction is over-granting convenience rather than under-granting failure. `disallowed-tools` — the actual restriction mechanism — is not needed here. |
| T-08-05 | Repudiation | The skill's stated planet reasoning | low | accept | Deferred to 08-04, where the attribution line and capture date make a ratified row distinguishable from a drifted one. Accepted here because this plan ships no correspondence content at all. |
| T-08-SC | Tampering | The registry fetch performed by the documented `npx` invocation | low | accept | No package is installed by this repository during this phase — `dependencies` and `devDependencies` are untouched, so the package-legitimacy gate does not apply. The runtime fetch the skill instructs is of this project's own scoped package, published in Phase 6 with a verified provenance attestation, and the skill documents the fully scoped name exactly, which is the practical typosquat mitigation. Accepted with the attestation as the transfer. |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck` and `npm test` all exit 0 on the working tree.
- `npm run skill:install` is idempotent on a clean tree and fails loudly on a divergent destination.
- `npm pack --dry-run --json` shows no repo-only top-level directory in the tarball, and `test/pack-install.test.js` is unmodified.
- The exact invocation string in `skill/SKILL.md` produces an SVG from a `mktemp -d` directory against the live registry.
</verification>

<success_criteria>
The runtime path of ROADMAP success criterion 3 is proven end to end on one slice: repo-canonical source, one-directional install, and a documented invocation that works against the published package from a machine location with no relationship to this repository. The two reversibility gates are resolved and recorded, and the phase's published-contract boundary is mechanically confirmed rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/08-the-sigil-skill/08-01-SUMMARY.md` when done. Record the resolution of both checkpoints verbatim, the resolved registry version observed at the precondition check, and the exact invocation string committed to `skill/SKILL.md`.
</output>
