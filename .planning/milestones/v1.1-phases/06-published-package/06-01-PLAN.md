---
phase: 06-published-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - LICENSE
  - README.md
  - test/package-identity.test.js
autonomous: false
requirements: [PKG-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "package.json declares name @falkensmage/sigil-spinner, version 1.0.0, license MIT, author Matt Stine (https://github.com/mstine), a repository object, homepage, bugs.url, and publishConfig.access public (D-62, D-63, D-66, D-67, D-68, D-69)"
    - "A root LICENSE file exists carrying the MIT text with the copyright line Copyright (c) 2026 Matt Stine, and package.json's license field says MIT — the two agree (D-66)"
    - "package.json repository.url equals git+https://github.com/mstine/sigil-spinner.git and matches git remote get-url origin byte-exactly, case-sensitive on owner and repo name, after normalizing only the git@github.com: to git+https://github.com/ transport prefix (D-67; edge PKG-04/encoding)"
    - "Every metadata field this plan writes is present and non-empty in package.json — name, version, license, author, repository.url, homepage, bugs.url, publishConfig.access — with no empty string, no null, and no absent key (edge PKG-04/empty)"
    - "README.md documents the install command and the library import specifier using the scoped package name, and a mechanical guard fails when either drifts from package.json's name (D-64, D-65)"
    - "README.md discloses in its opening section that the package is ESM-only (D-78)"
    - "The bin command name stays sigil-spinner — the scope is not applied to the bin key (D-63)"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text shipped at the tarball root"
      contains: "Copyright (c) 2026 Matt Stine"
      min_lines: 18
    - path: "package.json"
      provides: "Complete publication metadata (PKG-04)"
      contains: "@falkensmage/sigil-spinner"
    - path: "test/package-identity.test.js"
      provides: "D-65 mechanical guard binding README's documented specifier to package.json's name"
      min_lines: 25
    - path: "README.md"
      provides: "Scoped install/import specifier and ESM-only disclosure for the npm package page"
  key_links:
    - from: "test/package-identity.test.js"
      to: "package.json"
      via: "reads name from package.json rather than restating it — guards are keyed, not transcribed"
      pattern: "package\\.json"
    - from: "test/package-identity.test.js"
      to: "README.md"
      via: "regex-extracts the documented install command and import specifier and compares both to package.json's name"
      pattern: "README\\.md"
    - from: "package.json"
      to: "LICENSE"
      via: "license field value must match the license text actually shipped"
      pattern: "\"license\""
  prohibitions:
    - statement: "MUST NOT add an email address or any personal contact detail beyond the name and the public GitHub profile URL to author, bugs, or LICENSE — the npm author field is scraped, and D-68 deliberately withholds an email from it"
      category: privacy
      requirement_id: PKG-04
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the license the npm page declares disagree with the license text actually shipped in the tarball — a page saying MIT over a file saying anything else misrepresents the terms to every consumer who reads only the page"
      category: transparency
      requirement_id: PKG-04
      status: resolved
      verification: judgment
---

<objective>
Make the package's publication metadata complete and correct, and bind the README's
documented identity to it mechanically — before any version number becomes permanent.

This is PKG-04, and it is the plan that walks through five one-way doors at once: the
version number, the package name, the license, the repository URL, and the author string.
None of the five was confirmed by Matt in conversation — `06-CONTEXT.md` records that
`--auto` mode selected all of them. Task 1 is where that gets reviewed, because after the
publish in plan 06-03 the correction for any of them is a version bump, not an edit.

Purpose: the smoke test in plan 06-02 asserts a tarball manifest that includes `LICENSE`,
and asserts resolution through the *scoped* specifier. Both facts have to be true before
the tracer can be written against them.

Output: corrected `package.json`, a root `LICENSE`, a README pass scoped to what the npm
package page needs, and `test/package-identity.test.js` as a drift guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-published-package/06-CONTEXT.md
@.planning/phases/06-published-package/06-RESEARCH.md
@.planning/phases/06-published-package/06-PATTERNS.md
</context>

## Artifacts this phase produces

Phase-wide list of every symbol Phase 6 creates. The plan-review-convergence
source-grounding pass reads this section to exclude newly-created symbols from drift
verification — none of the names below exists in the codebase before this phase.

**New files**

| Path | Created by |
|------|------------|
| `LICENSE` | 06-01 |
| `test/package-identity.test.js` | 06-01 |
| `test/pack-install.test.js` | 06-02 |
| `.github/workflows/release.yml` | 06-02 |
| `.planning/phases/06-published-package/06-RELEASE-LOG.md` | 06-03 |

**New `package.json` keys**

`repository`, `repository.type`, `repository.url`, `homepage`, `bugs`, `bugs.url`,
`publishConfig`, `publishConfig.access`, `scripts.test:pack`

**Changed `package.json` values** (keys already exist)

`name`, `version`, `license`, `author`

**New `vitest.config.js` keys**

`test.exclude`

**New npm scripts**

`test:pack`

**New workflow inputs** (`.github/workflows/release.yml`, `workflow_dispatch`)

`mode`, `dist_tag`

**New module-level constants in new test files**

`ENTRY_POINTS`, `EXPECTED_TARBALL_FILES`, `ALLOWED_TARBALL_ROOTS`, `STATEMENT`, `PLANET`,
`REPO_ROOT`

## Edge Probe Disposition (spec-less fallback)

Phase 6 has no SPEC.md, so `## Edge Coverage` and `## Prohibitions` are absent and the
fallback protocol applies. The deterministic edge probe returned 8 rows, all
`unresolved`. This table is the authoritative disposition for all 8 — plans 06-02 through
06-04 restate only their own rows and point here.

**No-silent-drop equality: 8 probe-surfaced rows == 6 authored into `must_haves.truths`
(verification: explicit) + 2 surfaced as flagged assumptions. Zero dropped.**

| # | Req | Category | Disposition | One-line judgment |
|---|-----|----------|-------------|-------------------|
| 1 | PKG-01 | unclassified | **flagged assumption** | No data-shape edge applies. "Publish to a registry" has no input domain; PKG-01's real boundary condition is irreversibility (72h conditional unpublish, 24h name lock, versions never reusable), which the rehearsal ladder and the 06-03 decision checkpoint cover — not an input-edge criterion. Recorded in 06-03, never auto-resolved with a backstop. |
| 2 | PKG-03 | concurrency | **promoted → explicit truth** (06-02) | Genuinely applicable: the smoke test packs a tarball and installs it. `mkdtempSync` + `--pack-destination` make the answer concrete and assertable. |
| 3 | PKG-04 | empty | **promoted → explicit truth** (06-01) | Genuinely applicable: `author: ""` is the *current, documented* defect (PITFALLS.md § Pitfall 3), and an absent `repository` key is the null case. The empty/absent-field question is exactly the failure this requirement exists to close. |
| 4 | PKG-04 | encoding | **promoted → explicit truth** (06-01) | Genuinely applicable and the sharpest row in the report: npm matches `repository.url` **case-sensitively** for provenance, with a real Sigstore 422 failure on record (06-RESEARCH.md § Pitfall C). "Whose definition of equality applies" is precisely the right question; the answer is byte-exact including case. |
| 5 | PKG-04 | concurrency | **flagged assumption** | Classification miss. Editing a JSON manifest in a single-developer repo has no concurrency dimension — there is no interleaved writer, no partial-write recovery path, and no parallel invocation. No criterion minted; recorded here rather than dropped. |
| 6 | PKG-05 | adjacency | **promoted → explicit truth** (06-04) | Applicable under a real reading: after the promote, `next` and `latest` both point at `1.0.0`. Do they merge, collide, or coexist? They coexist — and asserting that is how we prove `dist-tag add` did not republish. |
| 7 | PKG-05 | empty | **promoted → explicit truth** (06-02) | Applicable and load-bearing: an omitted `workflow_dispatch` input that resolves to an empty string, interpolated into a `run:` command in a job holding a publish token, is threat T-06-02. Closed-option `choice` inputs with declared defaults answer it. |
| 8 | PKG-05 | ordering | **promoted → explicit truth** (06-02) | Applicable under a real reading: D-77 fixes a six-step quality gate whose *order* is the requirement. "Is output order specified and stable" maps onto "is the publish step reachable before the gates pass." |

### Flagged assumptions carried by this plan

- **PKG-04 / concurrency (row 5)** — assumed not applicable. If `package.json` ever gains a
  concurrent writer (a codegen step, a version-bump bot), reopen this row.

## Prohibition Probe Disposition (spec-less fallback)

Two-stage recall→precision run in-prompt over PKG-04. Stage 1 over-produced ~9 candidates;
Stage 2 kept 2 (authored above in `must_haves.prohibitions`, descriptor-less, disposing
flagged-unverified) and dropped the rest as routine engineering.

Canon-referral drops (breadcrumbs, not minted here):

- Secret-in-logs / CI credential exfiltration is canon — covered by `/gsd-secure-phase` and
  the `<threat_model>` block in 06-02; not minted as a prohibition.
- Workflow-input command injection is canon (injection) — covered by `/gsd-secure-phase` and
  threat `T-06-02` in 06-02; not minted as a prohibition.

<assumption_delta_decision>
**Primary noun:** configuration (as in "additional configuration")
**Decision:** `no-change`
**Rationale:** The detector fired on a `pluralization` signal for the term "additional" in
the ROADMAP success-criterion sentence "neither needs additional configuration." That is a
lexical match on a fixed English phrase, not a singular→plural transition in the identity
model — Phase 6 introduces no entity whose cardinality changed, and "configuration" is used
as a mass noun there, not as a countable set that grew. No plan structure changes. Advisory
checkpoint; does not block.
</assumption_delta_decision>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Freeze the one-way identity and metadata block (D-62, D-63, D-66, D-67, D-68)</name>
  <decision>Confirm the five permanent metadata values before they are written, and before plan 06-03 makes them irreversible.</decision>
  <context>
`06-CONTEXT.md` opens by recording that Phase 6 ran in `--auto` mode: **no decision below
was confirmed by Matt in conversation.** All five are rated one-way. Each is trivially
editable right now and permanently wrong after `npm publish` succeeds — the correction is a
version bump, not an edit, and a published version number can never be reused even after an
unpublish.

The five values about to be written:

| Field | Value | Why undo is not an edit |
|-------|-------|-------------------------|
| `version` (D-62) | `1.0.0` | A published version number is never reusable. Correcting it means publishing a second version and abandoning the first. Rejected alternative: `1.1.0` to match the GSD milestone label — the registry's semver line describes the public API surface, not milestone numbering. |
| `name` (D-63) | `@falkensmage/sigil-spinner` | The package name is the registry identity. A rename means publishing a second package and deprecating the first. The `bin` command stays `sigil-spinner` — unscoped — so `npx sigil-spinner ...` keeps working. |
| `license` (D-66) | `MIT` | Cannot be retroactively narrowed for anyone who already installed the version it shipped under. Current value `ISC` is a scaffold default that contradicts PROJECT.md. |
| `repository.url` (D-67) | `git+https://github.com/mstine/sigil-spinner.git` | Matched **case-sensitively** by npm's provenance check. A mismatch fails the attestation with a Sigstore 422 and the fix is a version bump. |
| `author` (D-68) | `Matt Stine (https://github.com/mstine)` | Scraped by npm and permanent per published version. Name and profile URL only, no email address. `06-CONTEXT.md` names this as **the one field Matt may most want to override** before the publish. |

Also being written, and reversible (no confirmation needed): `homepage`,
`bugs.url`, `publishConfig.access` (D-69).
  </context>
  <options>
    <option id="approve-all">
      <name>Approve all five as specified</name>
      <pros>Matches D-62 through D-68 exactly; unblocks the rest of the phase immediately; every value was researched against the live git remote and live npm documentation.</pros>
      <cons>Locks `author` and `1.0.0` permanently on a set of choices `--auto` made rather than Matt.</cons>
    </option>
    <option id="amend">
      <name>Amend one or more values before they are written</name>
      <pros>The only moment amendment is free. `author` and `version` in particular cost nothing to change now and cannot be changed after 06-03.</pros>
      <cons>Any change to `name` or `repository.url` ripples into the README sweep, the smoke test's entry-point table, and the release workflow's promote command — all of which key off the name.</cons>
    </option>
  </options>
  <resume-signal>Reply "approve-all", or "amend" followed by the field and the value you want instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the metadata, the LICENSE, the README npm pass, and the identity guard</name>
  <reversibility rating="one-way">Publishing under this name, version, license, repository URL, and author fixes all five permanently for version 1.0.0; the correction path is a new version, not an edit.</reversibility>
  <files>package.json, LICENSE, README.md, test/package-identity.test.js</files>
  <read_first>
    - `package.json` — the file being edited; read it in full so the edit is a targeted key insertion and not a rewrite. `description`, `keywords`, `engines`, `files`, `exports`, `bin`, `type`, `scripts`, `devDependencies` are already correct per D-69 and must be left byte-unchanged.
    - `README.md` — the file being edited. Read at least the first 60 lines (the intro through `### CLI`) plus every line matching `sigil-spinner`, so the sweep in D-64 is a sweep and not a single-line fix.
    - `test/citations.test.js` — the in-repo precedent for a mechanical drift guard that reads a shipped artifact and fails when a documented claim stops resolving (MAINT-01). Read the `describe`/`it` structure and the `node:fs`/`node:url`/`node:path` import block; imitate the shape, not the R1/R2 machinery — this guard is one string comparison wide.
    - `test/e2e/phase2-tracer.test.js` — the repo's `fileURLToPath(import.meta.url)` path-resolution idiom, lines 1-16. Note the relative depth: that file is in `test/e2e/` and uses `'..', '..'`; the new file is in `test/` and needs one `'..'`.
    - `.planning/phases/06-published-package/06-CONTEXT.md` § Publication metadata (D-66 through D-69) and § README npm pass (D-78) — the authoritative decision text.
    - `.planning/phases/06-published-package/06-RESEARCH.md` § Code Examples § `package.json` diff — the worked target block, and § Pitfall C for the case-sensitivity requirement on `repository.url`.
  </read_first>
  <action>
Edit `package.json` in place. Change `name` from `sigil-spinner` to `@falkensmage/sigil-spinner`
(D-63). Change `version` from `0.1.0` to `1.0.0` (D-62). Change `license` from `ISC` to `MIT`
(D-66). Change `author` from the empty string to `Matt Stine (https://github.com/mstine)` (D-68).
Add four new keys (D-69): `repository` as an object with `type` set to `git` and `url` set to
`git+https://github.com/mstine/sigil-spinner.git` (D-67); `homepage` set to
`https://github.com/mstine/sigil-spinner`; `bugs` as an object whose `url` is
`https://github.com/mstine/sigil-spinner/issues`; and `publishConfig` as an object whose
`access` is `public`. Leave the `bin` key exactly as it is — the command name stays unscoped
(D-63). Leave `description`, `keywords`, `engines`, `files`, `exports`, `type`, `scripts`, and
`devDependencies` untouched (D-69); `dependencies` stays absent-or-empty and no dependency of
any kind is added by this phase.

Before finishing the `repository.url` edit, verify the case-sensitivity requirement rather than
assuming it: run `git remote get-url origin`, normalize the `git@github.com:` prefix to
`git+https://github.com/` and append `.git` if absent, and confirm the result is byte-identical
to the value written — including the case of both the owner segment and the repo segment. npm
matches this field case-sensitively when minting a provenance attestation and rejects a mismatch
with a Sigstore 422 (06-RESEARCH.md § Pitfall C).

Create `LICENSE` at the repository root containing the standard, unmodified OSI MIT License
text, with the copyright line reading `Copyright (c) 2026 Matt Stine` (D-66). Do not add a
file extension — npm auto-includes a root-level file named exactly `LICENSE` in the tarball
without it being listed in `files`, which is why the smoke test in 06-02 asserts it shipped
rather than assuming it did.

Edit `README.md` for the npm package page (D-78), scoped to two changes and no other
restructuring. First, sweep every bare module specifier: grep the whole file for
`sigil-spinner` and change only the ones that are ESM module specifiers to the scoped name
(D-64). The known site is the library example in the `### Library` section around line 15.
The occurrences in the `### CLI` usage line, the stdin example, and the `bin/sigil-spinner.js`
path reference are the **bin command name and a repo file path**, not module specifiers —
leave all three alone. Relative imports under `src/`, `bin/`, and `test/` never resolve
through `exports` and are explicitly out of scope. Second, add a short `## Installation`
section between the opening description paragraph and the existing `## Usage` heading. It
carries the `npm install` command using the scoped name, and an unmissable one-or-two-sentence
disclosure that the package is ESM-only: it ships no CommonJS build, a synchronous
`require()` of it throws `ERR_REQUIRE_ESM`, and the consuming file must be ESM (`.mjs`, or a
package with `"type": "module"`). Phrase it as a plain declarative fact in the README's
existing direct technical register — this is a deliberate v1.0 choice being documented, not
an apology for a limitation. Both edits land in the same commit as the `package.json` name
change (D-64).

Create `test/package-identity.test.js` as the mechanical guard (D-65). It reads
`package.json` and `README.md` from disk with `node:fs`, resolving the repo root via the
`fileURLToPath(import.meta.url)` idiom with a single `'..'` segment. It parses the package
name out of `package.json` — never restating it as a literal, because guards are keyed from
the source of truth, not transcribed (the D-55 and D-61 precedent). It then regex-extracts
two things from the README: the argument of the documented `npm install` command, and the
module specifier in the documented library `import` example. Both must equal the parsed
package name. Assert each separately with a failure message naming which one drifted. Follow
the analog's `describe`/`it` shape with the requirement ID in the `describe` title and one
behavior per `it`; keep the file free of the citations checker's window/label machinery,
which solves a much harder N-to-M problem this guard does not have. This file stays in the
default `npm test` include on purpose — a guard that only runs at publish time cannot fail on
whoever introduces the drift.
  </action>
  <verify>
    <automated>npx vitest run test/package-identity.test.js &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').name"` prints `@falkensmage/sigil-spinner`
    - `node -p "require('./package.json').version"` prints `1.0.0`
    - `node -p "require('./package.json').license"` prints `MIT`
    - `node -p "require('./package.json').author"` prints `Matt Stine (https://github.com/mstine)`
    - `node -p "require('./package.json').repository.url"` prints `git+https://github.com/mstine/sigil-spinner.git`
    - `node -p "require('./package.json').publishConfig.access"` prints `public`
    - `node -p "require('./package.json').homepage"` prints `https://github.com/mstine/sigil-spinner`
    - `node -p "require('./package.json').bugs.url"` prints `https://github.com/mstine/sigil-spinner/issues`
    - `node -p "Object.keys(require('./package.json').bin)[0]"` prints `sigil-spinner` (the bin command name is not scoped)
    - `node -e "const p=require('./package.json');const f=['name','version','license','author','homepage'];for(const k of f){if(typeof p[k]!=='string'||p[k].trim()==='')throw new Error(k)};for(const k of ['repository.url','bugs.url','publishConfig.access']){const v=k.split('.').reduce((o,s)=>o&amp;&amp;o[s],p);if(typeof v!=='string'||v.trim()==='')throw new Error(k)};console.log('ok')"` prints `ok` — no metadata field this plan writes is empty, null, or absent (edge PKG-04/empty)
    - `node -e "const {execSync}=require('node:child_process');const u=execSync('git remote get-url origin').toString().trim();const n=u.replace(/^git@github\.com:/,'git+https://github.com/').replace(/(\.git)?$/,'.git');const p=require('./package.json').repository.url;if(n!==p)throw new Error(n+' !== '+p);console.log('ok')"` prints `ok` — byte-exact, case-sensitive match against the live remote (edge PKG-04/encoding)
    - `test -f LICENSE` succeeds and `grep -c 'Copyright (c) 2026 Matt Stine' LICENSE` returns `1`
    - `grep -c 'MIT License' LICENSE` returns at least `1`, and `node -p "require('./package.json').license"` prints the same license identifier the file's title names
    - `grep -c '@falkensmage/sigil-spinner' README.md` returns at least `2` (the install command and the library import example)
    - `npx vitest run test/package-identity.test.js` exits 0 and reports at least 2 passing assertions
    - `npx vitest run test/package-identity.test.js` fails when `package.json`'s `name` is temporarily changed to any other value — confirm this fail-first behavior once by hand and restore before committing
    - `npm test` exits 0 with `test/package-identity.test.js` included in the run
    - `npm run lint` exits 0 and `npm run typecheck` exits 0
    - `git status --porcelain` shows `package.json`, `README.md`, `LICENSE`, and `test/package-identity.test.js` staged together in one commit (D-64 requires the README specifier and the `package.json` name to move in the same commit)
  </acceptance_criteria>
  <done>`package.json` carries the complete PKG-04 metadata set with a byte-exact, case-matched `repository.url`; a root `LICENSE` ships the MIT text under Matt's copyright; the README documents the scoped install and import specifier plus the ESM-only disclosure; and `test/package-identity.test.js` fails the default suite the moment the README and the manifest disagree about the package name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → npm registry | Metadata written here becomes a permanent, public claim about identity, ownership, and license terms the moment 06-03 publishes. |
| repo → npm provenance check (Sigstore) | `repository.url` is read by the registry and compared case-sensitively against the workflow's own repository; it gates whether an attestation can be minted at all. |
| repo → public tarball | `files` plus npm's auto-inclusions determine what leaves this machine. Nothing in this plan widens that set; the assertion that it did not widen is in 06-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Spoofing | `package.json` `repository.url` | high | mitigate | Byte-exact, case-sensitive comparison against `git remote get-url origin` asserted in Task 2's acceptance criteria. A wrong or wrong-cased value silently detaches the attestation from the real repo, or fails it with a Sigstore 422 — both after the version is permanent. |
| T-06-05 | Information Disclosure | `author`, `bugs.url`, `LICENSE` | medium | mitigate | D-68 fixes `author` to name plus public GitHub URL with no email; npm scrapes this field. The prohibition in `must_haves.prohibitions` records the constraint so a later "helpful" addition of a contact address is a visible violation rather than a silent one. |
| T-06-06 | Repudiation | `LICENSE` vs `package.json` `license` | medium | mitigate | Task 2 asserts the shipped file and the declared identifier agree. A page claiming MIT over a file saying otherwise misstates the terms to anyone who reads only the npm page. |
| T-06-07 | Tampering | `package.json` `publishConfig.access` | high | mitigate | Set to `public` here (the belt); `--access public` on the publish command in 06-03 is the suspenders. PITFALLS.md § Pitfall 1 records tooling honoring the flag while ignoring `publishConfig` on a scoped package's **first** publish, and this is exactly that case — npm defaults scoped packages to private. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase. `dependencies` stays empty and no `devDependencies` entry is added, so there is no third-party supply-chain surface to audit — recorded as not-applicable in 06-RESEARCH.md § Package Legitimacy Audit rather than skipped silently. |
</threat_model>

<verification>
- `npm test` exits 0 (the full suite, now including `test/package-identity.test.js`)
- `npm run lint` and `npm run typecheck` both exit 0
- `npm pack --dry-run --json` lists `LICENSE` among the tarball files (the manifest assertion itself lands in 06-02; this is the eyeball check that the file will actually ship)
- `node -p "require('./package.json').dependencies || {}"` prints an empty object — the milestone-wide zero-runtime-dependency constraint is intact
- No file under `src/` was modified — `git diff --name-only HEAD~1 -- src/` is empty
</verification>

<success_criteria>
- All eight PKG-04 metadata assertions in Task 2's acceptance criteria pass
- `repository.url` matches the live remote byte-for-byte, case included
- The README documents the scoped install command and the scoped import specifier, and discloses ESM-only in its opening section
- `test/package-identity.test.js` runs in the default suite and has been observed to fail when the name is perturbed
- The `package.json` name change and the README specifier change are in one commit (D-64)
</success_criteria>

<output>
Create `.planning/phases/06-published-package/06-01-SUMMARY.md` when done
</output>
