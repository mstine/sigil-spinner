---
phase: quick-260812-n36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/SKILL.md
  - skill/references/correspondences.md
autonomous: true
requirements: [SKILL-MODERN-01, SKILL-ATTR-01, SKILL-VER-01]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`skill/SKILL.md`'s `--planet` flag row states the planet list as VERSION-DEPENDENT (D-03): the classical seven work on every published version, the modern three require a version later than 1.1.0 — a sentence that is true today against the live `@latest` and remains true after the next publish, so the version-skew hazard is eliminated rather than deferred to a release-day memory item."
    - "The Planet Selection table in `skill/SKILL.md` carries TEN rows — uranus, neptune and pluto present as peers of the classical seven, with no tiebreak, no ranking, and no hedge marking them as lesser options (D-01)."
    - "The three domain rows are substantively identical to CONTEXT.md § 'The three rows — final text, ratified and enriched' (D-02). Wording may be reflowed to fit the table; substance may not change. Pluto carries taboo; Neptune states diffusion plainly and admits the fog WITHOUT a both-poles construction; Uranus carries barriers and social justice."
    - "The Selection heuristic in `skill/SKILL.md` covers Pluto vs. Saturn, Neptune vs. Moon, and Uranus vs. Mars, verbatim in substance from CONTEXT.md § 'The three new ambiguity axes', alongside the pre-existing subject-vs-verb rule."
    - "`skill/SKILL.md`'s Published-Surface Boundary section records that the modern three require a version later than the currently-published 1.1.0."
    - "`skill/references/correspondences.md` records the 2026-08-12 round as its own dated provenance block, and a future reader can tell its provenance SHAPE apart from the 2026-08-09 round's: 2026-08-09 corrected/confirmed every row against Matt's judgment BEFORE writing; 2026-08-12 drafted, then ratified, THEN corpus-checked. The two rounds are not flattened into one undifferentiated 'ratified by Matt' claim."
    - "`skill/references/correspondences.md` names the corpus consulted on 2026-08-12 — which sources yielded material (Celestial Alchemy outer-planets and outer-planet-retrogrades, the Eighth House substack) and which yielded none."
    - "Farella's absence is recorded as a DELIBERATE METHODOLOGICAL STANCE — she does not work with the outer planets — and explicitly not as a gap, a limitation, or material to be filled in later."
    - "The generational tension (Celestial Alchemy classifies all three as generational; a sigil is a personal working) is recorded as a known, ACCEPTED trade-off attributed to Matt's 2026-08-12 decision. No tiebreak is added anywhere in either file as a consequence of recording it."
    - "`skill/references/correspondences.md`'s opening line states the inline table's row count as ten."
    - "This change is docs-only: `git status --porcelain -- src bin test` reports nothing. Zero snapshot movement, zero library behaviour change."
    - "`~/.claude/skills/sigil/` is byte-identical to `skill/` after the change, so `test/skill-install-parity.test.js` is green rather than red-on-a-stale-install."
  artifacts:
    - skill/SKILL.md
    - skill/references/correspondences.md
  key_links:
    - "`skill/SKILL.md` flag table ↔ `bin/sigil-spinner.js` option keys, bound by `test/skill-cli-parity.test.js`. The `--planet` row must stay a SINGLE table row still matching that guard's `^\\|\\s*`--([a-z-]+)`\\s*\\|` anchor — a multi-line rewrite breaks the guard."
    - "`skill/` ↔ `~/.claude/skills/sigil/`, bound byte-for-byte by `test/skill-install-parity.test.js`. Any edit under `skill/` requires `npm run skill:install -- --force` before the suite is green."
    - "CONTEXT.md's ratified rows and axes ↔ the text that lands in `skill/SKILL.md`. CONTEXT.md is the authority; the plan does not restate the rows, so there is exactly one place to copy from and no transcription fork."
    - "`src/data/kamea.js`'s `PLANET_ATTESTATION` ↔ which planets are non-traditional. It is the source of truth for the `traditional: false` set (uranus, neptune, pluto) that this documentation change describes."
---

<objective>
Close the one item quick task 260812-m4b deliberately deferred: teach `skill/SKILL.md` the three non-traditional planets, and extend `skill/references/correspondences.md`'s attribution record so the new rows' provenance is as honest as the classical seven's.

m4b added uranus, neptune and pluto as first-class planets to the library, CLI, README, examples and `skill/VERIFY.md`, but left `skill/SKILL.md` alone because the skill instructs sessions to invoke `@latest`, which resolves to the published 1.1.0 — a version that rejects those three with `E_UNKNOWN_PLANET`. That reasoning was sound; the conclusion was too weak. The fix does not need a release: **state the planet list as version-dependent** and the statement is true both before and after the next publish.

Purpose: the attribution work is the point of this task, not a footnote. `correspondences.md` exists specifically to make the provenance of the correspondence table honest, and it explicitly dates the line between "ratified" and "possibly drifted". The 2026-08-12 rows have a different provenance shape from the 2026-08-09 rows, and a future reader must be able to see that.

Output: two edited markdown files under `skill/`, a re-synced personal install, and a green suite with zero snapshot movement.

**Decision mapping.** CONTEXT.md numbers its ratified decisions 1–3 under § "Decisions (all confirmed by Matt this session, 2026-08-12)". This plan cites them as **D-01** (full rows, sessions may choose them, no tiebreak), **D-02** (the three domain rows are ratified text), **D-03** (version-aware flag table). All three are LOCKED — do not revisit, and in particular do not add a tiebreak ranking the modern three below the classical seven and do not reopen the generational objection.

**Why no tracer task.** Tracer-first proves an architecture end-to-end before expanding. There is no architecture here: this is a documentation sync across two markdown files with zero source, zero behaviour, and zero new layers. A thin vertical slice would prove nothing that Task 1's greps do not. Task 1 is `auto`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260812-n36-sync-the-sigil-skill-with-the-three-non-/260812-n36-CONTEXT.md
@skill/SKILL.md
@skill/references/correspondences.md
@.claude/CLAUDE.md

Read only if a task needs them; do not re-read what is already above:
- `skill/VERIFY.md` — ALREADY amended by 260812-m4b (its error-output block now shows the classical/modern split). Read as landed. Do not duplicate, do not revert, do not edit it in this task.
- `src/data/kamea.js` — `PLANET_ATTESTATION` (around line 197) is the source of truth for which planets carry `traditional: false`. Read only to confirm the set; do not edit.
- `test/skill-cli-parity.test.js` — the flag-table drift guard. Note line 56 says "all seven current options": that is seven **CLI flags** (`planet`, `json`, `output`, `glyph`, `curve`, `id-prefix`, `title`), not seven planets. It is unrelated to this change and MUST NOT be touched.
- `test/skill-install-parity.test.js` — the repo↔install byte-identity guard.

**CONTEXT.md is the single source for all ratified text.** This plan deliberately does not restate the three domain rows or the three ambiguity axes, so there is exactly one place to copy them from and no second copy that can drift.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teach SKILL.md the three modern planets, version-aware</name>
  <files>skill/SKILL.md</files>
  <read_first>
    `.planning/quick/260812-n36-sync-the-sigil-skill-with-the-three-non-/260812-n36-CONTEXT.md` — specifically § "Why this was deferred, and why the deferral is now void" (D-03), § "The three rows — final text, ratified and enriched" (D-02), and § "The three new ambiguity axes". Those two latter sections are RATIFIED TEXT: copy them, do not author replacements.
  </read_first>
  <precondition>`npm view @falkensmage/sigil-spinner version` returns `1.1.0`. Confirmed live at planning time on 2026-08-12. If it returns anything else, a release landed since planning and the "later than 1.1.0" wording needs different numbers — HALT and report rather than guessing which version now gates the modern three.</precondition>
  <action>
    Four edits to `skill/SKILL.md`, in file order.

    (1) **Flag table, the `--planet` row (currently line 63).** Per D-03, rewrite the Purpose cell so the planet list is version-dependent rather than a flat count. It must convey: the classical seven — saturn, jupiter, mars, sun, venus, mercury, moon — are valid on every published version; uranus, neptune and pluto require a version later than 1.1.0. Keep the row a SINGLE line beginning `| `--planet` | string, required |` — `test/skill-cli-parity.test.js` anchors on that row shape with a line-anchored regex, so wrapping the cell across lines silently breaks the drift guard. Do not add, remove or rename any flag.

    (2) **Planet Selection table.** Append the Uranus, Neptune and Pluto rows from CONTEXT.md § "The three rows — final text, ratified and enriched", in that order, after Moon. Match the existing row shape exactly: `| **Name** | Domains of intent | Reach for this when… |` with the planet name bolded in the first cell. Per D-01 they are peers — no asterisk, no footnote marker, no parenthetical marking them lesser, no reordering of the classical seven. Wording may be reflowed to fit the table; substance may not change. Do NOT invent, extend or improve the domain content of any row, classical or modern — if something reads as wrong, HALT and report rather than fixing it.

    (3) **Selection heuristic** (the paragraph after the table that currently covers only subject-vs-verb). Add the three axes from CONTEXT.md § "The three new ambiguity axes" — Pluto vs. Saturn, Neptune vs. Moon, Uranus vs. Mars — as an addition alongside the existing rule, not a replacement of it. The existing subject-vs-verb paragraph and the "State the reasoning out loud, every time" paragraph both stay. Per D-01 do not add a tiebreak that ranks the modern three below the classical seven; the axes discriminate, they do not rank.

    (4) **Published-Surface Boundary section** (currently around line 117). Add a note that the modern three require a version later than the currently-published 1.1.0. Leave the existing 1.1.0 / 2026-08-09 `exports` record, the two-embedding-paths list, and the 1.0.0 pinning warning intact — this is an addition to that section, not a rewrite of it.

    Touch nothing else in the file: the frontmatter, Invocation, Embedding Checklist and Going Deeper sections are out of scope.
  </action>
  <verify>
    <automated>test "$(npm view @falkensmage/sigil-spinner version)" = "1.1.0" && test "$(grep -cE '^\| `--planet` \| string, required \|.*[Uu]ranus' skill/SKILL.md)" -eq 1 && test "$(grep -cE '^\| \*\*[A-Z][a-z]+\*\* \|' skill/SKILL.md)" -eq 10 && test "$(grep -cE '\*\*(Pluto vs\. Saturn|Neptune vs\. Moon|Uranus vs\. Mars)\*\*' skill/SKILL.md)" -eq 3 && test "$(sed -n '/^## Published-Surface Boundary/,$p' skill/SKILL.md | grep -ci '1\.1\.0')" -ge 1 && test "$(sed -n '/^## Published-Surface Boundary/,$p' skill/SKILL.md | grep -ci 'uranus\|neptune\|pluto')" -ge 1 && npx vitest run test/skill-cli-parity.test.js</automated>
  </verify>
  <done>
    The `--planet` row is one line, still matches the parity guard's row anchor, and names both the classical seven and the modern three with the version condition attached. The Planet Selection table has exactly ten bolded-name rows. The three ambiguity axes are present by name and the pre-existing subject-vs-verb rule survives unchanged. The Published-Surface Boundary section names 1.1.0 and the modern three together. `test/skill-cli-parity.test.js` is green. No tiebreak or ranking was introduced anywhere.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the correspondences attribution record — two distinguishable rounds</name>
  <files>skill/references/correspondences.md</files>
  <read_first>
    `.planning/quick/260812-n36-sync-the-sigil-skill-with-the-three-non-/260812-n36-CONTEXT.md` § "Attribution — precise, because this file's whole job is being honest about it", § "Corpus consulted (2026-08-12)", § "What the corpus contributed to each", and § "The generational objection — record it, do not act on it". Also re-read the file's existing `## Attribution` section (lines 5–9) — the new material sits alongside it, it does not replace it.
  </read_first>
  <action>
    This is the substance of the task, not a footnote. The file exists to make the correspondence table's provenance honest, and it dates the line between "ratified" and "possibly drifted". Extend it so the 2026-08-12 rows are recorded with the same precision as the 2026-08-09 rows AND with their differing provenance shape preserved.

    (1) **Opening line (line 3).** It currently states the inline table's row count. That count is now ten. Update the count word only; the rest of the sentence — the one-hop rationale, and what this file holds — stays as written.

    (2) **Restructure `## Attribution` into two dated blocks**, so the rounds are separated structurally and not merely by prose a skimming reader can conflate. Each block is an H3 (`### `) heading whose text carries its own ISO date — `2026-08-09` for one, `2026-08-12` for the other — because a heading a reader can scan to is what makes the two rounds genuinely distinguishable rather than merely both present. The existing 2026-08-09 paragraph and its "Honest limitation" paragraph move under the first of those H3s, unchanged in substance. The second H3 covers:

      - **Provenance shape, and why it differs.** 2026-08-12: drafted by Claude from general astrological training in the existing table's shape → ratified by Matt in conversation the same day ("rows are good") → THEN enriched at Matt's instruction against the corpus. 2026-08-09: every draft row was corrected or confirmed against Matt's judgment BEFORE the file was written. State that ordering difference explicitly — ratification-then-corpus-check versus correction-then-write. Do not flatten the two rounds into one undifferentiated "ratified by Matt" claim.
      - **The ambiguity axes carry the same provenance shape** — Claude drafted them from the ratified rows, Matt was shown them in final form and ratified them verbatim ("axes are good, ship them", 2026-08-12).
      - **Corpus consulted, both halves.** What yielded material: `celestial-alchemy/U01-Foundations/M01-Planets-Luminaries/outer-planets.md` (the substantive source), `celestial-alchemy/U01-Foundations/M05-Retrogrades/outer-planet-retrogrades.md`, and the Eighth House substack (one Neptune data point). What yielded nothing: `podia-mythology-for-astrologers` and `podia-techniques-for-intermediate-astrologers`. Per the SYMB-06 behavioural rule the union of astrological corpora was searched, not the nearest match — say so.
      - **What the corpus actually contributed**, per CONTEXT.md § "What the corpus contributed to each": Pluto's taboo domain was absent from the draft entirely and came from the course; Uranus's barriers/social-justice framing replaced a more individualist draft; and Neptune's row dropped a borrowed both-poles construction because the corpus shows Neptune has no second pole — the fog IS the planet. Record that Saturn's row earned its both-poles treatment through a worked example and Neptune does not need one.
      - **Farella.** The School of Astrological Arcana yielded nothing because Cristina Farella does not work with the outer planets (per Matt, 2026-08-12). Record this as a deliberate methodological stance. It is explicitly NOT a gap in the corpus, NOT a limitation of this file, and NOT material to be filled in later — do not file it under any heading or sentence that frames it as missing.
      - **The generational trade-off.** Celestial Alchemy classifies all three as generational planets — describing how a generation shows up and interacts with society, with less impact on personality than the luminaries and personal planets; Uranus ~7 years per sign, Neptune ~14, Pluto 13–25. A sigil is a personal working on one person's stated intention, so there is real tension in treating explicitly non-personal instruments as peers in a table whose heuristic tells sessions to choose confidently and never ask the user. Matt was shown this objection directly and declined to change the design, wanting the option to work with these planets when he chooses. Record it as a KNOWN, ACCEPTED trade-off with that attribution — a recorded tension, not an open question and not a caveat that softens D-01.

    (3) **Do not add worked examples for the modern three.** The `## Worked Examples` section covers the ambiguous two-planet cases already ratified on 2026-08-09; CONTEXT.md ratified axes for the modern three, not worked examples. Leave that section untouched.

    Do not invent, extend or improve the domain content of any correspondence row, classical or modern. Do not add a tiebreak. Do not reopen the generational objection.
  </action>
  <verify>
    <automated>test "$(grep -cF 'The seven-row table' skill/references/correspondences.md)" -eq 0 && test "$(grep -cF 'ten-row' skill/references/correspondences.md)" -eq 1 && test "$(grep -c '2026-08-09' skill/references/correspondences.md)" -ge 1 && test "$(grep -c '2026-08-12' skill/references/correspondences.md)" -ge 1 && test "$(grep -cE '^### .*2026-08-(09|12)' skill/references/correspondences.md)" -eq 2 && test "$(grep -ci 'farella' skill/references/correspondences.md)" -ge 1 && test "$(grep -ci 'generational' skill/references/correspondences.md)" -ge 1 && test "$(grep -ci 'celestial alchemy' skill/references/correspondences.md)" -ge 1 && test "$(grep -ci 'outer-planets\.md' skill/references/correspondences.md)" -ge 1 && test "$(grep -cE '^### ' skill/references/correspondences.md)" -ge 5</automated>
  </verify>
  <done>
    Line 3 states the row count as ten. `## Attribution` carries exactly two dated subsections, one per round, and a reader can tell from the text alone that 2026-08-09 corrected-before-writing while 2026-08-12 ratified-then-corpus-checked. The corpus record names both what yielded material and what did not. Farella's absence reads as a methodological stance with no framing as a gap or limitation. The generational tension is present as an accepted trade-off attributed to Matt's 2026-08-12 decision, with no tiebreak added and no ranking implied. `## Worked Examples` is byte-unchanged (its three existing 2026-08-09 examples are the `>= 5` heading count's remainder alongside the two new dated subsections).
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-sync the personal install and prove the change is docs-only</name>
  <files>~/.claude/skills/sigil/SKILL.md, ~/.claude/skills/sigil/references/correspondences.md</files>
  <action>
    Mechanical closeout. Two independent things must hold, and neither is implied by the other.

    (1) **Re-sync the install.** `~/.claude/skills/sigil/` currently exists on this machine and was byte-identical to `skill/` before this change, so Tasks 1 and 2 have just made `test/skill-install-parity.test.js` red. Run `npm run skill:install -- --force` to push the repo-canonical content to the destination. The repo is the source of truth in one direction only — never edit the installed copy to satisfy the guard.

    (2) **Prove docs-only.** Run the full suite and confirm zero drift outside `skill/`. This change touches no source, no snapshots and no library behaviour, so `git status --porcelain -- src bin test` must be empty and no committed SVG file snapshot under `test/__file_snapshots__/` may be regenerated. If `test/browser/*` fails for a missing browser, run `npx playwright install chromium` — that is this repo's documented one-time prerequisite, not a regression introduced here — then re-run.
  </action>
  <verify>
    <automated>npm run skill:install -- --force && CI=true npm test && test -z "$(git status --porcelain -- src bin test)" && test -z "$(git status --porcelain -- package.json package-lock.json)"</automated>
  </verify>
  <done>
    `npm run skill:install -- --force` completed, `CI=true npm test` exits 0 with `test/skill-cli-parity.test.js` and `test/skill-install-parity.test.js` both green, and `git status --porcelain` shows changes confined to `skill/SKILL.md`, `skill/references/correspondences.md` and `.planning/` — nothing under `src/`, `bin/`, `test/`, and no snapshot movement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `skill/` → `~/.claude/skills/sigil/` | Repo content is copied into a directory Claude Code auto-loads into every session on this machine. Content that lands here is executed as instructions by future sessions. |
| `SKILL.md` → a live session's shell | The Invocation section's command strings are typed verbatim by sessions into a shell with the user's privileges. |
| CONTEXT.md ratified text → published skill guidance | Lineage knowledge crossing from a planning artifact into instructions a session acts on without further review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-n36-01 | Tampering | `skill/SKILL.md` Invocation section | high | mitigate | Out of scope by construction — no task touches the Invocation section, and Phase 8's security pass already fixed the injection path in the primary example. Task 3's `git status` gate proves the diff is confined to the four edit sites named in Task 1 plus the attribution file. |
| T-n36-02 | Information disclosure | Ratified correspondence text | low | accept | The correspondences are Matt's own lineage knowledge, deliberately published into a personal skill on his own machine. `skill/` is repo-only (D-100: not in package.json `files`), so nothing here reaches the npm artifact. |
| T-n36-03 | Denial of service | `--planet` row wording | medium | mitigate | A non-version-aware planet list instructs live sessions to pass planets that the published 1.1.0 rejects with `E_UNKNOWN_PLANET` — a self-inflicted failure on every cold session until the next release. D-03's version-dependent phrasing is the mitigation; Task 1's `npm view` precondition and the row grep are the gates. |
| T-n36-04 | Repudiation | `correspondences.md` attribution | medium | mitigate | Flattening two provenance rounds into one undifferentiated claim destroys a future reader's ability to date the ratified/drifted line — the exact failure this file exists to prevent. Task 2 mandates two structurally separate dated subsections; the verify counts them. |
| T-n36-05 | Tampering | `~/.claude/skills/sigil/` install | medium | mitigate | An installed copy edited directly, or left stale, becomes a de facto second source of truth. Task 3 runs the one-directional installer and `test/skill-install-parity.test.js` asserts byte-identity. |
| T-n36-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan. `npm run skill:install` is a local `node scripts/skill-install.js` file copy with no registry fetch; `npm view` is a read-only metadata query. No new dependency is added, so the package-legitimacy gate has nothing to audit. |
</threat_model>

## Source Coverage Audit

Every source item is COVERED. No item is deferred, simplified, or split out.

| Source | Item | Covered by |
|---|---|---|
| GOAL | No ROADMAP phase — quick task, no phase goal line. N/A, not a gap. | — |
| REQ | No REQUIREMENTS.md in tree (removed at v1.1 archive). IDs `SKILL-MODERN-01` / `SKILL-ATTR-01` / `SKILL-VER-01` are synthetic, matching quick task 260812-m4b's precedent. | Tasks 1–3 |
| RESEARCH | No RESEARCH.md. The corpus research (SYMB-06 union search, 2026-08-12) was performed during discussion and is recorded inline in CONTEXT.md §§ "Corpus consulted" and "What the corpus contributed to each". | Task 2 |
| CONTEXT | **D-01** — full rows, sessions may choose them, no tiebreak | Task 1 edits (2) and (3); Task 2 generational-trade-off note |
| CONTEXT | **D-02** — the three domain rows are ratified text | Task 1 edit (2) |
| CONTEXT | **D-03** — version-aware flag table | Task 1 edits (1) and (4), plus the `npm view` precondition |
| SCOPE | 1. `SKILL.md:63` `--planet` row made version-aware | Task 1 edit (1) |
| SCOPE | 2. Planet Selection table gains three rows | Task 1 edit (2) |
| SCOPE | 3. Selection heuristic gains three axes | Task 1 edit (3) |
| SCOPE | 4. Version section notes the modern three need a later version | Task 1 edit (4) |
| SCOPE | 5. `correspondences.md` attribution record extended | Task 2 (all sub-items) |
| SCOPE | 6. Both parity tests stay green; `skill-cli-parity.test.js:56` untouched | Task 1 verify (targeted run) + Task 3 verify (full suite); the "seven current options" line is named as out of scope in `<context>` |

<verification>
1. `CI=true npm test` exits 0, with `test/skill-cli-parity.test.js` and `test/skill-install-parity.test.js` both green.
2. `git status --porcelain -- src bin test` is empty — zero snapshot movement, zero source change.
3. `skill/SKILL.md`'s Planet Selection table has ten rows; the `--planet` flag row is a single line naming both the classical seven and the modern three with the version condition attached.
4. `skill/references/correspondences.md` carries two dated attribution subsections whose provenance shapes are distinguishable, names the corpus consulted on both halves, records Farella's stance as methodological rather than as a gap, and records the generational tension as accepted.
5. No tiebreak, ranking, or hedge marking the modern three as lesser appears in either file.
6. `skill/VERIFY.md` is byte-unchanged from its 260812-m4b state.
</verification>

<success_criteria>
- A cold session reading `skill/SKILL.md` can choose uranus, neptune or pluto with stated reasoning, and knows the version condition that governs whether the invocation will succeed.
- A future reader of `skill/references/correspondences.md` can date each round of the correspondence table and can tell the two rounds' provenance shapes apart without external context.
- The published-surface skew is eliminated permanently rather than deferred to a release-day memory item.
- Zero behavioural change: the library, CLI, snapshots and `skill/VERIFY.md` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260812-n36-sync-the-sigil-skill-with-the-three-non-/260812-n36-SUMMARY.md` when done.
</output>
