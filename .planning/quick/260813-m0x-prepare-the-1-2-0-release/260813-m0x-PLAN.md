---
phase: quick-260813-m0x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - skill/SKILL.md
  - test/skill-version-skew.test.js
autonomous: true
requirements: [REL-1.2.0]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`package.json` states version `1.2.0`. `package-lock.json` agrees with the manifest on BOTH `name` and `version`, at both the root object and the `packages.\"\"` object — the exact pair of fields the 1.1.0 release had to repair after Phase 6 let them strand."
    - "The `package-lock.json` diff is confined to version fields. No devDependency resolution, integrity hash, or transitive entry moves in this commit — a release commit is not the place to silently change what the dev toolchain resolves to."
    - "`skill/SKILL.md`'s three version-dependent LIVE claims are reconciled: the `--planet` flag-table row, the Published-Surface Boundary opening paragraph, and the **Planet-list skew.** paragraph."
    - "The **Planet-list skew.** paragraph is REWRITTEN, not renumbered. It states the real post-1.2.0 situation — the classical seven work on every published version; uranus, neptune and pluto arrived in `1.2.0` and are available from that release onward; a consumer pinned below `1.2.0` gets `E_UNKNOWN_PLANET` for any of the three. It no longer claims a skew that will not exist once 1.2.0 publishes."
    - "The rewritten skew paragraph preserves its instruction to re-check `npm view @falkensmage/sigil-spinner version`, retargeted at the fact that is genuinely still live (what `@latest` resolves to right now) rather than at the resolved one."
    - "No sentence anywhere in `skill/SKILL.md` claims a live `npm view` re-query that this task did not actually perform. The Published-Surface Boundary paragraph's existing provenance clause (`re-queried live after the 1.1.0 release rather than assumed`) is replaced with an honest statement, because renumbering it would assert a registry observation of 1.2.0 that cannot have happened before the publish."
    - "The paragraph beginning `Prior to ` is byte-identical to its pre-task state. It is a HISTORICAL claim about the `exports` map widening and stays pinned at 1.1.0/1.0.0 forever."
    - "The two entries of `HISTORICAL_PINS` in `test/skill-version-skew.test.js` — their versions and their reasons — are byte-identical to their pre-task state."
    - "The guard's region model is CORRECT at 1.2.0 and stays correct at 1.3.0: `planet-flag-row` and `planet-list-skew` are reclassified from LIVE to HISTORICAL, pinned to a new, separately-reasoned pin (`1.2.0` — the release at which uranus, neptune and pluto became available). `published-surface-boundary` is the only remaining LIVE claim, because it is the only claim in the file that genuinely tracks whatever is currently published."
    - "The reclassification is a STRENGTHENING. Post-change the two reclassified regions are pinned to an exact literal and a find-and-replace sweep across them is caught by name; pre-change they were bound to a moving target that would have dragged a permanently-true statement wrong at the next bump."
    - "`findHistoricalMismatches` still does NOT accept the current version as a parameter. The exemption stays structural, not merely commented — the property the original guard's own doc comment names as load-bearing."
    - "Every semver token in `skill/SKILL.md` remains accounted for by exactly one known region. Four regions total, one live and three historical."
    - "The guard's failure messages stay accurate under the new model: the live-claim message no longer names a single historical paragraph as the only do-not-touch region, and the historical message names WHICH historical region failed and why its pin is deliberate."
    - "New fixture soundness tests prove the new pin discriminates in BOTH reclassified regions independently, and that a bumped current version leaves all three historical regions green."
    - "`~/.claude/skills/sigil/` is byte-identical to `skill/` — re-synced after the SKILL.md edit, verified by `test/skill-install-parity.test.js` rather than assumed."
    - "`npm run lint` exits 0, `npm run typecheck` exits 0, `CI=true npm test` is fully green including the version-skew guard at 1.2.0, and `npm run test:pack` is green."
    - "Zero snapshot movement, proven mechanically: `git diff --exit-code -- test/__file_snapshots__ test/render/__snapshots__` exits 0 and `git status --porcelain -- test/__file_snapshots__ test/render/__snapshots__` is empty."
    - "Nothing under `src/` or `bin/` changed. No kamea grid, no correspondence row, no library behaviour. `git status --porcelain -- src bin` reports nothing."
    - "The pack manifest is inspected, not assumed: the new test files added by quick tasks 260812-m4b/n36/rfu are NOT packed, and the manifest's actual shape (file count, inclusions, exclusions, dependency count) is recorded in the commit body in 99b6f82's format."
    - "One commit exists, subject `chore(release): 1.2.0`, body in the project's established style — what changed, why it is a minor bump, and the gate numbers."
    - "The working tree is clean at the end. No tag was created (`git tag --points-at HEAD` is empty), nothing was pushed (the branch is ahead of `origin/main` by exactly 1), and nothing was published."
  artifacts:
    - package.json
    - package-lock.json
    - skill/SKILL.md
    - test/skill-version-skew.test.js
  key_links:
    - "`package.json` `version` ↔ `test/skill-version-skew.test.js` ↔ `skill/SKILL.md`. This is the binding quick task 260812-rfu created for exactly this moment. Task 1 fires it deliberately; Task 2 discharges the worklist it prints."
    - "`skill/` ↔ `~/.claude/skills/sigil/`, bound byte-for-byte by `test/skill-install-parity.test.js`. Task 2 changes `skill/SKILL.md`, so Task 3's re-sync is mandatory, not optional."
    - "`package.json` `files: [src, bin, README.md]` ↔ the pack manifest. The reason no test file can leak into the published tarball — verified in Task 3 rather than trusted."
    - "`HISTORICAL_PINS` (exports-map widening, 1.1.0/1.0.0) ↔ the new modern-three pin (1.2.0). Two independent pin sets, two independent reasons, one shared structural property: neither takes `package.json` as an input."
    - "99b6f82 (`chore(release): 1.1.0`) is the template for this commit's shape, scope, and body format — including the practice of recording the pack manifest's shape as a checkable number."
---

<objective>
Prepare a publishable 1.2.0 release commit, and stop.

Quick task 260812-m4b added uranus, neptune and pluto as first-class planets. Purely additive — the library surface, the `bin`, and the `exports` map are unchanged — so this is a minor bump, matching the precedent of 99b6f82, which took a minor for the additive `./element` subpath.

The interesting work is not the bump. Quick task 260812-rfu bound `skill/SKILL.md`'s version claims to `package.json` so that a release could not silently ship a false statement. That guard now fires. Discharging it correctly requires noticing that one of its three LIVE claims — and, on inspection, a second one it also covers — stop being live claims at all the moment 1.2.0 publishes, and that renumbering them would produce prose that is grammatically current and factually false.

Purpose: land the version bump and leave every version-dependent statement in the repo true — not just true today, but true at 1.3.0.
Output: one commit, `chore(release): 1.2.0`, on a clean tree.
</objective>

<hard_stop>
**Do NOT publish. Do NOT tag. Do NOT push.**

- No `npm publish`, no `npm dist-tag`, no `gh workflow run release.yml` — in any mode, including `--dry-run` variants that touch credentials.
- No `git tag`. `npm version` is invoked ONLY with `--no-git-tag-version`.
- No `git push`, no `gh pr create`.

This task produces a publishable commit and stops there. Publishing this package is not meaningfully reversible — npm's unpublish window is 72 hours, the name locks for 24 hours after a full unpublish, and a version number is never reusable. That keystroke is the developer's own. If a verify step in this plan appears to require a network write, the verify step is wrong; halt and report rather than reaching for the registry.
</hard_stop>

<decomposition_note>
Tracer-first decomposition does not apply here and is deliberately not used. There is no architecture under test and no layer stack to prove — this is a linear release checklist over already-proven machinery, where a thin end-to-end slice would produce no information a full pass does not. Task 1 nevertheless carries the property that makes a tracer useful: it ends by firing the real guard and reading its real output, so the worklist for Task 2 is machine-produced rather than transcribed from this plan.
</decomposition_note>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@skill/SKILL.md
@test/skill-version-skew.test.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump the manifest and lockfile to 1.2.0, then read the guard's worklist</name>
  <files>package.json, package-lock.json</files>
  <precondition>The working tree is clean and HEAD is 1a5db9e or a descendant. `git status --porcelain` is empty before starting.</precondition>
  <action>
Bump to 1.2.0 using `npm version 1.2.0 --no-git-tag-version`. Use that exact invocation and nothing else. It rewrites `package.json`'s `version` and reconciles `package-lock.json` in the same pass — root `version`, `packages[""].version`, and the `name` field at both levels — with no registry fetch, no commit, and no tag. This is the specific correctness the 1.1.0 release had to retrofit: 99b6f82's body records the lock stranded at `sigil-spinner@0.1.0` because Phase 6 moved the manifest without regenerating the lock, and the 1.0.0 publish tolerated the mismatch. Do not let it drift again.

Do NOT reach for a bare `npm install`, `npm update`, or `npm ci` to "fix up" the lock. Those resolve devDependency ranges against the registry and would move integrity hashes and transitive entries inside a release commit — a change nobody reviewed, riding along with one everyone did. If the lock somehow needs more than the version fields, halt and report rather than regenerating it.

Then fire the guard deliberately and capture its output: run `npx vitest run test/skill-version-skew.test.js`. It WILL fail, and that failure is the deliverable of this task, not a problem to route around. `buildLiveClaimFailureMessage` was written for exactly this moment — it prints `package.json`'s version as the source of truth, then names every mismatched region by id, description, and line number. Save that output verbatim; it is Task 2's worklist, and it is authoritative over any line number quoted in this plan.

Read the printed message before proceeding, including its two closing instructions (leave the `Prior to ` paragraph alone; re-sync the installed skill afterward). Both are honored later in this plan — confirm they say what this plan assumes they say.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'),l=require('./package-lock.json');const ok=p.version==='1.2.0'&&l.version===p.version&&l.packages[''].version===p.version&&l.name===p.name&&l.packages[''].name===p.name;if(!ok){console.error(JSON.stringify({pkg:[p.name,p.version],lockRoot:[l.name,l.version],lockPkg:[l.packages[''].name,l.packages[''].version]},null,2));process.exit(1)}console.log('manifest and lock agree on name and version at both levels')"</automated>
    <automated>git diff --stat -- package-lock.json; N=$(git diff --numstat -- package-lock.json | awk '{print $1+$2}'); echo "lock diff: ${N:-0} changed lines"; test "${N:-0}" -le 12 && echo "lock diff confined to version fields" || { echo "FAIL: lock diff is wider than the version fields — inspect it before continuing"; exit 1; }</automated>
    <automated>D=$(git status --porcelain -- src bin skill test); test -z "$D" && echo "no source, skill, or test file touched by the bump" || { echo "FAIL: bump touched files it must not:"; echo "$D"; exit 1; }</automated>
  </verify>
  <done>`package.json` is 1.2.0; `package-lock.json` agrees on name and version at both the root and `packages[""]`; the lock diff touches nothing but those fields; no source, skill, or test file changed; and the version-skew guard's failure message has been captured verbatim as Task 2's worklist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile SKILL.md's version claims and correct the guard's region model</name>
  <files>skill/SKILL.md, test/skill-version-skew.test.js</files>
  <read_first>skill/SKILL.md (lines 57-70 for the flag table, 124-137 for the Published-Surface Boundary section), test/skill-version-skew.test.js (whole file — its region model is what changes)</read_first>
  <behavior>
The guard, after this task, must exhibit exactly these properties. Write or adjust the fixture tests first, watch them fail against the current model, then change the model.

- Live-claim mismatch in the Published-Surface Boundary region → exactly one finding, `id === 'published-surface-boundary'`.
- All regions at their expected versions → zero live findings and zero historical findings (control).
- Current version bumped to something no region states → live findings > 0, AND all three historical regions still green. This is the exemption proof, now covering three historical regions rather than one.
- The exports-map pin altered in the `Prior to ` paragraph → exactly one historical finding, naming that region. (Existing sweep-detection test, must still pass.)
- The modern-three pin altered in the `--planet` flag row → exactly one historical finding, `id === 'planet-flag-row'`.
- The modern-three pin altered in the **Planet-list skew.** paragraph → exactly one historical finding, `id === 'planet-list-skew'`.
- A semver token outside every known region → one coverage gap, with its line number. (Existing, unchanged.)
- A missing anchor and an ambiguous anchor each throw their own distinctly-named error. (Existing, unchanged.)
- The live-tree region parse yields FOUR regions total: one live, three historical.
  </behavior>
  <action>
**Part A — the reasoning that drives this task. Do not skip it; the edit is wrong without it.**

Before the release, the `--planet` flag row and the **Planet-list skew.** paragraph both said the modern three "require a version later than `1.1.0`". That is a forward-looking, unresolved statement whose truth depended on what was currently published, which is precisely why binding it to `package.json` was right: it was a promissory note, and the guard was the thing that would force it to be reconciled at release time.

This release IS that reconciliation. Afterward the same fact resolves to "uranus, neptune and pluto arrived in `1.2.0`". That is a fact about a specific release. It is true at 1.2.0. It is true at 1.3.0. It is true at 2.0.0. It never moves again.

So keeping those two regions classified LIVE would pass right now by coincidence — 1.2.0 happens to be both `package.json`'s version and the arrival release — and then go red at 1.3.0, where the guard's own failure message would instruct a developer to rewrite `1.2.0` as `1.3.0` and thereby corrupt a correct statement about history. That is exactly the failure mode the guard's existing doc comment names as "actively wrong". Passing today for the wrong reason and failing tomorrow with wrong advice is not a guard; it is a trap with a test around it.

Counter-check, and the test to apply to every decision in this task: at 1.3.0, what in `skill/SKILL.md` is actually stale? Only the Published-Surface Boundary paragraph, which states what is currently published. Everything else in the file — the exports-map history, the arrival release of the modern three — is still true. The correct region model is the one that produces exactly that answer.

Therefore: reclassify `planet-flag-row` and `planet-list-skew` from LIVE to HISTORICAL under a new pin, and leave `published-surface-boundary` as the sole LIVE claim. This is a strengthening, not a weakening: those two regions go from bound-to-a-moving-target to pinned-to-an-exact-literal, which is strictly more detection, and a find-and-replace sweep across the file now gets caught in three places instead of one. Do not, under any circumstance, relax an assertion, widen a matcher, or delete a case to make the suite green.

**Part B — edit `skill/SKILL.md`.** Use Task 1's captured failure message as the worklist; its line numbers are authoritative.

1. **`--planet` flag row** (currently line 63) — renumber to state that uranus, neptune and pluto require `1.2.0` or later. Keep the whole claim on that single table-row line: the region is extracted with `kind: 'row'`, which stops at the next newline, so a token that wraps to a second line falls outside the region and trips the coverage-gap test.

2. **Published-Surface Boundary opening paragraph** (currently line 126) — two tokens, both LIVE, both become `1.2.0`. Update the date to 2026-08-13. Then fix the provenance clause: it currently reads `re-queried live after the 1.1.0 release rather than assumed`, and renumbering that verbatim would assert a registry observation of 1.2.0 that cannot have happened, because 1.2.0 is not published yet. Replace it with something honest — that this line is written at release-prep time for the 1.2.0 release, and that `npm view @falkensmage/sigil-spinner version` is the command that confirms what `latest` resolves to right now. Keep the anchor substring `The published package resolves to version` intact and unique; the guard and its fixture both key on it.

3. **Planet-list skew paragraph** (currently line 137) — REWRITE. The skew this paragraph describes ceases to exist when 1.2.0 publishes; all ten planets work on `@latest`. State the real post-release situation: the classical seven are valid on every published version; uranus, neptune and pluto arrived in `1.2.0` and are available from that release onward; a consumer pinned below `1.2.0` gets `E_UNKNOWN_PLANET` for any of the three. Say plainly that this boundary is a fact about which release added them and does not move when a later version publishes. Preserve the paragraph's genuinely useful instruction to re-check `npm view @falkensmage/sigil-spinner version`, retargeted at the fact that is still live — what `@latest` currently resolves to, and therefore what a bare `npx ...@latest` will actually run.

   Four mechanical constraints on this rewrite, each of which will break the guard if violated: keep it as ONE paragraph with no internal blank line (paragraph regions end at the first blank line, and this is the last paragraph in the file, so trailing tokens would land outside every region); keep the anchor `**Planet-list skew.**` intact and unique; do not introduce the substring `The published package resolves to version` or a second occurrence of `| \`--planet\` |`; and do not introduce a capital-P `Prior to ` anywhere, which would make the historical anchor ambiguous and throw.

4. **Do NOT touch the paragraph beginning `Prior to `.** It is about the `exports` map widening — an unrelated fact about the past — and stays at 1.1.0/1.0.0 forever. Verify it is byte-identical when you are done.

**Part C — correct `test/skill-version-skew.test.js`.**

Generalize the historical machinery from one anchor and one pin set to a list, mirroring the shape `LIVE_CLAIMS` already has:

- Add a `MODERN_THREE_PINS` constant holding a single entry, `1.2.0`, with a reason stating it is the release at which uranus, neptune and pluto became available. Give it the same care the existing pin set received: a doc comment explaining why it is transcribed rather than keyed, and why a later bump must not move it.
- Introduce a `HISTORICAL_CLAIMS` array whose entries carry `{ id, anchor, kind, description, pins }`: the exports-map region (`id: 'exports-map-widening'`, anchor `'Prior to '`, kind `paragraph`, pins `HISTORICAL_PINS`), plus the two reclassified regions (`planet-flag-row` as `row`, `planet-list-skew` as `paragraph`, both with `pins: MODERN_THREE_PINS`). Reuse the existing anchors and descriptions verbatim when moving those two entries out of `LIVE_CLAIMS` — the anchors are already proven unique against the real file.
- Reduce `LIVE_CLAIMS` to its single remaining member, `published-surface-boundary`.
- `HISTORICAL_PINS` itself — both entries, their versions and their reasons — stays byte-identical. Only the prose above it changes, because it is no longer "the one deliberate transcription exception in this file"; correct that sentence to describe two pin sets and say what distinguishes them. Leaving it stale would make the file lie about itself, which is the same defect class this whole task exists to close.
- `findHistoricalMismatches(md)` iterates `HISTORICAL_CLAIMS` and returns one finding per mismatched region, each carrying its `id` so tests and messages can name it. Its signature keeps taking exactly one parameter — the markdown — and never the current version. That structural exemption is load-bearing and is called out as such in the file's own doc comment; preserve it and preserve the comment that explains it.
- `findCoverageGaps` builds its region list from `LIVE_CLAIMS` plus `HISTORICAL_CLAIMS`. Still four regions.
- Rewrite the file's opening doc comment to describe the new model and, critically, WHY the reclassification happened — the Part A reasoning, compressed. A future reader must be able to tell that this was a deliberate correction rather than an erosion.
- Update `buildLiveClaimFailureMessage`: its do-not-touch line currently names one paragraph, and there are now three historical regions. Generalize it to name all three, derived from `HISTORICAL_CLAIMS` rather than transcribed a second time. Keep the `skill:install` re-sync instruction — Task 3 depends on a human or agent actually reading it.
- Give the historical test a failure message of comparable quality, naming which historical region drifted, which literal it now states, which pin it was supposed to match, and why that pin is deliberate.
- Update the live-tree test names and assertions to the new counts: one live region, three historical, four total.
- Leave `FIXTURE_DEFAULT_VERSION` at `1.1.0` and add a comment saying why that is now deliberate rather than stale: it is the fixture's stand-in for "whatever `package.json` currently says", and holding it DIFFERENT from `MODERN_THREE_PINS`' `1.2.0` is what makes the fixtures able to catch a bug that conflates a live token with a pinned one. Point the fixture builder's `planetRow` and `skewParagraph` defaults at the modern-three pin, and leave `boundaryParagraph` defaulting to `FIXTURE_DEFAULT_VERSION`.
- Add the two new sweep-detection fixture tests named in `<behavior>`, and rework the existing exemption-proof test to bump only the live region and assert all three historical regions stay green.
  </action>
  <verify>
    <automated>npx vitest run test/skill-version-skew.test.js</automated>
    <automated>node -e "const {execSync}=require('child_process');const cur=require('fs').readFileSync('skill/SKILL.md','utf8');const old=execSync('git show HEAD:skill/SKILL.md',{encoding:'utf8'});const para=(t)=>{const i=t.indexOf('Prior to ');if(i<0)throw new Error('anchor missing');const s=t.lastIndexOf('\n',i)+1;const m=t.slice(s).match(/\n[ \t]*\n/);return t.slice(s,m?s+m.index:t.length)};const a=para(old),b=para(cur);if(a!==b){console.error('HISTORICAL paragraph changed.\n--- was ---\n'+a+'\n--- now ---\n'+b);process.exit(1)}console.log('historical exports-map paragraph byte-identical')"</automated>
    <automated>node -e "const {execSync}=require('child_process');const g=(t)=>t.slice(t.indexOf('const HISTORICAL_PINS'),t.indexOf('];',t.indexOf('const HISTORICAL_PINS'))+2);const a=g(execSync('git show HEAD:test/skill-version-skew.test.js',{encoding:'utf8'})),b=g(require('fs').readFileSync('test/skill-version-skew.test.js','utf8'));if(!a||a!==b){console.error('HISTORICAL_PINS entries changed.\n--- was ---\n'+a+'\n--- now ---\n'+b);process.exit(1)}console.log('HISTORICAL_PINS byte-identical')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('test/skill-version-skew.test.js','utf8');const m=s.match(/function findHistoricalMismatches\(([^)]*)\)/);if(!m){console.error('findHistoricalMismatches not found');process.exit(1)}const params=m[1].split(',').map(p=>p.trim()).filter(Boolean);if(params.length!==1){console.error('structural exemption broken — findHistoricalMismatches now takes '+params.length+' params: '+params.join(', '));process.exit(1)}console.log('structural exemption intact: findHistoricalMismatches('+params[0]+')')"</automated>
    <automated>npm run typecheck</automated>
    <automated>D=$(git status --porcelain -- src bin test/__file_snapshots__ test/render/__snapshots__); test -z "$D" && echo "no source, bin, or snapshot file touched by this task" || { echo "FAIL: this task touched files it must not:"; echo "$D"; exit 1; }</automated>
  </verify>
  <done>All four version-dependent regions of `skill/SKILL.md` state something true at 1.2.0 and still true at 1.3.0; the skew paragraph describes a resolved arrival boundary rather than a skew that no longer exists; the boundary paragraph claims no registry query that did not happen; the `Prior to ` paragraph and the `HISTORICAL_PINS` entries are byte-identical; `findHistoricalMismatches` still takes exactly one parameter; the guard is green at 1.2.0 with one live region and three historical ones; and the new fixture tests prove the modern-three pin discriminates independently in both reclassified regions.</done>
</task>

<task type="auto">
  <name>Task 3: Re-sync the installed skill, run every release gate, and commit</name>
  <files>~/.claude/skills/sigil/ (regenerated, outside the repo), git commit</files>
  <precondition>Tasks 1 and 2 are complete. `skill/SKILL.md` is in its final state — the re-sync copies whatever is there, so any later edit would silently re-break parity.</precondition>
  <action>
Re-sync the installed skill with `npm run skill:install -- --force`. `skill/` is repo-canonical and the install is one-directional by design (D-99); `test/skill-install-parity.test.js` enforces byte-identity and will fail loudly otherwise. This writes to `~/.claude/skills/sigil/`, outside the repo, so it does not dirty the tree.

Run the four release gates in order and record each exact number — they go in the commit body, matching 99b6f82's format, which recorded `lint 0, typecheck 0, 1532/1532 tests, test:pack 2/2`:

1. `npm run lint` — record the exit code.
2. `npm run typecheck` — record the exit code.
3. `CI=true npm test` — record passed/total and the file count. The browser test needs a one-time `npx playwright install chromium` and fails loudly rather than skipping when absent; if it fails for that reason, install chromium and re-run rather than treating the failure as a regression. Any other failure is a real regression — halt and report.
4. `npm run test:pack` — record passed/total.

Then inspect the pack manifest with `npm pack --dry-run` rather than assuming it. Three quick tasks (260812-m4b, 260812-n36, 260812-rfu) added test files since 1.1.0; `files` is `["src","bin","README.md"]`, so none of them should be packed, but confirm it rather than trusting it — the manifest is what actually reaches the registry, and a widened manifest is the one failure in this task that would only be discovered by strangers. Record the manifest's real shape: file count, that `./element` is included, that `skill/`, `scripts/`, `examples/`, `test/` and `.planning/` are excluded, and that the dependency count is zero. If the file count differs from 1.1.0's 19, report the actual number and what accounts for the difference — do not restate 19.

Prove the snapshots did not move: `git diff --exit-code -- test/__file_snapshots__ test/render/__snapshots__` must exit 0 and `git status --porcelain -- test/__file_snapshots__ test/render/__snapshots__` must be empty. This task changes no rendering code, so any movement is a signal that something unintended happened, not something to accept.

Commit all four changed files as one commit, subject `chore(release): 1.2.0`. Write the body in the project's established style, following 99b6f82: what changed and why it is a minor bump (the three modern planets are additive — no breaking change to the library surface, the bin, or the `exports` map); the region-model correction and why it was a reclassification rather than a renumber; and the recorded gate numbers plus the real pack-manifest shape. End with the project's `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` trailer, as 99b6f82 does.

Then stop. Re-read this plan's `<hard_stop>` before doing anything else. The next action belongs to the developer, not to this task.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && CI=true npm test && npm run test:pack</automated>
    <automated>npm pack --dry-run 2>&1 | tee "${TMPDIR:-/tmp}/pack-manifest.txt" | tail -40; node -e "const t=require('fs').readFileSync(process.env.TMPDIR? process.env.TMPDIR+'/pack-manifest.txt':'/tmp/pack-manifest.txt','utf8');const bad=['skill/','scripts/','examples/','test/','.planning/'].filter(p=>new RegExp('\\\\s'+p.replace('.','\\\\.')).test(t));if(bad.length){console.error('pack manifest includes paths that must be excluded: '+bad.join(', '));process.exit(1)}if(!/element/.test(t)){console.error('pack manifest is missing the element entry');process.exit(1)}console.log('pack manifest excludes the repo-only trees and includes element')"</automated>
    <automated>git diff --exit-code -- test/__file_snapshots__ test/render/__snapshots__ && test -z "$(git status --porcelain -- test/__file_snapshots__ test/render/__snapshots__)" && echo "zero snapshot movement"</automated>
    <automated>D=$(git status --porcelain -- . ':(exclude).planning'); test -z "$D" && echo "working tree clean outside .planning (this plan's own docs are committed separately by the quick-task workflow, after this task)" || { echo "FAIL: tree not clean:"; echo "$D"; exit 1; }</automated>
    <automated>git log -1 --format=%s | grep -qx 'chore(release): 1.2.0' && echo "release commit is HEAD with the expected subject"</automated>
    <automated>git show --numstat --format= HEAD | grep -c . > /dev/null; N=$(git show --numstat --format= HEAD | grep -c .); git show --numstat --format= HEAD; test "$N" -eq 4 && echo "release commit touches exactly 4 files" || { echo "FAIL: release commit touches $N files, expected 4 (package.json, package-lock.json, skill/SKILL.md, test/skill-version-skew.test.js) — nothing from .planning/ belongs in this commit"; exit 1; }</automated>
    <automated>test -z "$(git tag --points-at HEAD)" && echo "no tag created at HEAD" || { echo "FAIL: a tag exists at HEAD — this task must not tag"; exit 1; }</automated>
    <automated>test -z "$(git branch -r --contains HEAD 2>/dev/null)" && echo "release commit is on no remote branch — nothing was pushed" || { echo "FAIL: HEAD is already on a remote — it was pushed"; exit 1; }</automated>
    <automated>npm view @falkensmage/sigil-spinner version 2>/dev/null | grep -qx '1.1.0' && echo "registry still serves 1.1.0 — nothing was published, as required" || echo "NOTE: registry did not return 1.1.0 (offline, or someone published); confirm by hand that this task published nothing"</automated>
  </verify>
  <done>`~/.claude/skills/sigil/` is byte-identical to `skill/` and the parity test proves it; lint and typecheck exit 0; the full suite and `test:pack` are green including the version-skew guard at 1.2.0; the pack manifest was inspected and its real shape recorded; snapshots did not move; one commit exists with subject `chore(release): 1.2.0` and a body carrying the real gate numbers; the tree is clean; no tag exists at HEAD; and exactly one commit sits unpushed ahead of `origin/main`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public npm registry | The `files` array and the resulting pack manifest decide what leaves this machine forever. This task prepares that payload; it does not send it. |
| repo → `~/.claude/skills/sigil/` | `npm run skill:install` writes outside the repo boundary, into a directory every Claude Code session on this machine reads. |
| plan → executor authority | The one irreversible action in this task's neighbourhood (publish/tag/push) is deliberately outside the executor's remit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m0x-01 | Elevation of Privilege | `npm publish` / `git tag` / `git push` | high | mitigate | `<hard_stop>` states the prohibition at plan level and Task 3's action closes by re-reading it. No task's `<verify>` performs a network write. Task 3 asserts `git tag --points-at HEAD` is empty and that exactly one commit sits unpushed, so drift into any of the three is detected mechanically rather than trusted. |
| T-m0x-02 | Information Disclosure | pack manifest | medium | mitigate | `npm pack --dry-run` is inspected, not assumed, and the verify fails if `skill/`, `scripts/`, `examples/`, `test/` or `.planning/` appear. This is the one defect in the task that would otherwise be found by strangers, after it is unfixable. |
| T-m0x-03 | Tampering | `package-lock.json` | medium | mitigate | Bump uses `npm version 1.2.0 --no-git-tag-version` only — no registry resolution. Task 1's verify fails if the lock diff exceeds the version fields, so a devDependency resolution cannot ride along inside a release commit under cover of a reviewed change. |
| T-m0x-04 | Tampering | `test/skill-version-skew.test.js` | medium | mitigate | The guard is being edited in the same task it constrains — the classic path to a weakened assertion. Countered by writing the fixture tests first (`<behavior>`), by an explicit prohibition on relaxing assertions, and by three byte-identity/structural verifies (`HISTORICAL_PINS` unchanged, the `Prior to ` paragraph unchanged, `findHistoricalMismatches` still single-parameter). |
| T-m0x-05 | Tampering | `~/.claude/skills/sigil/` | low | accept | The install is one-directional by design (D-99), writes only content reviewed in Task 2, and is parity-guarded. Accepted: the write is the point, and its integrity is asserted by `test/skill-install-parity.test.js`. |
| T-m0x-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages are installed. `npm version --no-git-tag-version` mutates the manifest and lock locally with no registry fetch, and Task 1 explicitly forbids `npm install`/`update`/`ci`. No `## Package Legitimacy Audit` is required because no install task exists. |
</threat_model>

<verification>
Run at the end of Task 3, after the commit:

1. `npm run lint` → 0; `npm run typecheck` → 0; `CI=true npm test` → fully green; `npm run test:pack` → green. Every number recorded in the commit body.
2. `npx vitest run test/skill-version-skew.test.js` → green at 1.2.0, with one live region and three historical regions parsed from the real `skill/SKILL.md`.
3. `npx vitest run test/skill-install-parity.test.js` → green without a further reinstall.
4. `git diff --exit-code -- test/__file_snapshots__ test/render/__snapshots__` → exit 0.
5. `git status --porcelain -- . ':(exclude).planning'` → empty. `.planning/` is excluded deliberately: this plan's own quick-task docs are committed separately by the workflow, after this task, and must NOT be swept into the release commit.
6. `git show --stat HEAD` → exactly four files: `package.json`, `package-lock.json`, `skill/SKILL.md`, `test/skill-version-skew.test.js`.
7. `git tag --points-at HEAD` → empty. `git branch -r --contains HEAD` → empty. `npm view @falkensmage/sigil-spinner version` → still `1.1.0`.

Manual read-through, which no test can do for you: read the rewritten skew paragraph and the boundary paragraph aloud as if 1.3.0 had just shipped. Every sentence must still be true. If one is not, the region model is wrong, not the prose.
</verification>

<success_criteria>
- `package.json` and `package-lock.json` both state `@falkensmage/sigil-spinner@1.2.0`, at every level the lock records it.
- The lock diff contains nothing but version fields.
- `skill/SKILL.md` carries no false statement at 1.2.0 and none that becomes false at 1.3.0.
- The Planet-list skew paragraph describes a resolved arrival boundary, not a skew.
- The `Prior to ` paragraph and the `HISTORICAL_PINS` entries are byte-identical to their pre-task state.
- The guard is correct, not merely green: one LIVE region, three HISTORICAL regions across two independently-reasoned pin sets, `findHistoricalMismatches` still taking no version parameter, and new fixtures proving the new pin discriminates in both reclassified regions.
- `~/.claude/skills/sigil/` is byte-identical to `skill/`.
- All four gates green, numbers recorded in the commit body, pack manifest inspected and its real shape reported.
- Zero snapshot movement. No change under `src/` or `bin/`.
- One commit, `chore(release): 1.2.0`, clean tree, no tag, unpushed.
- **Nothing published.**
</success_criteria>

<output>
Create `.planning/quick/260813-m0x-prepare-the-1-2-0-release/260813-m0x-SUMMARY.md` when done.

The summary must state, in its own words, the answer this task worked out: whether the modern-three availability version is a live claim or a historical one, and what the guard's region model looks like as a result. That reasoning is the durable output here — the version bump is the easy half.

The summary must also state plainly that the release is prepared but NOT published, and that the next action is the developer's own.
</output>
