---
phase: quick-260812-m4b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/kamea.js
  - src/render/glyphs.js
  - src/render/coords.js
  - test/data/kamea.test.js
  - test/render/glyphs.test.js
  - test/cli/cli.test.js
  - test/render/grid.test.js
  - test/browser/element.test.js
  - test/citations.test.js
  - skill/VERIFY.md
  - examples/element.html
  - README.md
autonomous: true
requirements: [KAM-PROV-01, KAM-INV-01, KAM-MODERN-01]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`src/data/kamea.js`'s header records that all seven classical grids were diffed cell-by-cell against a named physical printed source with page numbers, and does not claim more than that."
    - "The header records the Mercury divergence AND the two independent proofs that the repo is correct and the book defective — the book's anti-diagonal sums 257, and the book breaks associativity at 8 cells."
    - "The header records that the Sun kamea is non-associative by nature (order 6 is singly-even), not by defect."
    - "The header records that the construction rules are fitted empirically and are NOT attributable to Agrippa, with the dating reason (Agrippa c.1510; the Siamese method named for de la Loubère post-1687)."
    - "Every kamea in every kamea set is proven, by a standing test, to be a permutation of 1..n^2 and to sum to its magic constant on all rows, all columns, and both diagonals."
    - "Every kamea except the Sun is proven associative — v(r,c) + v(n-1-r,n-1-c) === n^2+1 — and the Sun's exemption carries a written reason at the exemption site."
    - "The invariant tests iterate the live kamea set, so a kamea added later is covered without anyone writing a new test."
    - "`--planet uranus`, `--planet neptune`, and `--planet pluto` each produce a sigil, exactly as the classical seven do — no gating flag, no separate kamea set."
    - "Neptune's entry records, at the point of divergence, that it contradicts the printed source and why the generated square was preferred."
    - "Every generated artifact for the existing seven planets is byte-identical to its pre-change bytes: no committed snapshot file changes, and none is regenerated."
  artifacts:
    - src/data/kamea.js
    - src/render/glyphs.js
    - test/data/kamea.test.js
    - test/render/glyphs.test.js
    - skill/VERIFY.md
    - README.md
  key_links:
    - "`PLANET_ORDER` in `src/data/kamea.js` is the single enumeration every other planet surface derives from — the error message, `planetNames()`, and `PLANET_GLYPHS`'s key set all bind to it, so extending it is what makes the three planets first-class."
    - "The word `pluto` is the repo's incumbent invalid-planet fixture in 15 places across tests, the skill, and the examples page. Promoting Pluto to a real planet silently inverts every one of them — several assert a throw that no longer happens. The sentinel rename is the load-bearing half of Task C, not cleanup."
    - "`test/citations.test.js` R1 requires every `.md` token in a `src/` or `bin/` comment to be `.planning/`-prefixed, resolve on disk, and sit within 20 characters of a double-quoted excerpt that is a real heading substring in that file. The Task A header rewrite is governed by this checker."
    - "`test/data/kamea.test.js`'s `SIGNED_OFF_GRIDS` is the exact-value source of truth that catches a dihedral orientation swap. The new invariant tests supplement it; they never replace it."
    - "The JSON working's field set is a published, snapshot-frozen contract. The attestation metadata must stay out of it, or every existing snapshot moves."
---

<objective>
Replace `src/data/kamea.js`'s provenance block with the state established by a cell-by-cell diff against a physical printed source, defend the data with structural invariants strong enough to catch the defect class that magic-sum checking cannot see, and add the three trans-Saturnian kameas as first-class planets carrying honest attestation labels.

Purpose: the current header honestly says five of seven grids rest on a single secondary web source with no independent corroboration. That is now out of date — all seven were diffed against Rankine & d'Este's printed appendix on 2026-08-12. The diff also found the thing that matters most: the book's Mercury square swaps two value-pairs between columns in a way that preserves every row sum AND every column sum. The repo's existing magic-sum-only verification could not have caught that defect had it landed the other way. Two harder invariants do catch it, and they belong in the suite as standing probes rather than as a one-time audit.

Output: an honest, current provenance record; per-grid permutation, full magic-sum (rows, columns, both diagonals), and associativity invariants that iterate the live kamea set; and Uranus, Neptune, and Pluto working through the same code path as the classical seven — with zero movement in any existing generated artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260812-m4b-kamea-provenance-from-physical-source-st/260812-m4b-CONTEXT.md
@.planning/STATE.md
@src/data/kamea.js
@src/render/glyphs.js
@test/data/kamea.test.js
</context>

<constraints>

**CONTEXT.md is locked reference data.** Its grids, magic constants, page numbers, attestation labels, and design decisions D-1 through D-4 are authoritative. Do not re-derive, re-verify against another source, or "correct" any digit. Copy the three grids character-for-character out of § "Authoritative grids — copy verbatim".

**Determinism is a published contract.** Output for saturn, jupiter, mars, sun, venus, mercury, and moon must be byte-identical after this change. No cell value changes anywhere in this plan. If any committed snapshot would move, that is a defect in the change, never a snapshot to update.

**The JSON working's field set does not change.** Attestation metadata stays in a sidecar map and out of `toWorking`'s output. Adding a field to the working would move every committed `.snap` and `.json` snapshot.

**Do not extend the determinism matrices in `test/determinism.test.js`.** Its six `describe.each(PLANETS)` matrices write file snapshots named per planet; extending `PLANETS` there would generate 18 new snapshot files for marginal signal. The invariant tests in Task B plus the render smoke check in Task C cover the three new planets adequately.

**Two surfaces are deliberately NOT extended in this plan, each for a stated reason. Record both as deferred items in the SUMMARY rather than closing them silently:**

1. `skill/SKILL.md`'s `--planet` flag row and its Planet Selection correspondence table. The skill invokes `@falkensmage/sigil-spinner@latest` from the npm registry, which resolves to `1.1.0` — seven planets. Listing ten there would make the skill instruct sessions to pass a planet the published CLI rejects. Reopen when a version containing these three planets is published. Independently, the correspondence table is Matt's lineage knowledge captured through a human gate (STATE.md § Pending Todos, "Matt's planet correspondences — lineage knowledge, not researchable") — Uranus/Neptune/Pluto rows cannot be invented here.
2. `test/skill-cli-parity.test.js` binds SKILL.md's table to the CLI's *option keys*, not to planet values, so it stays green either way. Confirm that rather than assume it.

</constraints>

<tasks>

<task type="auto">
  <name>Task A: Rewrite the SOURCE LINEAGE block in src/data/kamea.js</name>
  <files>src/data/kamea.js, test/citations.test.js</files>
  <precondition>`.planning/quick/260812-m4b-kamea-provenance-from-physical-source-st/260812-m4b-CONTEXT.md` exists on disk — the rewritten header cites it by path and `test/citations.test.js` R1 resolves that path against the filesystem.</precondition>
  <action>
Replace the `SOURCE LINEAGE (D-01, D-04)` block (roughly lines 4-45 of `src/data/kamea.js`) with a block recording the current verification state. Also update the `KAMEA_SET_VERSIONS` doc comment (roughly lines 79-94), which currently repeats the stale "independently cross-checked on only Saturn and Jupiter" claim.

Preserve the existing register exactly: plain, specific, refusing to claim more than was done. That tone is the file's virtue. Every factual claim below is drawn from CONTEXT.md; add nothing beyond it.

Record, at minimum:

1. The physical source with page numbers — Rankine, David & d'Este, Sorita, *Practical Planetary Magick*, Appendix 2 "The Kameas", pp. 177-181; classical seven on pp. 177-179. Note that p.177 is legible in the photograph and the remaining page numbers are per the developer's reading of the physical copy — that distinction is in CONTEXT.md and must survive.
2. The date of the diff (2026-08-12), what was diffed (every classical grid, cell by cell), and the four structural properties each grid was tested for.
3. Six of seven identical to the repo; Mercury the exception.
4. **Mercury** — the book prints, at rows 3-4, columns 5 and 8, two value-pairs swapped between columns (book row 3 has `48 ... 45` where the repo has `45 ... 48`; book row 4 has `25 ... 28` where the repo has `28 ... 25`). State why this matters: the swap preserves every row sum AND every column sum, which is exactly why magic-sum-only verification could not catch it. Then state the two proofs the repo is the correct one — the book's anti-diagonal sums 257 against a magic constant of 260, so as printed it is not a magic square; and it breaks associativity at those 4 cells and their 4 central-symmetry partners. The repo's Mercury is magic on all rows, columns and both diagonals and is associative in all 64 cells.
5. State explicitly that no cell value changes, so D-02 (add a new `KAMEA_SETS` key rather than mutate) does not fire and determinism is preserved.
6. **Sun** — neither the book's nor the repo's Sun is associative, and that is correct rather than a defect: order 6 is singly-even and the traditional Sun kamea genuinely lacks central symmetry. Both agree exactly.
7. **Construction rules — the nuance that must survive.** There is no "Agrippa formula" to cite. Agrippa presents the kameas in *De Occulta Philosophia* Book II as finished tables, not algorithms. The named construction methods postdate him: Agrippa wrote c.1510 (published 1531-33), while the odd-order "Siamese method" is named for Simon de la Loubère, who published it after his 1687 embassy to Siam. Rankine's claim (p.179) that he used "the same mathematical formula used to create the classical kameas" is his own assertion; the book does not publish the formula. So the odd-order and doubly-even rules stated in CONTEXT.md derive their authority from **fit, not lineage** — state the two rules and the grids each reproduces with zero deviation, and label them as empirically fitted.

Keep the `ORDERING CONVENTION (D-01)` and `GRID ORDER AND MAGIC CONSTANTS` sections; the latter's planet list is extended in Task C, not here.

**Citation-checker rules govern this rewrite** (`test/citations.test.js`, R1/R2). Three things follow:

- Do NOT delete the three existing `.md` citations — `"Pitfall 1: Kamea Orientation Ambiguity" in .planning/milestones/v1.0-research/PITFALLS.md`, `"Anti-Pattern 2: Kamea Cell Positions Hardcoded Outside \`data/kamea.js\`" in .planning/milestones/v1.0-research/ARCHITECTURE.md`, and the `"PKG-02 — Kamea version scheme and field shape" in .planning/milestones/v1.1-phases/05-publish-ready-source/05-CONTEXT.md` citation in the `KAMEA_SET_VERSIONS` comment. All three remain accurate and the checker enforces a floor on total citation sites.
- Every new `.md` token must be `.planning/`-prefixed, resolve on disk, and sit within 20 characters of a double-quoted excerpt that is a substring of a real heading line in that file — use the canonical `"<excerpt>" in <path>` form. Valid excerpts from the new CONTEXT file, copied exactly including em-dashes: `Verification performed 2026-08-12`, `Mercury — the repo is correct, the book is defective`, `Sun — non-associativity is expected, not a defect`, `Construction rules (empirically fitted, NOT cited to Agrippa)`, `Attestation of the three trans-Saturnian squares`.
- Raise `MINIMUM_CITATION_SITE_COUNT` in `test/citations.test.js` to the new total, per that constant's own doc comment ("A deliberate future addition to a source file's citations should RAISE this constant; it must never be lowered"). To read the real total mechanically: set the constant to `9999`, run `npx vitest run test/citations.test.js`, read the actual value out of the `toBeGreaterThanOrEqual` failure message, then set the constant to that value.

Reference the CONTEXT file for the photograph provenance rather than restating the image paths.
  </action>
  <verify>
    <automated>npx vitest run test/citations.test.js</automated>
    <automated>npm run typecheck &amp;&amp; npm run lint</automated>
    <automated>git diff --exit-code -- test/__file_snapshots__ test/render/__snapshots__</automated>
  </verify>
  <done>
The header names the printed source with page numbers; records the 2026-08-12 cell-by-cell diff and its four structural properties; names six exact matches and the Mercury divergence with both proofs (anti-diagonal 257, associativity break at 8 cells); records the Sun exemption reason; and labels the construction rules as empirically fitted with the Agrippa/de la Loubère dating reason. All three pre-existing `.md` citations survive. `test/citations.test.js` passes and `MINIMUM_CITATION_SITE_COUNT` is at or above the observed total. No cell value changed and no snapshot moved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task B: Structural invariant probes over every kamea in every set</name>
  <files>test/data/kamea.test.js</files>
  <behavior>
    - Every grid in every set in `KAMEA_SETS` is a permutation of 1..n^2 — n^2 distinct values, none missing, none repeated, none out of range.
    - Every grid: all n rows, all n columns, the main diagonal, and the anti-diagonal each equal the magic constant n(n^2+1)/2, computed from the order rather than transcribed from a lookup table.
    - Every grid except the Sun is associative: for every cell, v(r,c) + v(n-1-r, n-1-c) === n^2+1.
    - The Sun is exempted from associativity only, never from permutation or magic-sum, and the exemption is by explicit planet key with a written reason at the exemption site.
    - The Sun's exemption is proven to be a real exemption rather than a vacuous one: the Sun grid is asserted to actually violate associativity, so the exemption cannot silently outlive the condition that justifies it.
    - The suite iterates the live contents of `KAMEA_SETS`, so a kamea set or planet added later is covered with no new test written.
  </behavior>
  <action>
Add a `describe` block to `test/data/kamea.test.js` carrying the structural invariants. These are standing probes against silent-failure-class data — magic-sum checking alone is what would have let the Mercury-class defect through (see the Mercury paragraph the Task A header rewrite adds).

Drive the iteration off `Object.entries(KAMEA_SETS)` and, within each set, `Object.entries(setGrids)` — not off the local `PLANETS` array, and not off a hand-written list. A grid added later must be covered automatically; that is the point of the probe. Derive the magic constant as `(order * (order ** 2 + 1)) / 2` rather than reading `MAGIC_CONSTANTS`, so the invariant does not depend on a transcribed table.

Write the three checks as named helpers: an `isPermutationOfRange` check, a full magic-sum check covering rows, columns, and both diagonals, and an `isAssociative` check. Reuse the existing `sumsToConstant` helper if it is already correct for this purpose rather than writing a second one, but note that the existing helper takes the constant as an argument — pass the derived value.

The associativity exemption must be a named constant (an explicit set of planet keys, holding `sun` and nothing else) with a comment at that site explaining that order 6 is singly-even and the traditional Sun kamea genuinely lacks central symmetry, that both the printed source and the repo agree on this, and therefore the exemption is a property of the data being correct, not a suppressed failure. Without this exemption the suite fails on correct data.

Guard against the exemption going vacuous: add a test asserting the Sun grid genuinely violates associativity. If a future correction were ever to make the Sun associative, that test fails and someone re-reads the exemption instead of inheriting it forever.

Leave `SIGNED_OFF_GRIDS` and the existing exact-value and magic-sum describes untouched. The exact-value assertions are the source of truth for orientation; these invariants supplement them.

Run this task's tests BEFORE Task C so the probes are proven green against the known-good seven. Then prove they bite: temporarily swap two values within a single row of one grid in `src/data/kamea.js`, confirm the associativity test fails while the row-sum check still passes (that is the Mercury defect class reproduced), and revert. Do not commit the mutation.
  </action>
  <verify>
    <automated>npx vitest run test/data/kamea.test.js</automated>
    <automated>npm test</automated>
    <automated>git diff --exit-code -- src/data/kamea.js.orig 2>/dev/null; git status --porcelain -- test/__file_snapshots__ test/render/__snapshots__</automated>
  </verify>
  <done>
The invariant block passes against all seven classical grids. It iterates `KAMEA_SETS` rather than a hardcoded planet list. The Sun is exempted from associativity alone, by an explicit named key set carrying the singly-even reason inline, and a companion test asserts the Sun really is non-associative so the exemption cannot go vacuous. The fail-first mutation proof was run and reverted, and confirmed that a within-row value swap breaks associativity while leaving the row sum intact. The full suite is green and no snapshot moved.
  </done>
</task>

<task type="auto">
  <name>Task C: Add uranus, neptune, and pluto as first-class planets</name>
  <files>src/data/kamea.js, src/render/glyphs.js, src/render/coords.js, test/data/kamea.test.js, test/render/glyphs.test.js, test/cli/cli.test.js, test/render/grid.test.js, test/browser/element.test.js, skill/VERIFY.md, examples/element.html, README.md</files>
  <action>
Add the three trans-Saturnian kameas as first-class planets, per CONTEXT.md § "Design decisions (locked)" D-1 and D-2: `--planet uranus` works like any other planet, with no gating flag and no separate kamea set.

**C1 — the grids.** Copy `uranus` (11x11, M=671), `neptune` (12x12, M=870), and `pluto` (13x13, M=1105) verbatim out of CONTEXT.md § "Authoritative grids — copy verbatim" into `KAMEA_SETS.agrippa`, appended after `moon`. Do not retype from any other source. Do not adjust any digit. After pasting, the Task B invariants are the proof of a clean transcription — all three must pass permutation, full magic-sum, and associativity with no exemption.

**C2 — `PLANET_ORDER` and the enumeration surfaces inside `src/data/kamea.js`.** Append `'uranus', 'neptune', 'pluto'` to `PLANET_ORDER` after `'moon'`, preserving the existing seven in their existing order. Extend the `GRID ORDER AND MAGIC CONSTANTS` header list with the three new orders and constants, separated from the classical seven. Update `planetNames()`'s doc comment (it says "The seven classical planet names") and `gridSize()`'s doc comment ("3 for saturn through 9 for moon" → through 13 for pluto). Update `src/render/coords.js`'s matching JSDoc phrase at line 67; its arithmetic is order-generic and needs no logic change.

**C3 — attestation metadata as a sidecar.** `KAMEA_SETS[set]` is typed `Record<string, number[][]>` and every accessor indexes `set[key]` as a grid — folding metadata inside would break `gridSize`, `kameaGrid`, and `cellForNumber`. Follow the `KAMEA_SET_VERSIONS` precedent (D-60 chose a sidecar for exactly this reason): add a frozen, exported `PLANET_ATTESTATION` map keyed by planet, each value `{ traditional: boolean, attestation?: string }` — `attestation` optional, present only where it means something — placed next to `KAMEA_SET_VERSIONS`.

Per CONTEXT.md § "Attestation of the three trans-Saturnian squares": uranus and pluto are `traditional: false, attestation: 'attested'` (the book and the generated rule agree with zero differing cells); neptune is `traditional: false, attestation: 'derived'`.

**The seven classical planets carry `traditional: true` and NO `attestation` key at all.** The developer ruled on this directly: `attestation` answers "how do we know this non-traditional grid is right", a question that only arises where there is no traditional source to answer it. Tagging mercury `'corrected'` would misdescribe what happened — the repo grid was never changed; the book simply disagrees with it and is the defective one. Tagging it `'attested'` alongside the six exact matches would flatten that distinction instead. Mercury's divergence from the printed source is recorded in full sentences in the Task A citation block, which has the room to be accurate about it; a one-word enum does not.

The vocabulary is therefore two values — `'attested'` (an independent source and the fitted construction rule agree, zero differing cells) and `'derived'` (the fitted rule only, contradicting the sole printed source) — and it applies to `uranus`, `neptune`, and `pluto` only. Define that vocabulary, and the reason it is scoped to non-traditional grids, in the map's doc comment.

Add a lockstep test mirroring the existing D-61 `KAMEA_SET_VERSIONS` test at `test/data/kamea.test.js:152` — `PLANET_ATTESTATION`'s key set must equal `planetNames()`'s, so a planet added later without an attestation fails on whoever introduces the mismatch.

**C4 — the Neptune divergence note, at the point of divergence.** Immediately above the `neptune` grid literal, record that this square is generated and contradicts the printed book, and why the generated form was preferred. A future reader comparing against p.181 will notice the difference and must find the reason immediately. From CONTEXT.md § "Neptune — why the printed square was rejected": the book's printed Neptune prints `34` twice with `35` absent and `69` twice with `68` absent; even after repairing both typos it still fails, with columns 8 and 9 summing 869 and 871 against a magic constant of 870, and 4 cells breaking associativity. The generated square is clean on every test. Then record the honest caveat verbatim in substance: Neptune is the weakest link in the whole chain — it extrapolates a rule fitted at n=4 and n=8 out to n=12, and it disagrees structurally with the sole printed source (a column-block swap over and above the typos). The preference for the generated form is a sound inference, not provenance.

**C5 — glyphs.** Add `uranus` U+2645 ♅, `neptune` U+2646 ♆, and `pluto` U+2647 ♇ to `PLANET_GLYPHS` in `src/render/glyphs.js`, each carrying the same U+FE0E variation selector, in `PLANET_ORDER` sequence. Update that module's header: the cited-code-point list, the "seven classical planets" phrasing, and — as CONTEXT.md flags — the sentence claiming the selector "is a no-op on the other five glyphs", whose count is now wrong. Re-check the underlying claim rather than just renumbering it: confirm that none of U+2645/2646/2647 carries the Unicode `Emoji` property, so Venus and Mars remain the only two entries with real emoji-presentation risk and the count becomes eight. Update `test/render/glyphs.test.js`'s local `PLANET_ORDER` and `EXPECTED_BASE_CODE_POINT`, and its "closed seven-entry map" / "exactly seven entries" / "seven mutually distinct values" wording. Also re-check `src/render/svg.js`'s comment at roughly line 272 claiming none of the code points needs XML escaping — the claim still holds for the three additions, but its stated count does not.

**C6 — the invalid-planet fixture, the load-bearing half of this task.** The literal `pluto` is the repo's incumbent stand-in for an invalid planet in 15 places. Promoting Pluto silently inverts every one of them; several assert a throw that will no longer happen, and `examples/element.html`'s deliberate error-state demo would start rendering a real sigil. Sweep them all and replace the fixture with `nibiru` — chosen because it names no real body and therefore can never be promoted the way Pluto just was. Known sites, to be re-derived by grep rather than trusted as complete: `test/data/kamea.test.js` (3), `test/cli/cli.test.js` (8), `test/render/grid.test.js` (1), `test/browser/element.test.js` (1), `skill/VERIFY.md` (2), `examples/element.html` (1). Run a repo-wide search for the token before declaring the sweep done, and confirm each remaining occurrence is a genuine reference to the planet rather than a leftover fixture.

**C7 — the error message, and D-3 against the surface that actually exists.** CONTEXT.md D-3 asks for classical and modern to be separated in `--help`. This CLI has no `--help` — `skill/SKILL.md:59` documents its absence and `bin/sigil-spinner.js` delegates all planet validation to the library. The real surface where a user meets the planet enumeration is `resolvePlanetKey`'s `E_UNKNOWN_PLANET` message, which today ends `Valid planets: ${PLANET_ORDER.join(', ')}`. Implement D-3 there: separate the classical seven from the modern three, labelling the latter non-traditional, deriving both lists from `PLANET_ORDER` and `PLANET_ATTESTATION` rather than transcribing them. `test/cli/cli.test.js`'s existing assertion only checks that each planet name appears in stderr, so containment still holds — but grep for any test asserting the literal `Valid planets` phrasing and update it. Update `skill/VERIFY.md`'s two documented error-output lines to match, and its following prose asserting the message "names all seven valid planets". Note in `VERIFY.md` that the planet list in that output reflects the installed version, since that document describes the behaviour of whatever `@latest` resolves to.

**C8 — the remaining doc and demo surfaces.** `README.md`: the `planet` parameter list (~line 41); the provenance section (~lines 700-724), which repeats the now-superseded single-web-source account and must be brought into line with Task A's rewritten header, including the grid-order/magic-constant list and the three new planets with their attestation labels; and any error-table row quoting the unknown-planet message. `examples/element.html`: add the three to the live planet `<select>`, and fix the deliberate-error card per C6.

**C9 — smoke-prove the three end to end.** Add a small test asserting `generateSigil('I WILL SUCCEED', planet)` succeeds for each of uranus, neptune, and pluto, producing a `class="sigil sigil--<planet>"` root, a path layer, and coordinate output containing no `NaN`, no `Infinity`, and no exponential notation — Pluto's 13x13 is the finest grid the renderer has ever been given, and `src/render/svg.js`'s stroke-width and font-size fallback reasoning is written against Moon's 9x9 as the finest case. Do not add these to `test/determinism.test.js`'s file-snapshot matrices.
  </action>
  <verify>
    <automated>npx vitest run test/data/kamea.test.js test/render/glyphs.test.js</automated>
    <automated>CI=true npm test</automated>
    <automated>npm run typecheck &amp;&amp; npm run lint</automated>
    <automated>git status --porcelain -- test/__file_snapshots__ test/render/__snapshots__</automated>
    <automated>git diff --exit-code -- test/__file_snapshots__ test/render/__snapshots__</automated>
    <automated>node bin/sigil-spinner.js 'I WILL SUCCEED' --planet neptune | head -c 80</automated>
  </verify>
  <done>
`KAMEA_SETS.agrippa` holds ten grids; the three new ones pass permutation, full magic-sum, and associativity under Task B's probes with no exemption. `PLANET_ORDER` lists ten, classical seven first and in their original order. `PLANET_ATTESTATION` is frozen, covers all ten with `traditional`, and its key set is bound to `planetNames()` by a lockstep test; the `attestation` key appears on `uranus`, `neptune`, and `pluto` only, and on no classical planet. The Neptune divergence and its rationale sit immediately above the Neptune grid. `PLANET_GLYPHS` holds ten entries, each two code points ending in U+FE0E, with the emoji-risk comment re-counted and re-verified rather than renumbered. No `pluto`-as-invalid-fixture site remains, verified by repo-wide grep. The unknown-planet message separates classical from modern per D-3. `CI=true npm test` is green with zero snapshot writes, `git diff` over both snapshot directories is empty, `git status --porcelain` reports no untracked files there, and `--planet neptune` emits an SVG root on stdout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CONTEXT.md → source data | Grid literals are transcribed by hand from a machine-generated reference into shipped, published data |
| CLI argv / library argument → `resolvePlanetKey` | Caller-supplied planet name crosses into data lookup |
| Repo HEAD → published npm artifact | The skill and VERIFY.md describe a registry version that lags this change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m4b-01 | Tampering | Grid transcription in `src/data/kamea.js` | high | mitigate | Task B's permutation + full magic-sum + associativity probes run over the live `KAMEA_SETS`, so a mis-keyed digit in any of the 313 new cells fails the suite. This is the defect class the Mercury finding proved magic-sum-only checking cannot see. |
| T-m4b-02 | Tampering | Existing seven planets' generated output | critical | mitigate | Purely additive change; `CI=true npm test` refuses to write snapshots, and `git diff --exit-code` plus `git status --porcelain` over both snapshot directories prove byte-identical artifacts. |
| T-m4b-03 | Information disclosure | Provenance overstatement in the rewritten header | medium | mitigate | Every claim is sourced to CONTEXT.md; the empirically-fitted / not-Agrippa nuance and the Neptune weakest-link caveat are required content, not optional colour. |
| T-m4b-04 | Repudiation | `pluto` inverting from invalid fixture to valid planet | high | mitigate | C6 sweeps all 15 known sites, re-derives the list by grep, and replaces the fixture with a name that can never be promoted. Several affected tests fail loudly if missed; `examples/element.html`'s error demo does not, which is why the grep is mandatory rather than trusting the suite. |
| T-m4b-05 | Spoofing | Skill instructing a session to pass a planet the published CLI rejects | medium | accept | SKILL.md is deliberately not extended; recorded as a deferred item with the reopen condition (publication of a version carrying these planets). Accepted because the alternative — documenting ten planets against a seven-planet registry artifact — is the worse failure. |
| T-m4b-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan; `dependencies` stays empty. |
</threat_model>

<verification>
- `CI=true npm test` green, including the browser suite (requires `npx playwright install chromium` if this is a fresh tree).
- `npm run typecheck` and `npm run lint` exit 0.
- `git diff --exit-code -- test/__file_snapshots__ test/render/__snapshots__` exits 0, and `git status --porcelain` over those paths is empty — no snapshot changed, none was added.
- `npm run test:pack` still passes; `dependencies` in package.json remains empty.
- Repo-wide grep confirms no invalid-planet fixture still uses a now-valid planet name.
- `node bin/sigil-spinner.js 'I WILL SUCCEED' --planet uranus`, `--planet neptune`, and `--planet pluto` each emit an SVG root and exit 0.
</verification>

<success_criteria>
- The `src/data/kamea.js` header records the physical-source verification honestly and completely: source with pages, six exact matches, the Mercury divergence with both proofs, the Sun exemption reason, and the empirically-fitted construction rules with the Agrippa/de la Loubère dating.
- Permutation, full magic-sum (rows, columns, both diagonals), and associativity are standing tests that iterate the live kamea set; the Sun is exempted from associativity alone, with a written reason and a guard against the exemption going vacuous.
- Uranus, Neptune, and Pluto generate sigils through the same code path as the classical seven, carry `traditional: false` plus an attestation label, and Neptune's divergence from the printed source is documented where a reader will hit it.
- Every generated artifact for the existing seven is byte-identical, proven mechanically rather than asserted.
</success_criteria>

<output>
Create `.planning/quick/260812-m4b-kamea-provenance-from-physical-source-st/260812-m4b-SUMMARY.md` when done.

Record in it, explicitly:
- That `attestation` is scoped to the three non-traditional planets and no classical planet carries it — the developer ruled on this directly after the plan was written, vetoing an earlier `attestation: 'corrected'` label for mercury. Record it as settled, not as an open call.
- The two deferred surfaces (SKILL.md's flag row and correspondence table) with their stated reopen conditions.
- The mechanical evidence that no snapshot moved.
</output>
