---
phase: 05-publish-ready-source
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/data/kamea.js
  - src/generate.js
  - src/render/json.js
  - README.md
  - test/data/kamea.test.js
  - test/determinism.test.js
  - test/render/json.test.js
  - test/cli/cli.test.js
  - test/render/__snapshots__/json.test.js.snap
  - test/__file_snapshots__/worked-example.working.json
autonomous: true
requirements: [PKG-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PKG-02 / D-58 / D-59: a saved JSON working carries `kameaVersion` as a string field positioned immediately after `kameaSet`, so the working names both the kamea set and its version"
    - "PKG-02 / D-57: the emitted `kameaVersion` value is `2026-08-04` — the D-04 provenance sign-off date, not a semver"
    - "PKG-02 / D-60: the value is read from `KAMEA_SET_VERSIONS`, a frozen sidecar map keyed by set name, living beside `DEFAULT_KAMEA_SET` in `src/data/kamea.js`; `KAMEA_SETS`'s own shape is unchanged"
    - "PKG-02 / D-61: `Object.keys(KAMEA_SET_VERSIONS)` and `Object.keys(KAMEA_SETS)` are asserted to be the same set, so adding a kamea set without a version fails the suite on whoever added it"
    - "PKG-02: `kameaVersion` is a static in-source literal — the three files that produce it perform no filesystem read, no environment lookup, no clock call, no subprocess, and import no manifest, asserted by a committed guard rather than a one-time check"
    - "PKG-02: repeating the identical statement and planet produces byte-identical JSON working output across runs"
    - "PKG-02 (adjacency edge): the working has exactly 16 distinct keys; `kameaSet` and `kameaVersion` are separate sibling string fields — `kameaSet` is NOT merged into an object and no nested `kamea` block is introduced"
    - "PKG-02 (empty edge): a kamea set with no version entry cannot reach the working — the D-61 parity test fails first, so no working ever emits an undefined or absent `kameaVersion`"
    - "PKG-02 (encoding edge): the `kameaVersion` value in the emitted JSON is byte-identical to the source literal in `KAMEA_SET_VERSIONS`, compared against the imported constant rather than a transcribed copy"
    - "PKG-02 (ordering edge): the full 16-key working order is asserted explicitly as one list, replacing the previous prefix-plus-appends framing that an inserted key contradicts"
    - "Exactly the two JSON-shaped snapshots are rebased; all 46 SVG-shaped snapshots are byte-unchanged by this plan"
    - "`package.json` `dependencies` remains an empty object"
  artifacts:
    - path: "src/data/kamea.js"
      provides: "KAMEA_SET_VERSIONS frozen sidecar map (D-60)"
      contains: "KAMEA_SET_VERSIONS"
    - path: "src/render/json.js"
      provides: "kameaVersion emitted immediately after kameaSet, plus the matching JSDoc @property on SigilWorking and GeneratePipelineResult"
      contains: "kameaVersion"
    - path: "README.md"
      provides: "The working table's sixteenth field row, positioned after kameaSet to match the emitted key order"
      contains: "kameaVersion"
    - path: "test/data/kamea.test.js"
      provides: "D-61 key-parity assertion between KAMEA_SETS and KAMEA_SET_VERSIONS"
      contains: "KAMEA_SET_VERSIONS"
  key_links:
    - from: "src/generate.js"
      to: "src/data/kamea.js"
      via: "imports KAMEA_SET_VERSIONS and indexes it by DEFAULT_KAMEA_SET in the toWorking call"
      pattern: "KAMEA_SET_VERSIONS\\[DEFAULT_KAMEA_SET\\]"
    - from: "src/render/json.js"
      to: "src/generate.js"
      via: "destructures kameaVersion from the pipeline result and re-emits it in fixed key order"
      pattern: "kameaVersion"
    - from: "README.md"
      to: "src/render/json.js"
      via: "the working table is transcribed from the SigilWorking JSDoc typedef, its declared single source of truth"
      pattern: "SigilWorking"
  prohibitions:
    - statement: "MUST NOT derive the kamea version at build time or run time — not from the filesystem, the environment, the clock, a subprocess, or the package manifest. A version that varies by where the code runs destroys the byte-determinism guarantee this project's core value rests on"
      category: transparency
      requirement_id: PKG-02
      status: resolved
      verification: judgment
    - statement: "MUST NOT present `kameaVersion` as a correctness warranty for the kamea cells. The value is a provenance sign-off — these cells, verified to this degree, as of this date. Five of the seven grids rest on a single web source, magic-sum verified but not independently cross-checked, and the documentation of this field must not imply otherwise"
      category: transparency
      requirement_id: PKG-02
      status: resolved
      verification: judgment
---

<objective>
Add the kamea set's version to the JSON working as a static in-source constant, so a working saved today can be traced years from now to exactly the kamea data and verification state that produced it.

Purpose: `npm publish` is irreversible and versions are never reusable. `kameaVersion` becomes part of the documented working contract at v1.0.0 — the field name, its value format, and its position all ship permanently. Phase 5 exists so that contract is right before the version number becomes permanent, rather than needing a version bump to fix.

Output: `KAMEA_SET_VERSIONS` in `src/data/kamea.js`, `kameaVersion` threaded through `src/generate.js` and emitted by `src/render/json.js`, the working contract documented in README, and the test surface — including the D-61 parity guard and a committed determinism guard — updated with exactly two snapshots rebased.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-publish-ready-source/05-CONTEXT.md
@.planning/phases/05-publish-ready-source/05-RESEARCH.md
@.planning/phases/05-publish-ready-source/05-PATTERNS.md
@.planning/phases/05-publish-ready-source/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

New in Phase 05 — none of it exists in the pre-phase tree, so drift verification must not flag these as unacknowledged changes.

**Created by this plan (05-02):**
- `KAMEA_SET_VERSIONS` — new exported frozen constant in `src/data/kamea.js`
- `kameaVersion` — new field on the JSON working; new `@property {string} kameaVersion` on both the `SigilWorking` and `GeneratePipelineResult` JSDoc typedefs in `src/render/json.js`; new row in README's working table

**Created elsewhere in Phase 05:**
- `test/citations.test.js` (plan 05-01)
- `--title` CLI flag and the `titleArg` local in `bin/sigil-spinner.js` (plan 05-03)
- `role="img"` / `aria-labelledby` on the root `<svg>`, and `id` on the `<title>` element, conditionally emitted by `src/render/svg.js` (plan 05-03)
- `test/browser/accessible-name.test.js` (plan 05-03)

## The sixteen-key working order

The authored emission order after this plan, as `toWorking` returns it. This full list replaces the prefix-plus-appends framing currently in `test/determinism.test.js`, which an *inserted* key contradicts by construction.

`statement`, `planet`, `kameaSet`, `kameaVersion`, `gridSize`, `lettersKept`, `lettersStruck`, `letterNumbers`, `numbers`, `cells`, `segments`, `start`, `end`, `keptTrail`, `repeats`, `render`

Note the two snapshots serialize differently and both are correct: `test/__file_snapshots__/worked-example.working.json` is `JSON.stringify` output and preserves the authored order above (so the new line follows `"kameaSet"` at line 4). `test/render/__snapshots__/json.test.js.snap` is Vitest's own serializer, which sorts keys alphabetically — `kameaSet` < `kameaVersion` < `keptTrail`, so the new line also follows `"kameaSet"` at line 39. The positions coincide; the reasons differ.

<tasks>

<task type="tracer">
  <name>Task 1: kameaVersion end to end — constant, thread, emit, document</name>
  <reversibility rating="one-way">D-57, D-58 and D-59 ship `kameaVersion: '2026-08-04'` as a sibling of `kameaSet` in published JSON output from v1.0.0 onward; removing or restructuring it later breaks every consumer reading it and invalidates workings already embedded in pages, with no migration path for artifacts already saved. The human chose this exact shape in discuss-phase, so no checkpoint re-asks it — the rating is recorded so a later phase sees the weight.</reversibility>
  <files>src/data/kamea.js, src/generate.js, src/render/json.js, README.md</files>
  <read_first>
    - `src/data/kamea.js` lines 1-95 — the D-01/D-04 source-lineage block whose `2026-08-04` sign-off date is the value, and the `DEFAULT_KAMEA_SET` / `KAMEA_SETS` declarations the new map sits between
    - `src/render/glyphs.js` lines 45-55 — `PLANET_GLYPHS` is the established `Object.freeze` closed-lookup-map convention this new constant copies
    - `src/generate.js` line 14 (the kamea import) and lines 275-295 (the `toWorking` call)
    - `src/render/json.js` lines 30-110 — the `SigilWorking` and `GeneratePipelineResult` typedefs and the `toWorking` destructure-and-re-emit body
    - `README.md` lines 271-300 — the working table and its stated contract that the table is transcribed from the `SigilWorking` typedef in emitted key order
  </read_first>
  <action>
Wire the kamea version from its source constant all the way to the emitted JSON working, on one real path, and document the contract in the same change.

In `src/data/kamea.js`, declare `KAMEA_SET_VERSIONS` immediately after `DEFAULT_KAMEA_SET` and before the `KameaSet` typedef. It is a frozen object literal mapping set name to version string, with the single entry keyed `agrippa` and valued `2026-08-04`. Freeze it with `Object.freeze`, matching the `PLANET_GLYPHS` convention. Leave `KAMEA_SETS` entirely alone — D-60 explicitly rejected folding the version into those entries, because every existing accessor indexes that shape directly.

The doc comment on the new constant must say what the value means: a provenance sign-off date naming the verification state of these cells as of that date, not a release cadence and not a warranty that the cells are correct. Point it at the source-lineage block above it, which records that five of the seven grids rest on a single web source, magic-sum verified but not independently cross-checked. Add the citation in the canonical form plan 05-01 installed — a quoted heading excerpt plus a fully-qualified path — because `test/citations.test.js` is now enforcing it.

In `src/generate.js`, add `KAMEA_SET_VERSIONS` to the existing kamea import on line 14, and insert `kameaVersion: KAMEA_SET_VERSIONS[DEFAULT_KAMEA_SET],` into the `toWorking` object literal immediately after the existing `kameaSet: DEFAULT_KAMEA_SET,` line. No new function, no new parameter, no new threading path — this is a sibling field on a call that already exists.

In `src/render/json.js`, add `kameaVersion` to `toWorking`'s destructure immediately after `kameaSet`, and to the returned object literal immediately after `kameaSet`. Then add `@property {string} kameaVersion` lines to *both* the `SigilWorking` and the `GeneratePipelineResult` typedefs, each positioned immediately after that typedef's existing `kameaSet` property line. The description text should carry the provenance framing and cite D-57, matching how the neighbouring properties cite their own decisions.

In `README.md`, add a `kameaVersion` row to the working table immediately after the `kameaSet` row, typed `string`, with a description that says it names the kamea set's provenance sign-off date and cites D-57 — and that does not claim the cells are verified correct. Also add `[--title]` is NOT this plan's job; leave the CLI section alone. Keep the table's column alignment consistent with the existing rows.

The value must be a literal in the frozen map. Do not compute it, do not read it from anywhere at run time, and do not import the package manifest to obtain it — a version that varies by where the code runs breaks the byte-determinism this project's core value rests on.

Verify end to end by running the real CLI once with `--json` and confirming the emitted working carries `kameaVersion` with the expected value, positioned directly after `kameaSet` in the serialized output.
  </action>
  <verify>
    <automated>node bin/sigil-spinner.js "I WILL SUCCEED" --planet saturn --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const w=JSON.parse(s);const k=Object.keys(w);if(k.length!==16)throw new Error('key count '+k.length);if(k[2]!=='kameaSet'||k[3]!=='kameaVersion')throw new Error('order '+k.slice(0,5));if(w.kameaVersion!=='2026-08-04')throw new Error('value '+w.kameaVersion);if(typeof w.kameaSet!=='string')throw new Error('kameaSet not a string');console.log('ok',k[3],w.kameaVersion)})"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/kamea.js` contains `export const KAMEA_SET_VERSIONS = Object.freeze(` and the string `2026-08-04`
    - `src/data/kamea.js` still declares `KAMEA_SETS` as a plain name-to-grids map — `node -e "import('./src/data/kamea.js').then(m=>{if(typeof m.KAMEA_SETS.agrippa.saturn[0][0]!=='number')process.exit(1)})"` exits 0
    - `src/generate.js` contains `KAMEA_SET_VERSIONS[DEFAULT_KAMEA_SET]`
    - `src/index.js` is unchanged and does NOT re-export `KAMEA_SET_VERSIONS` — the constant stays internal, matching the precedent that `DEFAULT_KAMEA_SET` is not exported from the public barrel either. `kameaVersion` reaches consumers through the working, never as a standalone import. Exporting it later is additive and non-breaking; exporting it now would add unrequested public surface immediately before an irreversible publish. Verify with `node -e "import('./src/index.js').then(m=>{if('KAMEA_SET_VERSIONS' in m) process.exit(1)})"` exiting 0.
    - `src/render/json.js` contains `kameaVersion` in both the destructure and the returned literal, and contains two occurrences of `@property {string} kameaVersion`
    - Running the CLI with `--json` emits a working whose keys number exactly 16, with `kameaSet` at index 2 and `kameaVersion` at index 3
    - The emitted `kameaVersion` equals `2026-08-04`
    - `working.kameaSet` is still a JSON string, not an object — no nested `kamea` block exists in the output
    - Two consecutive CLI `--json` runs on the same input produce byte-identical stdout
    - `README.md` contains a `kameaVersion` table row and it appears after the `kameaSet` row in file order
    - `npx vitest run test/citations.test.js` exits 0 — the new doc comment's citation is in canonical form
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>A real CLI invocation emits a 16-key working carrying `kameaVersion: "2026-08-04"` directly after `kameaSet`, the value traces to a frozen in-source literal, and the contract is documented in README.</done>
</task>

<task type="auto">
  <name>Task 2: Test surface, parity guard, determinism guard, and the two-snapshot rebase</name>
  <files>test/data/kamea.test.js, test/determinism.test.js, test/render/json.test.js, test/cli/cli.test.js, test/render/__snapshots__/json.test.js.snap, test/__file_snapshots__/worked-example.working.json</files>
  <read_first>
    - `test/data/kamea.test.js` lines 1-5 and 125-140 — the import list and the existing `KAMEA_SETS` / `DEFAULT_KAMEA_SET` resolver-behavior assertions the parity test sits beside
    - `test/determinism.test.js` lines 235-260 — the hardcoded `phase1Order` array and the prefix-plus-appends assertion that an inserted key invalidates
    - `test/render/json.test.js` lines 1-35 and 85-95 — the `buildPipelineResult` fixture and the per-field `kameaSet` assertion
    - `test/cli/cli.test.js` lines 30-70 — the `runCli` harness and the `--json` working-fields assertion block
    - `test/__file_snapshots__/worked-example.working.json` lines 1-8
    - `test/render/__snapshots__/json.test.js.snap` lines 35-42
    - `test/render/svg.test.js` lines 296-311 — the established pattern for a test that reads a source file and asserts on its text, which the determinism guard follows
  </read_first>
  <action>
Close the test surface around the new field, then rebase exactly the two snapshots that carry it.

**D-61 parity test.** In `test/data/kamea.test.js`, extend the kamea import to include `KAMEA_SET_VERSIONS` and add an assertion in the resolver-behavior describe that the sorted key list of `KAMEA_SET_VERSIONS` equals the sorted key list of `KAMEA_SETS`. This is the one gap the sidecar shape opens: a future kamea set added without a version entry would otherwise emit an absent version into someone's published working years later. The test makes it fail on whoever adds the set. D-61 chose the test over a runtime throw in `resolveSet`, so do not also add a throw — the test makes the runtime case unreachable in practice.

**Key-order assertion rewrite.** The test in `test/determinism.test.js` currently asserts that a hardcoded twelve-key Phase 1 prefix is intact and that later phases only *appended* after it. `kameaVersion` is inserted at index 3, which contradicts that framing rather than merely extending it. Replace the prefix-plus-appends structure with a single assertion that the working's key list equals the full sixteen-key order given in § The sixteen-key working order above. Update the test name and the surrounding comment to say that the working's documented key order is now asserted whole, and that PKG-02's insertion is why. This is a strictly stronger assertion than the one it replaces — it pins the entire contract instead of a prefix.

**Determinism guard.** Also in `test/determinism.test.js`, add a committed guard that reads the source text of the three files that produce this field — the kamea data module, the orchestrator, and the JSON serializer — and asserts none of them contains a filesystem read call, an environment lookup, a clock call, a subprocess call, or an import whose specifier resolves to a JSON manifest. Strip comment lines before matching, exactly as `test/render/svg.test.js` already does for its own source-introspecting assertion, so a doc comment describing the prohibition does not trip the guard. Name the test so it states what it protects: the kamea version is a static in-source literal, and any of these constructs appearing in these three files is itself the defect signal. Add a second assertion that generating twice from identical input yields byte-identical `JSON.stringify` output.

**Fixture and per-field assertions.** In `test/render/json.test.js`, extend the kamea import to include `KAMEA_SET_VERSIONS` and add `kameaVersion: KAMEA_SET_VERSIONS[DEFAULT_KAMEA_SET],` to the `buildPipelineResult` return literal immediately after its `kameaSet` entry. Then extend the existing `kameaSet` per-field assertion so it also asserts `working.kameaVersion` equals `KAMEA_SET_VERSIONS[DEFAULT_KAMEA_SET]` — compare against the imported constant, not a transcribed copy of the date string, so the test cannot drift out of agreement with the source. In `test/cli/cli.test.js`, add a `kameaVersion` assertion to the `--json` working-fields block beside the existing `kameaSet` one, proving the field survives the CLI surface.

**Snapshot rebase.** Rebase the two JSON-shaped snapshots. In `test/__file_snapshots__/worked-example.working.json` the new line follows the `"kameaSet"` line, because that file preserves `JSON.stringify`'s authored order. In `test/render/__snapshots__/json.test.js.snap` the new line also follows the `"kameaSet"` line, because Vitest's serializer sorts keys alphabetically and `kameaVersion` sorts between `kameaSet` and `keptTrail`. Both are one added line. Review the resulting diff before committing: it must be exactly two files, one added line each.

**Attribution gate.** Confirm no SVG-shaped snapshot moved. There are 45 `.svg` file snapshots plus one entry in the SVG snapshot file; none of them contains the working's keys, and PKG-02 emits no SVG change, so every one of them must be byte-unchanged. Assert this from git rather than by inspection.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && git diff --name-only -- test/__file_snapshots__ test/render/__snapshots__ | sort | tr '\n' ' '</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with no snapshot written in update mode — the suite passes against the committed snapshots
    - `test/data/kamea.test.js` asserts sorted-key equality between `KAMEA_SET_VERSIONS` and `KAMEA_SETS`; temporarily adding a stub key to `KAMEA_SETS` makes that test fail, and removing the stub makes it pass again
    - `test/determinism.test.js` no longer contains a `phase1Order` prefix array, and asserts a single sixteen-element key list
    - `test/determinism.test.js` contains a source-introspecting guard naming `src/data/kamea.js`, `src/generate.js` and `src/render/json.js`, and that guard fails if a filesystem read call is inserted into any of the three
    - `test/render/json.test.js` compares `working.kameaVersion` against `KAMEA_SET_VERSIONS[DEFAULT_KAMEA_SET]` rather than a literal date string
    - `test/cli/cli.test.js` asserts `working.kameaVersion` in its `--json` block
    - `git diff --name-only -- test/__file_snapshots__ test/render/__snapshots__` lists exactly `test/__file_snapshots__/worked-example.working.json` and `test/render/__snapshots__/json.test.js.snap`
    - `git diff --numstat -- test/__file_snapshots__/worked-example.working.json test/render/__snapshots__/json.test.js.snap` shows 1 insertion and 0 deletions for each
    - `git diff --name-only -- test/__file_snapshots__/*.svg test/render/__snapshots__/svg.test.js.snap` is empty
    - `npx vitest run test/citations.test.js` exits 0 — plan 05-01's checker still passes
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
    - `node -e "const p=require('./package.json'); if (Object.keys(p.dependencies ?? {}).length !== 0) process.exit(1)"` exits 0
  </acceptance_criteria>
  <done>The parity guard, the whole-order assertion, the determinism guard and the per-field assertions are committed; exactly two snapshots gained exactly one line each; all 46 SVG-shaped snapshots are byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library → emitted JSON working | The working is a durable artifact that downstream tooling and future readers parse. Its field set and key order are a published contract from v1.0.0. |
| source constant → published artifact | `kameaVersion`'s value crosses from source into every saved working. Anything that makes that value environment-dependent makes the artifact non-reproducible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | `kameaVersion` derived at build time or run time in `src/data/kamea.js`, `src/generate.js` or `src/render/json.js` | high | mitigate | Value is a literal in a frozen sidecar map; a committed source-introspecting guard in `test/determinism.test.js` asserts none of the three files contains a filesystem read, environment lookup, clock call, subprocess call, or manifest import, and a byte-identity assertion covers repeat runs |
| T-05-05 | Repudiation | A provenance stamp that cannot be tied to the data it versions — a kamea set with no version entry, or a version entry for no set | medium | mitigate | D-61 key-parity assertion between `KAMEA_SET_VERSIONS` and `KAMEA_SETS` in `test/data/kamea.test.js`, failing on whoever introduces the mismatch |
| T-05-06 | Spoofing | The version read as a correctness warranty for the kamea cells, when five of seven grids are magic-sum verified against a single web source and not independently cross-checked | medium | mitigate | The constant's doc comment, the JSDoc `@property` description, and the README row all frame it as a provenance sign-off date and cite the source-lineage block; recorded as prohibition P2 in `must_haves.prohibitions` |
| T-05-07 | Information Disclosure | The working already carries the untrusted intention statement verbatim; adding a field must not widen what is emitted | low | accept | `kameaVersion` is a source constant carrying no user input. The pre-existing `statement` field is documented behavior under README's Data Handling section and is unchanged by this plan. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero packages and adds zero dependencies. `package.json` `dependencies` is empty before and after, asserted mechanically in both tasks' acceptance criteria. No package-legitimacy audit is required because there are no packages to audit. |
</threat_model>

<verification>
1. A real CLI `--json` invocation emits a 16-key working with `kameaSet` at index 2 and `kameaVersion` at index 3, valued `2026-08-04`.
2. Two consecutive identical invocations produce byte-identical stdout.
3. The committed determinism guard in `test/determinism.test.js` fails when a filesystem read is inserted into any of the three producing files, and passes otherwise.
4. The D-61 parity assertion fails when a kamea set is added without a version entry.
5. `git diff --name-only` over the snapshot directories lists exactly the two JSON-shaped files, one insertion each; the 46 SVG-shaped snapshots are byte-unchanged.
6. `npm test`, `npm run typecheck`, `npm run lint` and `npx vitest run test/citations.test.js` all exit 0.
7. `package.json` `dependencies` is an empty object.

**Boundary note on "identical whether run from the dev tree or an installed package"** (phase success criterion 5): this plan proves the *mechanism* — the value is a frozen source literal and the producing files perform no runtime derivation, so `src/` shipping verbatim makes dev-tree and installed output identical by construction. The end-to-end proof against a real packed tarball is Phase 6's PKG-03 smoke test and is deliberately not duplicated here.
</verification>

<success_criteria>
- The JSON working names both the kamea set and its version, satisfying phase success criterion 1.
- The value is a static in-source constant with a committed guard against runtime derivation.
- Adding a kamea set without a version fails the suite loudly at the moment of introduction.
- Exactly two snapshots rebased, both traceable to PKG-02; zero SVG snapshot movement.
</success_criteria>

<output>
Create `.planning/phases/05-publish-ready-source/05-02-SUMMARY.md` when done.
</output>
